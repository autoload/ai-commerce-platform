# Project Status

This document is the persistent source of truth for the current development state of the AI Commerce Platform. It exists so that development can continue correctly across sessions without relying on prior conversation context. Read this before starting significant work; update it after completing a significant phase.

## Phase Roadmap (Canonical)

This is the single authoritative numbering for development phases in this document. Earlier drafts of this file briefly used a second, restarted 0–3 counter for Customer Authentication, the Catalog API, the Database Design 2.6 migration, and STEP 3A–3D — all of that work is Phase 5 below. The historical STEP 3A/3B/3C/3D sub-labels are preserved unchanged inside Phase 5.

| Phase | Description | Status |
|---|---|---|
| 0 | Architecture & Database Design | COMPLETE |
| 1 | Infrastructure Bootstrap | COMPLETE |
| 2 | Database Migrations (2A–2F) | COMPLETE |
| 3 | Foundation Application Layer | COMPLETE |
| 4 | Back Office Feature Blocks | COMPLETE |
| 5 | Checkout / Payment / Inventory | COMPLETE |
| 6 | Payment Expiry Sweep | COMPLETE |
| 7 | Customer-facing Order History | COMPLETE |
| 8 | Frontend Storefront (Blocks 8-0/8A/8C/8B(revised)/8D/8E/8F) | COMPLETE |
| 9A | Organization Lifecycle Management (Platform Admin approve/reject/suspend/reactivate) | COMPLETE |
| 9B | Category Management (merchant Category CRUD) | COMPLETE |
| 9C | Customer Management (merchant-facing, read-only Customer list/detail) | COMPLETE |
| 9D | Refunds (merchant full-refund initiation, Stripe PaymentIntent refund, webhook-authoritative reconciliation) | COMPLETE |
| 9E-1 | G3-A — Detect & Alert (payment succeeds after Order already left Pending, merchant-cancellation case) | COMPLETE |
| 9E-2 | G3-B — Manual Compensation (merchant refund for the G3-A merchant-cancellation alarm) | COMPLETE |
| 9E-3 | G3-A Extension — Expiry-Sweep Late Payment Detection | COMPLETE |
| 9E-4 | Expiry-Sweep Late-Success Compensation (admin-triggered refund for the 9E-3 alarm) | COMPLETE |
| 9F | Analytics v1 (store-scoped sales/orders/products/customers) | COMPLETE |

**Documentation-reconciliation note (2026-09-12):** Phase 9F (Analytics v1, commit `08f7eb7`) and a related fix aligning `Customer.total_spent` with net-sales semantics (commit `695416e`) were implemented and committed in a prior session but were never recorded in this document — this pass adds them retroactively. Neither commit touched this file, which is why the gap existed. See "Analytics v1 (Phase 9F)" under "Completed" below for full detail.

## Development Strategy — Commerce-Complete Before AI (established after Phase 8)

**Phase 8 is complete. Phase 9A (Organization Lifecycle Management), Phase 9B (Category Management), Phase 9C (Customer Management), Phase 9D (Refunds), Phase 9E-1 (G3-A — Detect & Alert), Phase 9E-2 (G3-B — Manual Compensation), Phase 9E-3 (G3-A Extension — Expiry-Sweep Late Payment Detection), Phase 9E-4 (Expiry-Sweep Late-Success Compensation), and Phase 9F (Analytics v1) are also complete** — see "Current Task" and the canonical roadmap table above. Analytics — the last item this document's own "Development Strategy" section required before AI work could begin — is now implemented. No AI-family scope has been designed, reviewed, or approved yet; the project is in the gap between Phase 9F and whatever comes next (further Commerce Completeness/Production Readiness items, or the start of AI work).

**Clarified strategic priority** (not a new architectural decision — this restates and makes explicit the pre-existing principle already recorded in `PRD.md` §34, "Project Principle": *"Build a real business workflow first, then use AI to make the workflow smarter"*): the immediate objective is to make the Commerce platform a **complete, usable, runnable product** — AI is not the immediate next implementation step. Analytics is treated as part of completing the commerce product, not part of the AI milestone, and is expected to land before AI work begins. AI Assistant / AI Tools / AI Insights / Investigation / Reports remain intentionally later work.

**Intended high-level milestone sequence** (names only — no Phase numbers are assigned to the unstarted milestones; see below):

```
Foundation
   ↓
Commerce Core
   ↓
Customer Storefront
   ↓
Commerce Completeness
   ↓
Analytics
   ↓
Production Readiness
   ↓
AI Commerce
```

The already-completed Phases 0–8 (canonical roadmap table above) correspond to the first three milestones: **Foundation** (Phases 0–3 — architecture, database design, infrastructure, models/enums), **Commerce Core** (Phases 4–7 — back-office CRUD, checkout/payment/inventory, payment expiry sweep, customer order history), and **Customer Storefront** (Phase 8 — the full customer-facing storefront, Blocks 8-0 through 8F). **Commerce Completeness has now begun**: Phase 9A (Organization Lifecycle Management), Phase 9B (Category Management), Phase 9C (Customer Management), and Phase 9D (Refunds) are its first four completed increments — see "Current Task" and the canonical roadmap table above. The remainder of Commerce Completeness, plus Analytics, Production Readiness, and AI Commerce, **are not yet started, not yet numbered (beyond 9A/9B/9C/9D), and not yet scoped** — each requires its own Discovery → Design → Review → Approval → Implementation → Verification cycle before any code is written, per this project's established incremental approach. This document does not assign a Phase number to any of the unstarted items and does not select which one is implemented next.

**Scope distinction, for clarity when planning future work:**

- **A. Already implemented** — Platform Admin auth, merchant auth/org/RBAC, Store/Product/Inventory/Order management, Checkout/Payment/Inventory (Stripe, idempotent webhooks, payment retry, expiry sweep), customer auth/cart/checkout/order-history, the full storefront UI with its Playwright coverage, **Platform Admin organization approve/reject/suspend/reactivate lifecycle (Phase 9A)**, **merchant Category management (Phase 9B)**, **merchant-facing, read-only Customer management (Phase 9C)**, and **merchant full-refund initiation with webhook-authoritative reconciliation (Phase 9D)**. See "Completed" below and the canonical roadmap table above.
- **B. Remaining work to complete the Commerce MVP** (candidates only — appearing here approves nothing; each item's existing classification elsewhere in this document, e.g. under "Not Started" or "Known Issues," is unchanged): Analytics (`PRD.md` §11 — no code exists); production readiness / CI/CD (no pipeline exists); Notifications; Jobs/queue business logic (`app/Jobs` exists but is empty — Phase 9D deliberately did not introduce one, per its approved scope); discount/tax logic if the final MVP scope requires it (`orders.discount_total`/`tax_total` are currently unused columns); password reset and other optional completeness items. (Customer Management and refund initiation, formerly listed here as candidates, are now implemented — see A above. Partial refunds, Customer CRUD, `CustomerAddress`, and any CRM-style capability remain unimplemented and are not implied by Phase 9C/9D's approved scopes.)
- **C. Future product capabilities** — already explicitly deferred, unchanged: everything under `PRD.md` §33 "Future Roadmap" (POS/Shopify integration, additional payment providers, multi-channel commerce, marketing automation, AI recommendations, real-time analytics, event-driven architecture) and the MVP exclusions already recorded in `architecture-review.md` (inventory soft-holds, pre-aggregated analytics, multi-org membership, read replicas).
- **D. AI / advanced intelligence work** — intentionally later work, architecture unchanged: AI Assistant, AI Tools (`getSales`/`getOrders`/`getProducts`/`getCustomers`/`getRefunds`/`getInventory`/`comparePeriods`), AI Insights, Investigation, and Reports, all as already specified in `CLAUDE.md`'s "AI Agent" section and `PRD.md` §12–18. Intended sequencing relative to the rest of the platform:

```
Commerce data and business services
        ↓
   Analytics
        ↓
Production-ready Commerce platform
        ↓
AI Tools / AI Assistant
        ↓
AI Insights / Reports / Advanced AI
```

The future AI layer is required to consume the same safe, tenant-scoped business services/tools the rest of the application already uses — per `CLAUDE.md`'s existing, unchanged "AI Agent — Tool-Calling, Not Direct DB Access" rules (the LLM must never have direct database access). This document does not redesign that architecture; it only records where AI work sits in the overall sequence.

**No Phase 9E (or later) scope is approved.** Whatever is implemented next — whether that turns out to be another Commerce Completeness item, Analytics, or something else — requires its own Discovery → Design → Review → Approval → Implementation → Verification → Commit pass before any code is written, per this project's established incremental-by-functional-area approach. This section records strategic sequencing only, not a Phase 9E decision.

## Current Phase

**Phase 3 — Foundation Application Layer (Models + Enums). Complete and verified.** Phase 2F (Inventory) through Phase 2A (Platform & Tenant Identity), Phase 1 (Docker Infrastructure Bootstrap), and Phase 0 (Architecture & Database Design) are all complete and approved — the full Phase 2A–2F migration plan (24 tables) is migrated and verified. Phase 3's first slice added Eloquent Models for all 24 tables and 9 PHP enums for every documented finite state/value set, with relationships, casts, soft-deletes, and timestamps configured to match the live schema exactly. Full detail: "Phase 3 (Foundation Application Layer)" section below (see "Phase 2A/2B/2C/2D/2E/2F (Database Migrations)" for the prior batches). **Phase 4 (Back Office Feature Blocks), Phase 5 (Checkout / Payment / Inventory), Phase 6 (Payment Expiry Sweep), and Phase 7 (Customer-facing Order History) are all also complete — see the canonical roadmap table above for the full Phase 0–8 status. Phase 8 (Frontend Storefront) is complete: Blocks 8-0, 8A, 8C, 8B (as revised), 8D, 8E, and 8F are all complete and verified — see the "Phase 8" entries under "Completed" below.**

**Phase 4 — Back Office (feature-block track). Block 1 — Platform Admin Authentication + Back-Office Shell: complete and verified**, 2026-08-29. Remaining application-layer work is now tracked as a numbered sequence of independently-shippable back-office feature blocks (per the roadmap agreed for this track), rather than continuing the Phase-N/schema-oriented numbering above. Block 1 established: a dedicated `platform_admins` Sanctum guard/provider structurally isolated from a new `merchant` guard/provider (`users`) via Sanctum's own provider-matching mechanism; `POST /api/platform/auth/login`, `POST /api/platform/auth/logout`, `GET /api/platform/auth/me`; and the React/TypeScript back-office application shell (Tailwind CSS, React Router, React Query, login page, protected routing, admin layout, dashboard placeholder, Coming Soon pages for every other nav item) consuming those endpoints over real HTTP — no mocked auth.

**Block 2 — Merchant Identity, Organization Registration & RBAC Foundation: complete and verified**, 2026-08-30. Resolved the `organization_user.role` open decision (`owner`/`store_admin`/`staff`, exactly the approved set) via a new `OrganizationRole` backed enum, now cast on `OrganizationUser`. Added merchant authentication on the existing `merchant` guard — `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me` — where registration atomically creates the `User`, a `pending` `Organization`, and the owning `organization_user` row in one DB transaction. Added a `TenantContext` value object plus `ResolveMerchantTenantContext` middleware (alias `tenant.merchant`) that resolves organization/role server-side from the authenticated user's own membership row — never from client input — and binds it into the container for protected merchant routes. Added the first Policy (`OrganizationPolicy`), establishing the Owner ⊃ Store Admin ⊃ Staff capability pattern later blocks will extend. **Block 2b — Merchant Authentication UI: complete and verified**, same day — the browser-facing register/login/session-restore/landing/logout flow at `/merchant/*`, closing the gap where Block 2's backend had no way to be exercised outside `curl`/tests.

**Block 3 — Store Management & Store-Level RBAC Foundation: complete and verified**, 2026-08-31. Full CRUD on the `Store` resource (`GET/POST /api/stores`, `GET/PATCH/DELETE /api/stores/{store}`) plus the store-level authorization foundation later blocks (Products, Orders, Inventory...) build on. Added `StorePolicy` (Owner-only create/update/delete, gated additionally on `organization.status === active`; `view` open to Owner or an assigned Store Admin/Staff via `store_user`); a new `ResolveMerchantStoreContext` middleware (alias `tenant.merchant.store`) that enriches `TenantContext` with a verified `Store`, resolving `{store}` itself (not via Laravel implicit route-model binding) so a cross-organization store id always 404s; and a new `App\Support\TenantAccess` class consolidating the membership/role/store-access lookups that had been duplicated across Block 2 (`ResolveMerchantTenantContext`, `MerchantAuthController::login()`, `OrganizationPolicy`) — all three now delegate to it, verified via re-running Block 2's full test suite with zero behavior change. `OrganizationRole` gained an `atLeast()` hierarchy helper (Owner > Store Admin > Staff), added proactively for Block 4's benefit since Block 3's own policy doesn't need to differentiate Store Admin from Staff (both get identical `view`-only store-level access in this block, by design). `StoreResource` introduces Laravel API Resources as the project's standard business-resource response shape going forward. Frontend: `/merchant/stores`, `/merchant/stores/new`, `/merchant/stores/:storeId`, reachable via a new "Manage stores" link on the unchanged `/merchant` landing page (not an automatic redirect).

**Block 4A — Product Management MVP: complete, reviewed (PASS WITH NOTES), and accepted**, 2026-08-31. The first real merchant business resource: full CRUD on `Product` (`GET/POST /api/stores/{store}/products`, `GET/PATCH/DELETE /api/stores/{store}/products/{product}`), deliberately scoped down from the full catalog schema — every product also gets exactly one default `ProductVariant` (`option_signature: ''`, no option matrix), created atomically with it in one `DB::transaction()`; `ProductOption`/`ProductOptionValue`/`ProductVariantOptionValue`/`ProductImage`/multiple-variants are explicitly deferred to a later slice. No new middleware was introduced — Product reuses `tenant.merchant.store` verbatim (it's directly store-scoped, same as `Store` itself), and `{product}` is resolved and tenant-verified at the controller level, the same discipline `ResolveMerchantStoreContext` already established for `{store}`. `ProductPolicy` is the first Policy to use `OrganizationRole::atLeast(StoreAdmin)` (Owner-or-Store-Admin mutate gate) rather than an exact role match, since two roles now qualify — Staff gets identical read access to Store Admin with no additional restriction, per the approved design. Frontend: `/merchant/stores/:storeId/products`, `/products/new`, `/products/:productId`, reusing `MerchantLayout`/`useMerchantAuth` unchanged, reachable via a new "Products →" link on `StoreDetailPage`. The architectural acceptance review's three recommended cleanup items were completed before Block 4B began — see "Pre-4B Cleanup" below.

**Block 4B — Inventory: complete and verified**, 2026-08-31. `Inventory`/`InventoryTransaction` (Phase 3 models, previously unused) now have a real, working, concurrency-safe mutation path: `App\Services\InventoryAdjustmentService` — the single locked service CLAUDE.md requires — wraps every quantity change in `DB::transaction()` + `SELECT ... FOR UPDATE` + quantity update + ledger insert, atomically. Merchant-facing adjustments are restricted to `restock`/`adjustment` reasons (validated in `InventoryAdjustRequest`, not the service itself, which stays reason-agnostic for the later system-driven `sale`/`refund` flows). No new tenant middleware — Inventory reuses `tenant.merchant.store` exactly like Product; `{variant}` is resolved and store-scoped at the controller level, same discipline as `{product}`. A real bug was found and fixed during implementation: `InventoryPolicy` authorizes against `ProductVariant` (inventory carries no `organization_id`/`store_id` of its own), which doesn't match Laravel's naming-convention auto-discovery (`ProductVariant` → `ProductVariantPolicy`) — `Gate::policy(ProductVariant::class, InventoryPolicy::class)` is now explicitly registered in `AppServiceProvider::boot()` to bridge this, the one place in this project a Policy isn't found by convention alone. Frontend: no new top-level Inventory browsing UI — a `InventorySection` component is embedded directly in the existing Product detail page, showing current quantity to all roles and an adjust form only to Owner/Store Admin (server-side `InventoryPolicy` remains authoritative regardless). Full detail: "Block 4B — Inventory" under "Phase 4 (Back Office Feature Blocks)" below.

**Block 4C — Order Management: complete and verified**, 2026-08-31 (design freshly re-verified against the live repository state before implementation, per instruction — not assumed from the prior Block 4 design pass). Merchant order **listing** (`GET /api/stores/{store}/orders`, one optional `status` filter, `created_at` DESC, 15/page) and **detail** (`GET .../orders/{order}`, eager-loading `items`/shipping `addresses`) plus a single, narrow **status-transition** endpoint (`PATCH .../orders/{order}/status`) — no order creation, no generic update, no deletion; checkout remains entirely out of scope. `App\Support\MerchantOrderStatusTransitions` is a pure, RBAC-free whitelist of exactly the four merchant-triggerable edges (`pending→cancelled`, `paid→processing`, `processing→shipped`, `shipped→completed`) — deliberately named/scoped so it can never be confused with or extended to cover the webhook/system-only edges (`pending→paid`, `{paid,processing,shipped,completed}→refunded`), which this block explicitly never allows a merchant to trigger, matching `database-design.md`'s authoritative state-machine documentation exactly. `App\Services\OrderStatusUpdateService` mirrors `InventoryAdjustmentService`'s proven shape — `DB::transaction()` + `SELECT ... FOR UPDATE` on the `orders` row + re-validate the current status against the whitelist + apply + (on cancellation) set `cancelled_at` + save — directly satisfying `database-design.md`'s own documented concurrency requirement for order-status races. `OrderStatusUpdateRequest` accepts only `{"status": "cancelled"|"processing"|"shipped"|"completed"}` — `status_reason` is deliberately left untouched in this block (approved decision). `OrderPolicy` follows `ProductPolicy`/`InventoryPolicy`'s exact discipline (`viewAny`/`view`/`updateStatus`, `atLeast(StoreAdmin)` for mutation, org-active-gated); auto-discovered correctly by Laravel's naming convention (`Order` → `OrderPolicy`, unlike `InventoryPolicy`'s Block 4B exception). No new middleware — reuses `tenant.merchant.store` verbatim; `{order}` is resolved and store-scoped at the controller level (`resolveOrder()`), same discipline as `{product}`/`{variant}`. `OrderResource` is a single resource class using `whenLoaded()` for `items`/`shipping_address` (index omits both; show includes both) — no payment/refund data is exposed anywhere in this block. `Customer`/`Order`/`OrderItem`/`OrderAddress` gained `HasFactory` and their first factories (`CustomerFactory`, `OrderFactory::forStore()`, `OrderItemFactory::forOrder()`, `OrderAddressFactory::forOrder()`) strictly as test-fixture infrastructure — no customer-management feature. 58 new tests (201 total): a full 7×7 transition-matrix sweep proving the whitelist is exactly the four documented edges and nothing more (`MerchantOrderStatusTransitionsTest`), `OrderPolicyTest`'s RBAC matrix, and `OrderManagementTest`/`OrderTenantIsolationTest` covering every valid/invalid transition (including the explicit `paid→cancelled` and skip-ahead/backward/terminal-state rejections), immutable-field/`status_reason` protection, `SELECT ... FOR UPDATE` proof, a forced-transaction-rollback proof, pagination/filter/ordering, and the full tenant-isolation matrix (cross-org 404, same-org-unassigned 403, wrong-store 404, spoofed `store_id`/`organization_id` in the body ignored). Live nginx walkthrough exercised the complete transition lifecycle end-to-end (including cross-org 404) against orders/customers inserted directly via SQL — matching reality, since no order-creation path exists yet (that's checkout's job). Frontend: `/merchant/stores/:storeId/orders`, `/orders/:orderId`, reusing `MerchantLayout`/`useMerchantAuth` unchanged, reachable via a new "Orders →" link on `StoreDetailPage` (alongside the existing "Products →" link); the detail page computes its next-status action button from a small client-side mirror of the same whitelist, shown only to Owner/Store Admin — server-side `OrderPolicy` + `MerchantOrderStatusTransitions` remain fully authoritative regardless. **Still not present: Checkout, Customers (beyond the test-fixture factory), Payments, Analytics, the Product option/variant matrix, and system-driven (`sale`/`refund`) inventory mutations or the `pending→paid`/`refunded` webhook transitions — none of that exists yet.** Full detail: "Block 4C — Order Management" under "Phase 4 (Back Office Feature Blocks)" below.

**Phase 4 Architectural Acceptance Review (Blocks 1–4C): PASS, no BLOCKING issues**, 2026-08-31 — a full re-verification of the actual working-tree implementation (not assumed from prior reports) across tenant isolation, RBAC, store-scoped resource resolution, mutation boundaries, transaction/concurrency correctness, the order state machine, API/frontend/testing architecture, and scope discipline. One `SHOULD FIX BEFORE CHECKOUT` finding: `StoreController::destroy()` had no dependent-resource guard, so a store with active products or non-terminal orders could be silently soft-deleted, stranding them. Blocks 4A–4C were then committed as-reviewed (`237f6b4`, `feat: add product, inventory, and order management (Blocks 4A-4C)`).

**Store-deletion dependent-resource guard (fix for the above finding): complete, verified, and committed**, 2026-08-31 (`1c5003b`, `fix: block store deletion while active products or non-terminal orders exist`). Implements `database-design.md`'s pre-existing "Cascading soft-deletes — resolved" decision (deletion blocked at the application layer while active resources exist, never cascaded): `StoreController::destroy()` now rejects (`422`) deletion of a store with any non-archived `Product` (`status` `draft`/`active`) or any `Order` in a non-terminal status (`pending`/`paid`/`processing`/`shipped`); archived products and terminal orders (`completed`/`cancelled`/`refunded`) never block deletion. A new `StoreHasActiveDependentsException` (mirroring `InsufficientInventoryException`/`InvalidOrderTransitionException`'s shape) is thrown from a private `StoreController::assertNoActiveDependents()` and mapped to `422` — no new Policy method and no Service class, consistent with this codebase's existing convention that Policies gate actor capability only, not domain/data state. **Categories and Customers are deliberately excluded from this guard** — the design doc's original wording names them too, but neither has any merchant-facing archive/reassignment workflow implemented yet, so a block on their mere existence would have no way to ever be satisfied (a store with any order, which requires a customer, could never be deleted at all). This is an accepted, explicit scope boundary, not an oversight — revisit once Category management and/or Customer lifecycle features exist. 7 new tests in `StoreManagementTest` (non-archived product blocks; archived product allows; each of the four non-terminal statuses blocks via a `DataProvider`; all-terminal-orders allows).

**Checkout / Payment / Inventory Architecture — design finalized (Database Design 2.6, APPROVED); NOT YET IMPLEMENTED**, 2026-08-31, after four rounds of explicit design review and stress-testing (initial design → three successive challenge/revision passes). Full authoritative detail lives in `docs/database/database-design.md` §"Order & Payment State Models" §9–§14 and its 2.6 changelog entry — this entry is a pointer/summary, not a duplicate. Headline decisions: **inventory is claimed atomically at checkout** (not at payment success), closing the previously-deferred "late payment success against already-depleted inventory" gap (Concurrency Review item 10, now CLOSED); the ledger's `sale` reason is retired in favor of `checkout`/`release` (`checkout` is a claim, explicitly not revenue — Sales Analytics must derive completed sales from `orders.status`/`paid_at`/`total`, never from this ledger); a failed/cancelled payment releases its claim **immediately**, and a retry must atomically re-claim inventory against the *same* order before a new PaymentIntent is created (found and closed a real inventory-starvation bug during review: holding a claim through failure would let one customer with a failing card block others from scarce stock); PaymentIntent-first sequencing is reconfirmed on atomicity grounds, not a schema technicality; a formal "at most one non-terminal Payment per order" invariant is defined; a `payment_id`-and-generated-`dedup_key` idempotency model replaces the plain `(order_item_id, reason)` ledger key (a bare 3-column `(order_item_id, reason, payment_id)` alternative was proposed and **rejected** — it would have silently relied on MySQL's NULL-distinct behavior for `restock`/`adjustment`'s exemption rather than that exemption being explicit); and durable, DB-backed idempotency keys are required on `orders`/`payments` (Redis is confirmed as a performance optimization only, never the correctness mechanism). **Required schema changes are fully specified but no migration has been written**: `inventory_transactions.payment_id`/`dedup_key`/a `CHECK` constraint/a replaced unique index; `orders.idempotency_key`/`idempotency_key_payload_hash`; `payments.idempotency_key`. `docs/architecture/system-architecture.md` §4/§5 and `CLAUDE.md`'s "Payment → Order → Inventory Flow" section were also updated to match — both previously described inventory decrementing at the `paid` webhook step and (`CLAUDE.md`) creating the pending order before the PaymentIntent, which this design explicitly supersedes; leaving those files stale would have actively misdirected a future implementation session. Customer Authentication (no `customer` guard/provider exists yet) and a minimal public catalog-browsing API are confirmed hard prerequisites to implementation, sequenced as prerequisites within Phase 5 ahead of Checkout itself. No application code, migrations, tests, or frontend were touched during this design work.

**Final Architecture Audit (read-only): READY FOR IMPLEMENTATION, no blocking contradiction found**, 2026-08-31 — a full re-verification of the current repository (migrations, models, enums, `InventoryAdjustmentService`, `OrderStatusUpdateService`, `MerchantOrderStatusTransitions`, `config/auth.php`) against the four authoritative documents. Confirmed: zero shipped code assumes any part of the superseded pre-2.6 model (no code references `InventoryTransactionReason::Sale`/`Refund` outside the still-unused enum cases themselves); `MerchantOrderStatusTransitions`/`OrderStatusUpdateService` are correctly scoped to merchant-only edges and don't conflict with the new webhook-only transitions; `status_reason` is confirmed genuinely unset by the existing merchant-cancel path (matching, not contradicting, the documentation's own disclosure). No files were modified during the audit.

**Phase 5 (Checkout / Payment / Inventory) — Prerequisite: Customer Authentication: complete and verified**, 2026-08-31. The first prerequisite identified by both the Checkout design and the architecture audit. Confirmed the already-approved architecture (CLAUDE.md: "three separate identity tables, three separate guards... no shared table, no shared guard") before writing any code — customers authenticate via the existing `customers` table/`Customer` model, never via `users`; no new table or migration was needed, since `Customer` already existed (added in Block 4C purely as Order test-fixture infrastructure) with the right columns, hashed password, and soft-deletes already in place. Added a fourth Sanctum guard/provider pair, `customer`/`customers` (`config/auth.php`), alongside the existing `platform_admin`/`merchant` guards — neither existing guard's configuration was touched. `Customer` gained `HasApiTokens`. **One real structural difference from merchant/platform auth, confirmed from the schema rather than assumed**: `customers.email` is unique only *per store* (`unique(store_id, email)`), unlike the globally-unique `users.email`/`platform_admins.email` — a bare email lookup at login would be ambiguous across stores, so `CustomerLoginRequest`/`CustomerRegisterRequest` both require a client-supplied `store_id` (safe pre-authentication input — a correct password is still required, and it never becomes a source of tenant context once authenticated). `CustomerAuthController` (`register`/`login`/`logout`/`me`) mirrors `MerchantAuthController`/`PlatformAuthController`'s exact shape; registration rejects a target store that isn't `active` or whose organization isn't `active` (403/422 as appropriate). A new `CustomerContext` value object + `ResolveCustomerContext` middleware (alias `tenant.customer`) mirrors `TenantContext`/`ResolveMerchantTenantContext`'s "never trust client input" principle — simpler than the merchant version, since a Customer's `organization_id`/`store_id` are fixed columns on the row itself, not a membership lookup — and additionally rejects (403) if the customer's organization is no longer active or their store is no longer active, re-checked fresh on every request rather than cached from login time. Routes: `POST /api/customers/auth/{register,login}` (public), `POST /api/customers/auth/logout`, `GET /api/customers/auth/me` (both `auth:customer` + `tenant.customer`). 24 new tests (232 total, up from 208) across `tests/Feature/Customer/{CustomerAuthTest,CustomerTenantIsolationTest}.php` — registration (including the per-store-unique-email distinction: same email accepted across different stores, rejected within the same store), login (including store-scoped lookup rejecting a correct email+password combination submitted against the wrong store), logout/token revocation, and tenant isolation (cross-guard rejection in both directions against Merchant and Platform Admin, spoofed `store_id` via query param and header on `/me` ignored, two customers from different stores each seeing only their own via `/me`, and context resolution correctly rejecting a since-suspended organization or since-deactivated store). A repeat of the already-known `RequestGuard` per-Application caching gotcha was hit and fixed the same way as in Platform Admin/Merchant's own auth tests (`Auth::forgetGuards()` between two authenticated requests as different identities within one test method). Full backend suite re-run afterward confirms zero regressions to existing Platform Admin or Merchant authentication. **No Checkout, Cart, PaymentIntent/Stripe, payment retry, inventory claim/release, expiry sweep, Orders API, or catalog work was implemented in this block** — this is authentication only, per the approved phase ordering.

**Phase 5 (Checkout / Payment / Inventory) — Prerequisite: Minimal Public Catalog API: complete and verified**, 2026-08-31. Read-only, unauthenticated storefront browsing — no `customer` guard involvement at all, deliberately: browsing must not require Customer Authentication. Verified the actual schema before implementing rather than assuming it: `stores.slug` is unique only per `(organization_id, slug)`, not globally (confirmed directly in the migration), so the originally-suggested `{storeSlug}` route parameter cannot uniquely resolve a store platform-wide — every existing store-scoped route in this codebase already resolves `{store}` by numeric id for exactly this reason, so the public catalog does the same, with zero schema changes. New routes `GET /api/shop/stores/{store}/products` and `GET /api/shop/stores/{store}/products/{product}` — placed under a new `/api/shop/*` prefix specifically because `/api/stores/{store}/products` already exists (Merchant, authenticated); reusing that exact URI for a second, public route would collide. `App\Http\Controllers\Catalog\ProductController` resolves and validates the store itself (existence, `status === active`, parent organization `status === active` — an inactive store or one under a suspended/pending organization returns 404, matching this project's established "don't distinguish absence from unavailability" convention) and scopes every product query through `$store->products()` (the Eloquent relation) rather than `Product::find($id)` + a separate ownership check, so the tenant boundary exists at query time. `CatalogProductResource` is a new, deliberately distinct resource from the merchant-facing `ProductResource` — inlines category/images/options/option-values/variants as plain nested arrays (matching `OrderResource`'s existing convention of inlining nested shapes rather than one Resource class per shape) and exposes only customer-safe fields (no `store_id`, `organization_id`, `metadata`, `status`, or timestamps). Four new test-fixture-only factories were added — `CategoryFactory`, `ProductOptionFactory`, `ProductOptionValueFactory`, `ProductImageFactory` (with `HasFactory` added to their four respective models, the same "test-fixture infrastructure only, no management feature" pattern already established for `Customer`/`Order`/etc. in Block 4C) — since none of Block 4A's shipped merchant flow ever populates `ProductOption`/`ProductOptionValue`/`ProductImage` rows (every product ships with exactly one default variant and no option matrix), so these relationships were previously real but entirely untested. 18 new tests (250 total, up from 232) covering store access (active/unknown/inactive store, store under an inactive organization), product isolation (list and detail scoped correctly, a cross-store product id returns 404 without leaking its data in the error response), nested-relationship isolation (category/options/option-values/variants/images each independently verified not to leak across a store boundary), response correctness (expected fields present, admin-only fields absent, draft products neither listed nor directly reachable, empty catalog handled cleanly, full nested detail shape verified), and a query-count regression test proving the eager-load chain doesn't grow with the number of products (a genuine N+1-shaped test failure was hit and fixed during this block — the test itself, not the application code, had a bug: `DB::flushQueryLog()` clears the log but doesn't disable it, so fixture-creation queries between the two measured requests were being counted; fixed by explicitly disabling logging during fixture setup). Full backend suite re-run afterward confirms zero regressions to Platform Admin, Merchant, or Customer authentication (250/250 passing). **No Checkout, Cart, Orders API, order history, PaymentIntent/Stripe, payment retry/idempotency, inventory claim/release/transactions, or the approved 2.6 schema migration was implemented in this block** — read-only catalog browsing only, per the approved phase ordering.

**Phase 5 (Checkout / Payment / Inventory) — Prerequisite: Database Design 2.6 Migration: complete and verified**, 2026-08-31. Schema-only — no Checkout, PaymentIntent, Stripe webhook, retry-payment, expiry, or frontend code was implemented. Verified the live schema empirically before writing anything (`inventory_transactions`/`orders`/`payments` all had zero rows in the dev database, confirming no backfill was needed for anything) rather than assuming it from prior documentation. Three new migrations:
- **`inventory_transactions`**: added `payment_id` (nullable FK → `payments.id`, `RESTRICT`, indexed) and a generated `STORED` column `dedup_key` (`CASE WHEN reason IN ('checkout','release','refund') THEN CONCAT(order_item_id,':',reason,':',payment_id) ELSE NULL END`), plus the CHECK constraint requiring both ids non-null for those three reasons. `UNIQUE(order_item_id, reason)` was dropped and replaced by `UNIQUE(dedup_key)` — both cannot coexist, since the old constraint would still reject a retried payment attempt's second `checkout` row before `dedup_key` is ever reached. Verified directly via `information_schema` (columns, indexes, and the CHECK constraint's exact clause) rather than assuming the migration applied as written.
- **`orders`**: added `idempotency_key` (nullable, `UNIQUE(customer_id, idempotency_key)`) and `idempotency_key_payload_hash` (nullable, no index).
- **`payments`**: added `idempotency_key` (nullable, `UNIQUE(order_id, idempotency_key)`).

**A real reversibility bug was found and fixed during this block**: the `orders` migration's `down()` initially failed with *"Cannot drop index ... needed in a foreign key constraint"* — none of `orders`' pre-existing indexes have `customer_id` as a leftmost column, so MySQL had silently adopted the new `(customer_id, idempotency_key)` unique index as the sole index satisfying the `customer_id` foreign key; dropping it during rollback would have left that FK with no covering index at all. Fixed by having `down()` re-establish a plain `customer_id` index before dropping the composite unique. Full rollback → re-migrate cycle re-run afterward and confirmed clean (schema verified back to its exact original shape, then forward to the final approved shape).

**One enum change, explicitly approved as part of this migration (Database Design 2.6 §9), not a side effect of adding a column**: `InventoryTransactionReason` retires `Sale` in favor of `Checkout`/`Release` (`Refund`/`Restock`/`Adjustment` unchanged) — confirmed unused in every shipped code path before changing it (Block 4B's `InventoryAdjustRequest` validates against a literal `['restock','adjustment']` string list, not the enum, so it's entirely unaffected).

**One additional model change beyond the literal schema, flagged explicitly rather than made silently**: `Payment` gained `HasFactory` (+ a new `PaymentFactory`), needed because the required schema tests (idempotency scoping, ledger dedup keyed by a real `payment_id`) need real `Payment` rows to exist, and none of Block 4A–4C's shipped code has ever needed to construct one. Mirrors the identical, already-established "test-fixture infrastructure only, no feature" precedent used for `Customer`/`Order`/`OrderItem` in Block 4C. No `payment()` relation was added to `InventoryTransaction` — not required for the migration or these tests, deliberately left for whichever future block actually consumes it.

16 new tests (266 total, up from 250): `tests/Unit/Models/InventoryTransactionDedupKeyTest.php` (12 — checkout/release claim-and-duplicate-rejection for one and a second payment attempt, restock/adjustment remaining unconstrained, the CHECK constraint rejecting a null `payment_id`/`order_item_id` for `checkout`/`release`/`refund`) and `tests/Unit/Models/IdempotencyKeyTest.php` (4 — the same key allowed across different customers/orders, rejected for the same customer/order). Full backend suite re-run afterward: 266/266 passing, zero regressions — Block 4B's `InventoryManagementTest`/`InventoryTenantIsolationTest`/`InventoryPolicyTest` (including the `SELECT...FOR UPDATE` and forced-rollback proofs) and all Platform Admin/Merchant/Customer authentication and Phase 1 catalog tests explicitly re-verified green.

**Development Environment**: see `docs/development/development-environment.md` for the full, permanent record of the verified local environment — exact versions, Docker architecture, service configuration, credential flow, and commands. That document is now the source of truth for environment detail; the "Infrastructure (Phase 1)" section below is a summary, not a duplicate.

**Database Design 2.5 is APPROVED** as the authoritative database design (corrected from a stale "2.4" reference previously left in this document — see "Residual documentation gap" under "Phase 2E (Database Migrations)" below for how that happened and was closed). Version 2.0 substantially upgraded the original MVP schema into a production-quality e-commerce data model — product variants/options, customer saved addresses, immutable order address snapshots, and Stripe saved payment methods — and resolved its one remaining open decision (order/payment status separation). Version 2.1 added Platform Admin as a third, structurally separate identity domain plus an organization approval/suspension lifecycle. Version 2.2 removed `carts`/`cart_items` from the MySQL schema entirely. Version 2.3 resolved three Phase 2C catalog design-review findings. Version 2.4 resolved the Phase 2D Orders design review. **Version 2.5, a documentation-only reconciliation, brought the document into full agreement with the already-implemented Phase 2E `payments`/`refunds`/`stripe_webhook_events` migrations** — `refunds` gained a complete formal column table (previously the only table in the document without one), `stripe_webhook_events` gained exact types/nullability, and `payments.organization_id`/`store_id`'s `ON DELETE RESTRICT` was made explicit. See "Platform Admin (Database Design 2.1)," "Cart Architecture (Database Design 2.2)," "Catalog Design Review Corrections (Database Design 2.3)," "Phase 2D Orders Design Review Resolutions (Database Design 2.4)," under Database Decisions for full detail on 2.1–2.4. Phase 2A's six tables, Phase 2B's two tables, Phase 2C's seven tables, Phase 2D's three tables, Phase 2E's four tables, and Phase 2F's two tables (twenty-four total) are now migrated and verified — this completes every table in the originally-scoped Phase 2A–2F migration plan. Only `reports` (AI-generated artifacts, not yet broken down into its own phase) remains unmigrated from the full Database Design 2.5 schema.

## Completed

**Design / review work (all documentation, no code):**
- `PRD.md` authored — full product requirements and MVP scope.
- `CLAUDE.md` authored and corrected — Laravel Sanctum auth, pending-order-first payment flow, correct Laravel commands, React Query-first frontend state approach, structural multi-tenancy scoping strategy, AI-as-application-feature security framing, development rules, code-quality conventions, testing strategy, tech stack, a caution against relying on outdated Laravel AI SDK documentation, and project philosophy.
- Full system architecture review completed — covering architecture overview, multi-tenancy, auth/RBAC, order & payment architecture, inventory & concurrency, Redis/queue architecture, analytics architecture, AI agent architecture, API architecture, React architecture, a ranked security review, a scalability review, interview/portfolio-value analysis, and final recommendations with an architecture score of 7.5/10.
- Phase 0 database schema design completed — full entity list, ERD, relationship explanation, tenant isolation strategy, order/payment/inventory state models, constraints, and concurrency considerations.
- A focused schema review of that design — identifying and resolving critical issues, recommending improvements, and explicitly recording MVP simplifications that were deliberately accepted rather than overlooked.
- Documentation consolidated into the approved structure: `docs/architecture/architecture-review.md`, `docs/architecture/system-architecture.md`, `docs/database/database-design.md` (moved from `docs/architecture/` to `docs/database/`), with empty `docs/api/`, `docs/decisions/`, `docs/development/` reserved for future use.
- A documentation consistency check across `PRD.md`, `CLAUDE.md`, and the three architecture/database documents, surfacing one unresolved contradiction (see Known Issues).
- Persistent project-state tracking established (`docs/development/project-status.md`, plus the "Project State Management" section added to `CLAUDE.md`).
- **Database Design 2.0 — APPROVED**: the schema was upgraded from a minimal MVP catalog into a proper product/variant model. Major additions: `product_options`/`product_option_values`/`product_variants`/`product_variant_option_values` (Product Variant established as the single sellable unit — `cart_items`, `order_items`, `inventory`, and `inventory_transactions` all now reference `product_variant_id`, never `product_id`, directly); `customer_addresses` (reinstated saved-address book); `order_addresses` (immutable per-order shipping snapshot, replacing the old inline `shipping_*` columns on `orders`); `payment_methods` (Stripe saved payment methods) and `customers.stripe_customer_id`; a corrected inventory idempotency key (`inventory_transactions` now keyed by `order_item_id`, not `order_id` — the old key was a real bug once orders could contain multiple line items); an explicit soft-delete strategy (mutate-on-delete) and cascading-soft-delete policy (blocked at the application layer, not cascaded). Full detail in `docs/database/database-design.md`.
- **Order/payment status separation resolved and approved** — see "Final Resolution" under Database Decisions below.
- **Platform Admin architecture correction (Database Design 2.1)** — identified before any Phase 2 migration was written: `platform_admins` added as a third identity domain, structurally separate from `users`/`organization_user`/`store_user` and from `customers`; `organizations` gained an approval/suspension lifecycle (`status`, `status_reason`, `approved_at`/`approved_by_platform_admin_id`, `rejected_at`/`rejected_by_platform_admin_id`, `suspended_at`/`suspended_by_platform_admin_id`). `CLAUDE.md`, `docs/architecture/system-architecture.md`, `docs/database/database-design.md`, and `PRD.md` (new §3.4) all updated. See "Platform Admin (Database Design 2.1)" under Database Decisions below for full detail.
- **Cart Architecture correction (Database Design 2.2)** — identified and approved before Phase 2D (Cart & Orders, not yet started) was reached: `carts`/`cart_items` removed from the MySQL schema entirely. Cart state for MVP is ephemeral — guest carts in browser `localStorage`, authenticated-customer carts in Redis (server-derived tenant/customer key, TTL'd) — with checkout treating cart contents as untrusted input and revalidating/recalculating everything against MySQL. `CLAUDE.md`, `docs/architecture/system-architecture.md` (new §7 "Cart Architecture"), and `docs/database/database-design.md` all updated; no Phase 2A/2B migration was affected. See "Cart Architecture (Database Design 2.2)" under Database Decisions below for full detail.
- **Catalog Design Review corrections (Database Design 2.3)** — identified during the Phase 2C design review and resolved before any Phase 2C migration was written: `categories.deleted_at` added (correcting an internal documentation contradiction), the variant option-value cardinality rule documented as an application-layer invariant, and product soft-delete confirmed as never blocked by active variants/options (unlike store deactivation). `docs/database/database-design.md` and `docs/development/project-status.md` updated; no migration, model, controller, or config file touched. See "Catalog Design Review Corrections (Database Design 2.3)" under Database Decisions below for full detail.
- **Phase 1 — Docker Infrastructure Bootstrap (implementation, not documentation)**: Laravel 13 scaffolded under `backend/`, React 19.2 + TS + Vite scaffolded under `frontend/`, full Docker Compose stack (app, nginx, mysql, redis, queue, node) built and verified running, Sanctum + API routing foundation installed. See "Infrastructure (Phase 1)" below for exact versions, verification results, and commands.
- **Phase 2A — Platform & Tenant Identity migrations (implementation, verified)**: six migrations created and run against MySQL — `platform_admins`, `organizations`, `stores`, `add_soft_deletes_to_users_table`, `organization_user`, `store_user`. Verified via `php artisan migrate --pretend` (SQL reviewed before running), `php artisan migrate` (batch 2, all six `Ran`), `php artisan db:table` on all six tables (columns, defaults, FK actions, indexes, unique constraints all confirmed matching Database Design 2.1), `php artisan migrate:status`, and `php artisan test` (2/2 passing — Laravel's default example tests; no schema-specific tests exist yet). See "Phase 2A (Database Migrations)" below for full detail.
- **Phase 2B — Customer Domain migrations (implementation, verified)**: two migrations created and run against MySQL — `create_customers_table`, `create_customer_addresses_table`. Verified via `php -l` on both files, `php artisan migrate --pretend` (SQL reviewed before running), `php artisan migrate` (batch 3, both `Ran`), `php artisan db:table` on both tables (columns, defaults, FK actions — including the `RESTRICT`/`RESTRICT`/`CASCADE` behavior decided for the previously-unresolved `customers` tenant FKs — indexes, unique constraints all confirmed matching the approved Phase 2B plan), `php artisan migrate:status`, and `php artisan test` (2/2 passing). See "Phase 2B (Database Migrations)" below for full detail.
- **Phase 2C — Catalog migrations (implementation, verified)**: seven migrations created and run against MySQL — `create_categories_table`, `create_products_table`, `create_product_options_table`, `create_product_option_values_table`, `create_product_variants_table`, `create_product_variant_option_values_table`, `create_product_images_table`. Verified via `php -l` on all 7 files, `php artisan migrate --pretend` (SQL reviewed before running and matched Database Design 2.3 exactly), `php artisan migrate` (batch 4, all 7 `Ran` after one fix — see "Phase 2C (Database Migrations)" below), `php artisan db:table` on all 7 tables (columns, defaults, FK actions, indexes, unique constraints all confirmed matching Database Design 2.3), full rollback/re-migrate exercise of all 7 `down()` methods, `php artisan migrate:status`, and `php artisan test` (2/2 passing). Phase 2A/2B tables confirmed untouched throughout. See "Phase 2C (Database Migrations)" below for full detail, including two flagged implementation deviations.
- **Phase 2D Orders Design Review resolutions (Database Design 2.4)** — a comprehensive 19-point design review of the already-specified `orders`/`order_items`/`order_addresses` schema (unchanged since Database Design 2.0), resolving four open questions before any Phase 2D migration was written: confirmed no `orders.shipping_total` column; recorded `paid → cancelled` as an explicitly disallowed transition (authoritative business invariant); recorded the late-payment/inventory-oversell scenario as a known decision deferred to Phase 2F; widened `orders`' customer-history index to `(store_id, customer_id, created_at)`. `docs/database/database-design.md`, `docs/architecture/system-architecture.md` (§4 transaction-boundary wording), and `docs/development/project-status.md` updated; no migration, model, controller, or config file touched. See "Phase 2D Orders Design Review Resolutions (Database Design 2.4)" under Database Decisions below for full detail.
- **Phase 2D — Orders migrations (implementation, verified)**: three migrations created and run against MySQL — `create_orders_table`, `create_order_items_table`, `create_order_addresses_table`. A mid-task conflict between the task's literal column lists and the already-approved Database Design 2.4 was caught and clarified with you before any file was written (see "Phase 2D (Database Migrations)" below). Verified via `php -l` on all 3 files, `php artisan migrate --pretend` (SQL reviewed before running and matched Database Design 2.4 exactly), `php artisan migrate` (batch 5, all 3 `Ran` on the first attempt), `php artisan db:table` on all 3 tables (columns, defaults, FK actions, indexes, unique constraints all confirmed matching Database Design 2.4), a raw `information_schema` query confirming the `CHECK (quantity > 0)` constraint on `order_items` (Laravel's MySQL grammar has no native `check()` support — confirmed by inspecting the vendor source), a raw `information_schema` query confirming none of `shipping_total`/`payment_status`/`fulfillment_status`/`deleted_at` exist anywhere in the 3 tables, full rollback/re-migrate exercise of all 3 `down()` methods (with the `CHECK` constraint re-verified afterward), `php artisan migrate:status`, and `php artisan test` (2/2 passing). Phase 2A/2B/2C tables confirmed untouched throughout. See "Phase 2D (Database Migrations)" below for full detail.
- **Phase 2E Payments Design Review** — a 25-point design review of `payments`/`payment_methods`/`refunds`/`stripe_webhook_events`, surfacing that `refunds` had **no formal column-type specification anywhere** in `database-design.md` (unlike every other table) — flagged as I-1, along with two minor gaps (I-2: `payments`' tenant-FK `ON DELETE` behavior unstated; I-3: `stripe_webhook_events` varchar lengths/`processed_at` nullability unstated). Read-only review, no files modified. You approved concrete resolutions for all three, implemented in Phase 2E below.
- **Phase 2E — Payments migrations (implementation, verified)**: four migrations created and run against MySQL — `create_payment_methods_table`, `create_payments_table`, `create_refunds_table`, `create_stripe_webhook_events_table`, implementing Database Design 2.4 plus the approved I-1/I-2/I-3 decisions. Verified via `php -l` on all 4 files, `php artisan migrate --pretend` (SQL reviewed before running), `php artisan migrate` (batch 6, all 4 `Ran` on the first attempt), `php artisan db:table` on all 4 tables, an `information_schema.COLUMNS` query cross-verifying exact nullability/defaults for every column (confirming `payments.status`/`currency` correctly have no default, while `refunds.status` correctly defaults to `'pending'`), an `information_schema.COLUMNS` query confirming `orders` has no Stripe-specific or `payment_status` column, full rollback/re-migrate exercise of all 4 `down()` methods in correct reverse-FK order, `php artisan migrate:status`, and `php artisan test` (2/2 passing). Phase 2A/2B/2C/2D tables (18 total) confirmed untouched throughout; `git diff` confirmed empty on every prior migration file and on `backend/app`/`backend/routes`. See "Phase 2E (Database Migrations)" below for full detail, including a residual documentation gap in `database-design.md` itself (since closed — see below).
- **Phase 2E Documentation Reconciliation (Database Design 2.5)** — a separate, documentation-only turn brought `database-design.md` into full agreement with the already-implemented Phase 2E migrations: `refunds` gained a complete formal column table (closing the I-1 gap), `stripe_webhook_events` gained exact types/nullability (I-3), and `payments.organization_id`/`store_id`'s `ON DELETE RESTRICT` was made explicit (I-2). No schema change — confirmed field-by-field against the actual migration files. Only `docs/database/database-design.md` was modified.
- **Phase 2F Inventory Design Review** — a 17-point read-only design review of the already-fully-specified `inventory`/`inventory_transactions` schema (unchanged since Database Design 2.0). Unlike Phase 2E's `refunds` gap, both tables were already complete with no missing types, FKs, or constraints — verdict: READY. Confirmed the G3 late-payment/inventory-oversell question needs no schema accommodation and re-scoped it explicitly to a future service-design phase (not a Phase 2F migration concern). Reconfirmed `system-architecture.md` §5's pre-existing "one inventory row per product" staleness is still present, cosmetic, and already tracked — left unmodified per instruction. No files modified.
- **Phase 2F — Inventory migrations (implementation, verified)**: two migrations created and run against MySQL — `create_inventory_table`, `create_inventory_transactions_table`, implementing Database Design 2.5 exactly with no schema deviation. Verified via `php -l` on both files, `php artisan migrate --pretend` (SQL matched exactly; every identifier name checked against MySQL's 64-character limit, longest was 58 characters), `php artisan migrate` (batch 7, both `Ran` on the first attempt), `php artisan db:table` on both tables, an `information_schema.COLUMNS` query listing the complete column set for both (confirming no `organization_id`/`store_id`/`product_id`/`reserved_quantity`/`available_quantity`/`deleted_at`/`before_quantity`/`after_quantity` anywhere), the `CHECK (quantity_on_hand >= 0)` constraint verified via `information_schema.CHECK_CONSTRAINTS`, full rollback/re-migrate exercise of both `down()` methods (with the `CHECK` constraint re-verified afterward), `php artisan migrate:status`, and `php artisan test` (2/2 passing). Phase 2A–2E tables (22 total) confirmed untouched throughout; `git diff` confirmed empty on every prior migration file and on `backend/app`/`backend/routes`. This completes every table in the originally-scoped Phase 2A–2F migration plan. See "Phase 2F (Database Migrations)" below for full detail.
- **Phase 3 — Foundation Application Layer: Models + Enums (implementation, verified)**: 24 Eloquent Models (one per Phase 2A–2F table) and 9 backed PHP enums created under `backend/app/Models/` and `backend/app/Enums/`, plus the existing `User` model updated for its new `deleted_at` column and RBAC relationships. Every relationship (45 FKs, 3 many-to-many pivots), enum cast, `decimal:2` monetary cast, `SoftDeletes` application (exactly the 9 tables with `deleted_at`), and `UPDATED_AT = null` override (the 3 `created_at`-only tables) verified against the live schema via `php artisan model:show` on all 24 models — not just written, but confirmed resolving correctly. One genuine ambiguity was found and **not** silently resolved: `organization_user.role`'s exact string value set is undocumented anywhere in the current design docs, so it was left as a plain string (no enum cast) pending your decision — see Open Decisions below. `php -l` passed on all 33 touched files; `php artisan test` passed (2/2, unchanged); `git diff` confirmed zero changes to any migration, docs, routes, or config file. See "Phase 3 (Foundation Application Layer)" below for full detail.
- **Phase 4 Block 1 — Platform Admin Authentication + Back-Office Shell (implementation, verified)**, 2026-08-29: dedicated `platform_admin`/`merchant` Sanctum guards added to `config/auth.php`, each bound to its own provider (`platform_admins`/`users`) — isolation enforced by Sanctum's own `Guard::hasValidProvider()`, not custom code; `HasApiTokens`/`HasFactory` added to `PlatformAdmin`, `HasApiTokens` added to `User`; `PlatformAuthController` (login/logout/me) plus `PlatformAdminLoginRequest` under `app/Http/{Controllers,Requests}/Platform/`; routes `POST /api/platform/auth/{login,logout}`, `GET /api/platform/auth/me`, and the pre-existing `/api/user` stub re-scoped from the provider-agnostic `auth:sanctum` to `auth:merchant`; `PlatformAdminFactory` added; 12 feature tests in `tests/Feature/Platform/PlatformAdminAuthTest.php` covering login success/invalid-password/unknown-email/missing-fields, valid-token access, merchant-token-rejected-by-platform-routes, platform-token-rejected-by-merchant-routes, logout, revoked-token-rejected, and unauthenticated-rejected. On the frontend: Tailwind CSS v4 and React Router v7 installed (both already named in CLAUDE.md's declared stack, not previously installed) alongside `@tanstack/react-query`; unused Vite/React demo boilerplate (`App.css`, demo assets, `icons.svg`) removed; new `src/services/` (typed `apiClient`/`platformAuth`), `src/admin/auth/` (`AuthContext` — token in `localStorage`, React-Query-backed session restore via `/me`, deriving `unauthenticated` directly from a query error rather than clearing storage from an effect; `ProtectedRoute`; `LoginPage`), `src/admin/layout/` (`AdminLayout`/`Header`/`Sidebar`, responsive: fixed column on desktop, horizontal strip on mobile), `src/admin/dashboard/DashboardPage` (explicitly-placeholder metric cards, no real data), and `src/admin/pages/ComingSoonPage` reused for every other nav item (Organizations/Stores/Users/Products/Orders/Customers/Inventory/Analytics/AI). See "Phase 4 (Back Office Feature Blocks)" below for full detail, including a documented Laravel-testing-harness gotcha (`RequestGuard` caching) hit and resolved while writing the revoked-token test.
- **Phase 4 Block 2 — Merchant Identity, Organization Registration & RBAC Foundation (implementation, verified)**, 2026-08-30: closed the `organization_user.role` open decision with a new `OrganizationRole` backed enum (`owner`/`store_admin`/`staff`, the approved set), now cast on `OrganizationUser`; `MerchantAuthController` (`register`/`login`/`logout`/`me`) plus `MerchantRegisterRequest`/`MerchantLoginRequest` under `app/Http/{Controllers,Requests}/Merchant/`, routed at `POST /api/auth/{register,login,logout}`, `GET /api/auth/me`; registration atomically creates the `User`, a `pending` `Organization`, and the owning `organization_user` row inside one `DB::transaction()`; a new `TenantContext` value object plus `ResolveMerchantTenantContext` middleware (alias `tenant.merchant`) resolves organization/role server-side from the authenticated user's own membership row — never from client input — and binds it into the container for protected merchant routes; a new `OrganizationPolicy` (Owner-only `update`/`manageStores`/`manageUsers`/`viewAnalytics`, any-member `view`) establishes the RBAC pattern later Policies extend. 23 new tests (35 total, up from 12) across `tests/Feature/Merchant/MerchantAuthTest.php`, `tests/Feature/Merchant/TenantIsolationTest.php`, and `tests/Unit/Policies/OrganizationPolicyTest.php` — including two explicit client-supplied-`organization_id` spoofing attempts (query param and header) that TenantContext correctly ignores. On the frontend: only `src/services/merchantAuth.ts` added (typed register/login/logout/me calls) — no merchant login page, `AuthContext`, or routes, since `/admin/*` is Platform Admin's own application and no merchant-facing UI exists yet to need one. See "Phase 4 (Back Office Feature Blocks)" → "Block 2" below for full detail, including a second instance of Block 1's `RequestGuard`-caching testing-harness gotcha.

**Implementation work completed:** infrastructure (Phase 1), Phase 2A/2B/2C/2D/2E/2F migrations, Phase 3's Models + Enums, Phase 4 Block 1's Platform Admin authentication + back-office shell, Phase 4 Block 2/2b's merchant authentication + organization registration + `TenantContext`/RBAC foundation + auth UI, Phase 4 Block 3's Store Management + store-level RBAC foundation, Phase 4 Block 4A's Product Management MVP, Phase 4 Block 4B's Inventory, and Phase 4 Block 4C's Order Management (all above) — all verified working via actual command execution, not assumed. Platform Admin and merchant both have working, structurally-isolated auth flows; `OrganizationPolicy`/`StorePolicy`/`ProductPolicy`/`InventoryPolicy`/`OrderPolicy` are the only five Policies; `Store` and `Product` (with exactly one default variant each, no option matrix) both have full CRUD with tenant/role-based authorization, `Inventory` has a real, concurrency-safe, locked mutation path (`InventoryAdjustmentService`), and `Order` has read access plus a narrow, whitelisted status-transition path (`OrderStatusUpdateService`/`MerchantOrderStatusTransitions`) using the same locked-transaction discipline; no Customer authentication, Checkout, or Eloquent tenant-scoping global scopes (applied generally, not per-controller) exist yet; tests total 201 (12 Platform Admin + 35 merchant/tenant-isolation/`OrganizationPolicy` + 30 `OrganizationRole`/`TenantAccess`/`StorePolicy`/Store Management/store-tenant-isolation + 36 `ProductPolicy`/Product Management/product-tenant-isolation (34 + 2 from the pre-4B cleanup) + 30 `InventoryPolicy`/Inventory Management/inventory-tenant-isolation + 58 `MerchantOrderStatusTransitions`/`OrderPolicy`/Order Management/order-tenant-isolation), plus Laravel's default example tests.

**Phase 5 STEP 3A — Order + Inventory-Claim Transaction: complete and verified**, 2026-09-01. The first slice of Phase 5 (Checkout), scoped narrowly per approved instruction to just database-design.md §9/§13's atomic write — explicitly excluding cart reading/validation (this service takes already-resolved `{variant, quantity}` line items) and Stripe PaymentIntent creation (this service takes an already-obtained `stripe_payment_intent_id` string; §13's PaymentIntent-first sequencing means the caller must create it before invoking this service — no Stripe SDK call is made anywhere in this slice). New `App\Services\CheckoutOrderCreationService::createPendingOrder()` wraps the entire write — `Order` (status `pending`, ULID `order_number`, snapshot `customer_name`/`customer_email`, live-computed `subtotal`/`total` from each variant's current `price`, never client-supplied), `OrderItem`s (snapshot `product_name`/`sku`/`unit_price`/`line_total`), one `OrderAddress` (`Shipping`), and a `Payment` (`requires_payment`) — in one `DB::transaction()`. Rather than re-implementing row locking, inventory is claimed by calling the existing `InventoryAdjustmentService::adjust()` once per line item (now extended with optional `?OrderItem $orderItem, ?Payment $payment` params so the ledger row can carry `order_id`/`order_item_id`/`payment_id`, satisfying the `checkout` reason's `dedup_key`/CHECK constraint added in Phase 2) — existing Restock/Adjustment callers are unaffected, since both new params default to null. Laravel nests `adjust()`'s own `DB::transaction()` as a savepoint inside the outer one, so an `InsufficientInventoryException` from any line item unwinds the whole order, including any claim already made for an earlier line item in the same checkout (proven directly by a two-variant test). Two new exceptions: `ProductVariantUnavailableException` (a variant fails checkout-time revalidation — wrong store, or not `Active`; per CLAUDE.md, availability is never trusted from whatever produced the line item and is re-checked here) and `IdempotencyKeyConflictException` (the already-migrated `orders.idempotency_key`/`idempotency_key_payload_hash` columns are put to use: a repeat submission with the same key+payload hash returns the existing order via a caught `QueryException` on the durable unique constraint, matching database-design.md §14's crash-recovery table exactly; same key with a different hash is rejected, never silently resolved either way). Line items are sorted by variant id before claiming, bounding lock-ordering deadlock risk across concurrent multi-item checkouts sharing overlapping variants. `InventoryTransaction` gained the `payment()` relation (left deliberately unadded in Phase 2, pending the block that would actually consume it). 8 new tests in `tests/Unit/Services/CheckoutOrderCreationServiceTest.php` (274 total, up from 266) — happy path (order/items/address/payment/ledger row all correct, inventory decremented), insufficient-inventory full rollback, second-line-item failure rolling back the first line item's already-made claim, cross-store variant rejection, inactive-variant rejection, idempotent replay (same key+hash returns the same order without re-claiming), key-reuse-with-different-hash rejection, and a `SELECT ... FOR UPDATE` proof mirroring Block 4B's pattern. Full backend suite re-run afterward: 274/274 passing, zero regressions. Pint clean. **No controller, route, Stripe SDK call, cart/Redis integration, webhook, retry-payment, or expiry sweep exists yet** — this is the transaction only, per the approved STEP 3A scope. Not yet committed.

**STEP 3A Audit Findings Cleanup — complete and verified**, 2026-09-01. A read-only architecture/code review of STEP 3A surfaced two findings, both closed before sign-off: (1) `CheckoutOrderCreationService::createPendingOrder()` verified every line item's `ProductVariant` belongs to the supplied `Store`, but never verified the supplied `Customer` also belongs to that `Store`/`Organization` — closed by a new `App\Exceptions\CustomerStoreMismatchException` and an `assertCustomerBelongsToStore()` check as the very first thing the method does, so the service remains safe even if a future caller ever invoked it with a mismatched pair, rather than trusting the caller (e.g. a future controller) to guarantee it. (2) `CheckoutOrderCreationServiceTest::test_rejects_a_variant_belonging_to_a_different_store` used `expectException()` immediately before an `assertDatabaseCount()` line that could therefore never execute (dead code, since PHPUnit's `expectException` lets the exception propagate out of the test method rather than being caught locally) — fixed to the same `try/catch` pattern already used elsewhere in the file. One new test (`test_rejects_a_customer_belonging_to_a_different_store`) added. 275 total (up from 274). Full suite re-run: 275/275 passing. Pint clean.

**Phase 5 STEP 3B — Checkout Orchestration: complete and verified**, 2026-09-01, following a two-round design review (initial proposal, then a decision-and-final-confirmation pass verifying the exact installed `stripe/stripe-php` v21.3.0 exception hierarchy from its actual source rather than assuming it) before any implementation began. Adds the orchestration layer STEP 3A deliberately excluded: `POST /api/checkout` (`App\Http\Controllers\Customer\CheckoutController`, guarded by `auth:customer` + `tenant.customer`) — `CustomerContext` supplies `Customer`/`Store`/`Organization` server-side (never a client-supplied `store_id`/`organization_id`/`customer_id`); `App\Http\Requests\Customer\CheckoutRequest` validates request *shape* only (items array/quantities, shipping-address fields, a **required** `Idempotency-Key` header merged into validated input via `prepareForValidation()` — a missing key is a 422, never silently defaulted or server-generated); every requested `ProductVariant` is then reloaded from MySQL scoped directly through `$store->id` + `CatalogStatus::Active` (one query, `whereIn`) — a nonexistent, wrong-store, or inactive id all fold into the same `ProductVariantUnavailableException` (422) **before** Stripe is ever called, matching this project's established "don't distinguish absence from unavailability" convention; a provisional Stripe amount is computed from live variant prices (never client-supplied) using the same float+round arithmetic `CheckoutOrderCreationService` itself uses; a SHA-256 idempotency payload hash is computed from a deterministic, sorted-by-variant-id representation (customer id + items + shipping address — never the provisional amount, never a DB-ordering-dependent value); a new `App\Services\StripePaymentIntentGateway` interface + `StripeApiPaymentIntentGateway` concrete implementation (thin wrapper around `Stripe\StripeClient`, bound in `AppServiceProvider`) creates the PaymentIntent (`payment_method_types: ['card']`, `metadata`: organization/store/customer id + the idempotency key) using the client's `Idempotency-Key` verbatim as Stripe's own idempotency key — the same value then threaded unchanged into `CheckoutOrderCreationService::createPendingOrder()`'s `$idempotencyKey`/`$idempotencyKeyPayloadHash` parameters, so one client-supplied value governs Stripe idempotency, `orders.idempotency_key`, and `payments.idempotency_key` together, never three independently-derived keys. **`CheckoutOrderCreationService` itself was not modified in any way** — its STEP 3A interface is treated as frozen. No lock is ever held across the Stripe network call (the only lock in the whole flow lives inside `CheckoutOrderCreationService`'s own transaction, entirely after Stripe has already resolved). Domain exceptions map to HTTP status using this codebase's existing per-controller `abort($status, $message)` convention — no new error envelope: `ProductVariantUnavailableException`/`InsufficientInventoryException`/`CustomerStoreMismatchException` → 422 (the last one effectively unreachable via this endpoint today, since `CustomerContext` structurally guarantees `Store` is derived from the authenticated `Customer`'s own row — kept as defense-in-depth, not dead code removal); `IdempotencyKeyConflictException` → 409 (a deliberate, approved departure from this codebase's prior all-422 domain-exception convention, since a key-reuse conflict is semantically a 409). Stripe's own `\Stripe\Exception\IdempotencyException` (thrown when `error.type === 'idempotency_error'`) is caught ahead of the broader `\Stripe\Exception\ApiErrorException` it extends (PHP matches catch clauses in listed order); `getStripeCode() === \Stripe\ErrorObject::CODE_IDEMPOTENCY_KEY_IN_USE` (a concurrent identical-key request still in flight) is distinguished from a same-key-different-params conflict, both mapped to 409; every other `ApiErrorException` maps to 502 with a fixed, generic message — the real Stripe exception message is never returned to the client (only `report()`-logged). Success returns `{"data": <OrderResource>, "payment": {"client_secret", "stripe_payment_intent_id"}}` — 201 for a newly-created order, 200 for an idempotent replay (detected via Eloquent's own `$order->wasRecentlyCreated`, no `CheckoutOrderCreationService` change needed); `client_secret` is never persisted anywhere, only ever taken from the current request's own Stripe response (correct even on a replay, since Stripe returns the full object including `client_secret` on an idempotent cache hit too). A **narrow, explicitly accepted MVP residual risk**: the provisional pre-Stripe amount and `CheckoutOrderCreationService`'s later, authoritative recomputation could theoretically diverge if a variant's price changes in the small window between the two reads — per explicit instruction, **no `PaymentIntent::update()` reconciliation call was added**; a log-only mismatch check (`Log::warning`) runs after `createPendingOrder()` returns, with no Stripe mutation. New `Tests\Doubles\FakePaymentIntentGateway` (bound into the container in place of the real gateway) records every `create()` call and can script a canned `PaymentIntent` or a real Stripe SDK exception (built via the SDK's own `::factory()` — e.g. `IdempotencyException::factory(..., ErrorObject::CODE_IDEMPOTENCY_KEY_IN_USE)`) — no real Stripe credentials or network traffic anywhere in the suite. 11 new tests in `tests/Feature/Customer/CheckoutTest.php` (286 total, up from 275): happy path (order/items/address/payment/inventory claim all correct, exact Stripe amount/currency/`payment_method_types`/metadata/idempotency-key verified against the fake gateway's recorded call, `client_secret` returned); missing-Idempotency-Key, empty-cart, malformed-quantity, nonexistent-variant, and cross-store-variant all rejected with 422 and zero Stripe calls; insufficient-inventory rejected with Stripe called exactly once and the entire local write rolled back; same-key-same-payload replay returns the identical order (200, no duplicate rows); same-key-different-payload rejected (409, original order unchanged); Stripe's own concurrent-idempotency-key conflict mapped to 409; a generic Stripe API failure mapped to 502 without leaking the underlying Stripe message. **One requested test case was deliberately not written as literally specified**: an HTTP-level "customer/store mismatch defense-in-depth" test, because `ResolveCustomerContext` re-binds `CustomerContext` fresh on every request from the authenticated customer's own `store_id` column, and the checkout request schema has no `store_id` field at all — there is no HTTP-reachable input that could ever produce a mismatch, and pre-seeding a mismatched `CustomerContext` before dispatch is unconditionally overwritten by the middleware before the controller runs. That mechanism is already proven directly at the service level by the STEP 3A audit's own `test_rejects_a_customer_belonging_to_a_different_store`; a forced HTTP-level test here would have had to bypass the very middleware that makes the case unreachable, testing something false. Full backend suite re-run afterward: 286/286 passing, zero regressions. Pint clean (8 files). **No `/api/cart`/Redis cart infrastructure, saved-address CRUD, Stripe webhook, retry-payment endpoint, expiry sweep, or frontend code was implemented in this block** — checkout accepts cart/address as inline request input, per the explicitly approved STEP 3B scope decision. Not yet committed.

**STEP 3B Audit Fixes — complete and verified**, 2026-09-01. Two MEDIUM findings from STEP 3B's implementation audit, both closed at the request boundary (`App\Http\Requests\Customer\CheckoutRequest`) without touching `CheckoutController`, `CheckoutOrderCreationService`, or any other frozen file: (1) duplicate `product_variant_id` entries in a single checkout request (e.g. the same variant listed twice with different quantities) were previously neither merged nor rejected, silently producing two `OrderItem` rows and two inventory-claim ledger rows for what should be one line — closed by a new `mergeDuplicateItems()` step in `prepareForValidation()` that sums quantities per variant id before validation ever runs, so every downstream step (validation, pricing, the idempotency payload hash, `CheckoutOrderCreationService`'s own line items) sees an already-canonical cart; a request expressing the same cart as `[{123,5}]` or `[{123,2},{123,3}]` now produces the identical idempotency hash. (2) the `Idempotency-Key` header accepted whitespace-only values as "present" (Laravel's `required` rule doesn't reject a string of spaces) — closed by trimming the header in the same `prepareForValidation()` step before validation, so a whitespace-only key now correctly fails `required` (422) and the trimmed value is what's used consistently for Stripe's idempotency option and `orders.idempotency_key`. Both fixes are defensive against malformed input, never throwing on it — an unrecognized item shape falls through to the existing `required`/`integer` rules rather than crashing `prepareForValidation()` itself. 4 new tests in `CheckoutTest.php` (290 total, up from 286): duplicate-line merging (one `OrderItem`, summed quantity, correct subtotal, single Stripe amount, single inventory claim), cross-shape idempotency-payload equivalence, whitespace-key rejection, and trim-and-consistency across Stripe/`orders.idempotency_key`/a follow-up request using the already-trimmed key. Full suite re-run: 290/290 passing. Pint clean.

**Checkpoint commit**, 2026-09-01 (`d72a09e`, `feat(checkout): add customer checkout with Stripe PaymentIntent`) — bundles STEP 3A, its audit-findings cleanup, STEP 3B, and STEP 3B's audit fixes into one commit (16 files). Database Design 2.6's schema migration was already committed separately, earlier, in `a5e4952`. 290/290 passing at commit time, Pint clean across all 180 project files at that point.

**Phase 5 STEP 3C — Stripe Payment Webhook Processing: complete and verified**, 2026-09-01, following a read-only design/architecture audit pass (inspecting the actual installed `stripe/stripe-php` v21.3.0 signature-verification API and exception taxonomy from source, not assumed) before implementation began. Adds `POST /api/webhooks/stripe` (`App\Http\Controllers\Webhooks\StripeWebhookController`) — deliberately unauthenticated and untenanted (no `auth:*`/`tenant.*` middleware; Stripe cannot authenticate via any of this app's guards, and API routes in this Laravel version carry no CSRF middleware to exclude). `Stripe\Webhook::constructEvent()` against the raw, unparsed request body (`$request->getContent()`) and a new `services.stripe.webhook_secret` (`STRIPE_WEBHOOK_SECRET`) is the entire trust boundary — a `SignatureVerificationException`/`UnexpectedValueException` maps to 400 before any database work happens; nothing downstream ever re-derives tenant/store/customer identity from the event's own `metadata` (only Stripe's own `payment_intent.id` is used, purely as a lookup key into `payments.stripe_payment_intent_id`, which is unique) — the local `Payment`/`Order` rows are authoritative for everything else.

New `App\Services\StripePaymentWebhookService` owns the entire transactional core, mirroring `CheckoutOrderCreationService`/`InventoryAdjustmentService`'s established shape: one `DB::transaction()` per event, `stripe_webhook_events` (`stripe_event_id` unique — the durable, at-least-once-delivery-safe idempotency guard; `processed_at = now()` set at insert time, satisfying its `NOT NULL` constraint, correctness coming from the whole transaction committing or rolling back atomically rather than from that timestamp's precise meaning) inserted first, then a `lockForUpdate()`-locked `Payment` row re-read and transitioned only if its *current* status (checked under the lock, never a pre-lock read) is non-terminal — `Succeeded`/`Failed`/`Canceled` never regress or re-transition, so a stale/out-of-order event (e.g. `processing` arriving after `succeeded` was already applied) is a safe no-op that's still durably recorded. Exactly four event types are handled (`payment_intent.processing`/`succeeded`/`payment_failed`/`canceled`); any other type (including `charge.refunded`, out of scope) is recorded in `stripe_webhook_events` for the audit trail and otherwise ignored, returning 200 — Stripe should never be asked to retry an event this app will never act on. A duplicate delivery of an already-processed event is detected specifically via the `stripe_webhook_events_stripe_event_id_unique` constraint name in the caught `QueryException` (not a generic catch, and not a pre-check-then-insert, which would race) and returns 200 without reprocessing.

`payment_intent.succeeded` is the only event that can move `orders.status: pending → paid` (`paid_at` set) — guarded by its own `lockForUpdate()` on the `orders` row and only applied if the order is *currently* `pending`; if not (e.g. already cancelled through some other path), the Payment still transitions to `succeeded` but the order is left untouched, never regressed. This is a **webhook-specific transition that deliberately does not go through `OrderStatusUpdateService`/`MerchantOrderStatusTransitions`** — both were confirmed (by reading the actual code, not assumed) to be scoped to merchant-triggered edges only, and `pending→paid` is explicitly excluded from that whitelist by design; extending it would have blurred that class's documented boundary, so the one system-only edge is a small dedicated check inside the new service instead. No inventory change on success (already claimed at checkout). `payment_intent.payment_failed`/`payment_intent.canceled` set `payments.failure_reason` (from `last_payment_error.message`, or `cancellation_reason` when present for a cancellation) and release inventory — critically, **driven entirely by `inventory_transactions` rows where `payment_id = $payment->id AND reason = 'checkout'`, never by `$order->items`** — a real design finding from this pass: `order_items` carries no `payment_id` of its own, and only equals "what this specific payment attempt claimed" today because retry-payment (a second `Payment` per `Order`) doesn't exist yet; querying the ledger instead means this stays correct once it does, with no rework needed later. `InventoryAdjustmentService::adjust()` is reused completely unmodified for the release (reason `Release`, same `$orderItem`/`$payment` linkage the original `checkout` claim used, producing the correct `dedup_key`). A supported event referencing a Stripe PaymentIntent id with no matching local `Payment` throws a new `App\Exceptions\UnknownPaymentIntentException` **from inside** the transaction, so the entire transaction — including the just-inserted `stripe_webhook_events` row — rolls back, and the controller maps it to 404 (retryable), letting a genuine Stripe retry self-heal a narrow timing race rather than permanently losing the event.

25 tests in `tests/Feature/Webhooks/StripeWebhookTest.php` (315 total, up from 290) — every fixture `Payment` is produced by the real, unmodified `CheckoutOrderCreationService` (not hand-built factories), and every signature is genuinely computed/verified via the real SDK (`Stripe\WebhookSignature::generateSignatureHeader()`/`Stripe\Webhook::constructEvent()`, no fake — signature verification is pure local HMAC, no network call, so nothing needed faking): the four core event types; invalid signature, malformed JSON, expired signature timestamp, missing signature header, and whitespace-only signature header (all 400, nothing persisted); duplicate delivery (processed once); unknown PaymentIntent (404, full rollback verified); unsupported event type (200, recorded, no state change); stale `processing` after `succeeded` (no regression); `failed` unable to later become `succeeded`; `succeeded` correctly leaving a non-`pending` order untouched; the full `RequiresPayment`/`Processing` → each terminal-state predecessor matrix; inventory release scoped correctly to one specific payment (proven against two independent fixtures, not just asserted by inspection); a `SELECT ... FOR UPDATE` query-log proof against `payments`; and a forced mid-transaction failure (`Order::saving()` hook, mirroring Block 4B's established pattern) proving the event row, Payment change, and everything else all roll back together, not just that validation can reject a request beforehand.

**Read-only audit (same day) found no blocking issues, but one MEDIUM test-coverage finding**: only 2 of the 6 explicitly-relevant terminal-state regressions (`Failed→Succeeded`, `Succeeded→Processing`) had a dedicated test at that point — `Canceled→Succeeded`, `Succeeded→Failed`, and `Succeeded→Canceled` were provably safe by code inspection (all five rejections share one identical guard) but untested directly; separately, the only "duplicate delivery" test used `payment_intent.succeeded`, which never touches inventory, so "a duplicate failure/cancellation can't release inventory twice" was actually unverified despite looking covered. **Closed same day**: 4 additional tests — `test_canceled_payment_cannot_later_become_succeeded`, `test_succeeded_payment_cannot_later_become_failed`, `test_succeeded_payment_cannot_later_become_canceled` (each sets the Payment's precondition status directly, then sends the "wrong-direction" event and asserts no regression/no inventory mutation), and `test_duplicate_payment_failed_event_does_not_release_inventory_twice` (sends the *identical* `payment_intent.payment_failed` event twice, asserting exactly one `release`-reason ledger row and unchanged inventory quantity after the second delivery — not just a 200 status). All 4 passed on the first run with zero code changes — confirming the audit's inference from shared code was correct, not just plausible. No production behavior was changed as a result of closing this finding.

Full backend suite re-run afterward: 315/315 passing, zero regressions. Pint clean across all 184 project files (one cosmetic docblock-alignment fix applied by Pint itself during the original implementation, tests re-verified green afterward; no further Pint changes needed when closing the audit finding). **No Job classes, queued analytics/notifications, refund handling (`charge.refunded`), retry-payment endpoint, expiry sweep, or frontend code was implemented in this block** — `database-design.md` §4's "queue analytics/notification jobs" on success is a known, explicitly deferred gap: no Job/queue-consumer infrastructure exists anywhere in this codebase yet to queue to. Not yet committed.

**Phase 5 STEP 3D — Payment Retry / Recovery: complete and verified**, 2026-09-02, implemented exactly per the design approved 2026-09-01 (below) after two design passes (initial architecture, then a required revision closing a real concurrency gap the first pass missed) plus empirical verification against the live database. This section remains the authoritative spec — now confirmed matching the shipped code, with one approved implementation-time deviation documented after "Implemented files" below.

**Objective**: `POST /api/orders/{order}/payment-retry` — let a customer retry after their Payment `Failed`/`Canceled`, per "one Payment = one payment attempt"; a retry always creates a **new** Payment + new Stripe PaymentIntent, never revives a terminal one.

**Critical concurrency finding, resolved**: the first design pass proposed `[lock Order, check eligibility, release lock] → Stripe call → [new transaction: create Payment + claim inventory]`. This does **not** prevent two concurrent retries with *different* idempotency keys from both passing the eligibility check and both calling Stripe, since the Payment (the only durable "active" marker) doesn't exist until after Stripe resolves, by which point the Order lock has already been released. **Resolved by holding the `orders` row lock across the entire sequence, including the Stripe call**, deliberately breaking this project's own general "never hold a lock across a network call" rule for this one case — justified in detail below, and empirically proven correct (not just argued) via a real two-connection MySQL test: a second connection's `lockForUpdate()` was shown to block for the full duration of a first connection's held lock (including a nested-transaction rollback followed by a compensating write), only proceeding once the first connection's *entire* transaction had committed, and observing only the fully-resolved final state — never any intermediate/rolled-back state. Verified against the actual installed `laravel/framework` source (`Concerns/ManagesTransactions.php`): a `DB::transaction()` call made while already inside another one issues a real SQL `SAVEPOINT` (MySQL grammar supports this); if its closure throws, Laravel issues `ROLLBACK TO SAVEPOINT` — **not** a full rollback — so the outer transaction and every lock it holds survive untouched, and execution can continue inside the same outer transaction afterward (e.g. to write an Order cancellation) before that outer transaction finally commits or rolls back as a whole.

**Approved transaction sequence**:
```
DB::transaction():
    lock Order FOR UPDATE                         (held for the whole sequence)
    if Order.status !== Pending: throw (422)
    if Payment exists for (order_id, this Idempotency-Key): return it (200) — same-key replay
    if an active Payment (RequiresPayment/Processing) exists for this order: throw (409) — different-key conflict, Stripe never called
    if no prior Failed/Canceled Payment exists: throw (422) — sanity guard
    (optional, advisory-only, non-authoritative quick inventory glance)
    stripeIdempotencyKey = sha256("retry:{order_id}:{client_idempotency_key}")
    call Stripe PaymentIntent::create() — amount = order.total (unchanged, historical
        snapshot, never re-priced from current catalog), lock still held
        — on Stripe error: propagate, full outer rollback, nothing persisted, map per error type
    try:
        DB::transaction():                         (SAVEPOINT)
            insert Payment (status=RequiresPayment, stripe_payment_intent_id=real id,
                             idempotency_key=client's raw key)
            claim inventory for every order_item via InventoryAdjustmentService::adjust()
                (reason Checkout, this new Payment) — reused completely unmodified
    catch InsufficientInventoryException:
        (SAVEPOINT already rolled back the Payment+claim; outer lock/transaction intact)
        Order.status = Cancelled; Order.status_reason = item_no_longer_available; save()
        return — outer transaction COMMITS the cancellation (not the failed attempt)
    return — outer transaction COMMITS the new Payment + claim
```
Exactly one of two durable outcomes per attempt (new Payment+claim, or Order cancellation) — never a third, partial state; a crash before the outer commit leaves the Order untouched (`Pending`, cleanly retryable again), never a stuck/ambiguous row.

**All decisions finalized**:
- Order eligibility: `status === Pending` only. Payment eligibility: "active" = `RequiresPayment`/`Processing`; retry requires zero active Payments and ≥1 prior terminal (`Failed`/`Canceled`) one.
- An active Payment already existing → reject (409), no auto-cancel-and-replace (deliberately narrower than the original pre-STEP-3A-3C `database-design.md` §10 draft, which described cancelling a prior non-terminal PaymentIntent — not adopted, to avoid needing a new gateway `cancel()` method for this step).
- Retry amount = `order.total` (existing, write-once, historical) — never re-derived from current catalog prices. No `CatalogStatus::Active` re-check on retry (inventory availability is the only gate) — the Order already committed to these specific items at checkout.
- Idempotency: `payments.idempotency_key` (`UNIQUE(order_id, idempotency_key)`, already migrated) stores the client's **raw** key; no payload-hash column needed (retry has no variable cart/address input to diverge on, unlike checkout). Stripe's own idempotency key is a **derived** `sha256("retry:{order_id}:{client_key}")` — deliberately *not* the raw client key, to prevent a client accidentally reusing their original checkout's key from causing Stripe to return the original, dead PaymentIntent. A `payments.stripe_payment_intent_id` unique-constraint collision (structurally near-impossible under the hashed key) maps to 409.
- No migration, no new table, no placeholder/fake Stripe PaymentIntent ID anywhere. The "at most one active Payment per Order" invariant is enforced entirely by the held Order lock; a generated-column unique-index backstop (mirroring `inventory_transactions.dedup_key`'s pattern) remains available but explicitly not adopted, matching `database-design.md` §10's own original conclusion.
- Orphaned Stripe PaymentIntent on inventory failure is left inert, uncancelled — matches checkout's own established precedent; no new `StripePaymentIntentGateway::cancel()` method needed for this step.
- Lock-wait timeout on a genuinely blocked second request → 503.
- **Zero changes to STEP 3A/3B/3C** — `CheckoutOrderCreationService`, `InventoryAdjustmentService`, `StripePaymentIntentGateway`, `StripePaymentWebhookService`, `OrderStatusUpdateService`, `MerchantOrderStatusTransitions` all reused/left exactly as-is. STEP 3C's existing `UnknownPaymentIntentException` → 404 → rollback → Stripe-retries-later behavior is directly load-bearing for retry's own webhook-race safety (re-verified compatible under the revised lock design), not merely unaffected — no new webhook logic needed.

**Implemented files**: `App\Http\Controllers\Customer\RetryPaymentController`, `App\Http\Requests\Customer\RetryPaymentRequest`, `App\Services\PaymentRetryService`, `App\Exceptions\OrderNotEligibleForRetryException`, `App\Exceptions\ActivePaymentExistsException`, `tests/Feature/Customer/PaymentRetryTest.php`; `routes/api.php` modified to add the one new route. Route: `POST /api/orders/{order}/payment-retry`, `auth:customer`+`tenant.customer`, `{order}` resolved and scoped to `$context->customer->id` (never implicit route-model binding, matching every existing resource-resolution convention in this codebase). Response shape mirrors checkout: `{"data": <OrderResource>, "payment": {"client_secret", "stripe_payment_intent_id"}}`, 201 new / 200 replay. No migration, no schema change — confirmed unnecessary, exactly as designed.

**One approved deviation from the design as originally written, forced by real MySQL behavior discovered during implementation, not a design flaw**: the plan called for detecting a same-key replay's duplicate `Payment` insert by matching the `payments_order_id_idempotency_key_unique` constraint name in the caught `QueryException` message (mirroring `CheckoutOrderCreationService::isIdempotencyKeyViolation()` verbatim). Testing revealed this doesn't work for retry: a genuine same-key replay sends Stripe the identical derived idempotency key, so Stripe returns the identical `PaymentIntent`, which means the resulting `INSERT` collides on `payments_stripe_payment_intent_id_unique` — an older index, created in the original `payments` migration — before MySQL ever reaches the newer composite `(order_id, idempotency_key)` index added for this design; MySQL only reports whichever constraint it checks first. Resolved by making `PaymentRetryService` constraint-name-agnostic: on any MySQL 1062 ("Duplicate entry") — detected narrowly via `$e->errorInfo[1] === 1062`, the same narrow-detection style already required for the 1205 lock-wait-timeout case — it looks up whether a `Payment` already exists for `(order_id, idempotency_key)`; if found, that's the replay (200, no new row); if not, it's a genuine, unresolvable conflict, mapped to 409 via `ActivePaymentExistsException`. This is a more robust implementation of the same intent than the original constraint-name match — it resolves by asking the question that actually matters ("does a Payment for this exact request already exist?") rather than by naming a specific index — and it also correctly handles the structurally-near-impossible `stripe_payment_intent_id` cross-order collision the design already called out, which the original constraint-name-only approach would not have mapped to 409 at all.

**14 tests, 73 assertions, in `tests/Feature/Customer/PaymentRetryTest.php`** (329 total backend tests, up from 315): happy path (new Payment + inventory re-claim); missing idempotency key; order not Pending; no prior terminal Payment; active-Payment conflict (different key → 409); same-key replay (Stripe called again with the identical derived key, no duplicate Payment row, same `client_secret` returned); insufficient-inventory-triggers-cancellation; `stripe_payment_intent_id` collision across two unrelated orders (409); cross-customer order access (404); generic Stripe API failure (502, no detail leak); a narrow reflection-based test proving the MySQL 1205 lock-wait-timeout detector matches *only* 1205 (not 1213 deadlock, not 1062 duplicate-entry); plus three tests added specifically to close the architecture-review findings below — a `DB::listen()`/`DB::transactionLevel()` proof that the `orders` row lock is genuinely acquired via `SELECT ... FOR UPDATE` and is still held (the transaction still open) at the exact moment the Stripe call happens, not released and reacquired around it; a forced mid-sequence failure (an `InventoryTransaction::saving()` listener, mirroring `StripeWebhookTest`'s established forced-failure pattern) proving the entire attempt — the new Payment row and the inventory decrement together — rolls back cleanly with the Order left untouched at `Pending`; and a two-item retry (mirroring `CheckoutOrderCreationServiceTest::test_second_line_items_failure_rolls_back_the_first_line_items_claim_too`) proving a second item's genuine insufficient-inventory failure also unwinds the first item's already-successful claim and the Payment row together, leaving the Order cleanly `Cancelled` rather than partially claimed.

**Read-only architecture review across STEP 3A–3D (same day, before this closure), verdict APPROVED WITH CONCERNS, no Critical/High findings**: a full inspection of checkout, PaymentIntent creation, retry, webhook processing, order/payment status transitions, inventory claim/release, transactions/savepoints, row locking, idempotency/replay, Stripe↔DB failure scenarios, duplicate handling, webhook ordering, tenant isolation, and error mapping found no correctness defect, double-charge, double-decrement, or tenant-isolation break. Two MEDIUM test-coverage findings — no committed test proved the `orders` row lock is genuinely acquired and held across the Stripe call (STEP 3C has this exact proof for `payments`; STEP 3D didn't yet), and no test proved a multi-item retry's second-item failure rolls back the first item's already-successful claim (checkout has this exact proof; STEP 3D didn't yet) — were **closed the same day** by the three tests listed above, with zero production code changes. Several LOW/informational notes were also recorded (an inherent, accepted eventual-consistency window between the webhook's terminal-failure path — which locks only `payments`, not `orders` — and retry's unlocked eligibility read, degrading at worst to one spurious 409 self-resolved on the next attempt, never to data corruption; the accepted lock-across-Stripe-call trade-off's interaction with `innodb_lock_wait_timeout` and with a concurrent merchant order-cancel; G3's late-payment/inventory-oversell scenario across genuinely different checkout submissions remains open, unaffected by STEP 3D) — none blocking, no code changes required.

**Empirical verification performed during design** (not just reasoned): a disposable scratch table (created and dropped via raw SQL, no migration, no repo file touched) and two genuinely concurrent `docker compose exec` processes proved the exact required invariant — "once inventory claim fails for a retry attempt, the Order becomes permanently non-retryable before another retry request can acquire the lock and observe it as Pending" — holds under real MySQL locking, with millisecond-precision timestamps confirming the second connection blocked for the full duration and never observed the rolled-back intermediate state.

Full backend suite re-run afterward: 329/329 passing, 1028 assertions, zero regressions. Pint clean across all 190 project files. Not yet committed.

**Phase 6 — Payment Expiry Sweep: complete, verified, and committed**, 2026-09-03 (`455917b`, `feat: implement payment expiry sweep`) — the scheduled counterpart to `StripePaymentWebhookService`'s failure/cancellation handling (database-design.md §12): cancels a `pending` Order whose current `Payment` has sat at `requires_payment` (never reached `processing`) past a configured window, releasing its claimed inventory and best-effort cancelling the orphaned Stripe PaymentIntent. New `App\Services\PaymentExpirySweepService` anchors the staleness window on the `Payment`'s own `created_at` (never the `Order`'s, since a retry legitimately restarts the window), locks `payments` then `orders` under `DB::transaction()` mirroring `StripePaymentWebhookService`'s own lock order, performs a live Stripe `retrieve()` as a final guard before cancelling, and processes each stale `Payment` independently so one Stripe/lock failure never blocks the rest of the batch; the best-effort `PaymentIntent::cancel()` call happens only after the local cancellation has already committed. `StripePaymentIntentGateway` gains `retrieve()`/`cancel()` alongside the existing `create()` — `CheckoutOrderCreationService`, `PaymentRetryService`, and `StripePaymentWebhookService` are all unchanged. A new `App\Console\Commands\ExpireStalePayments` command (`payments:expire-stale`) is what runs the sweep; a new `scheduler` service (`php artisan schedule:work`) added to `docker-compose.yml` is what actually fires it every minute in this Docker environment, verified firing on schedule via the running stack, not just assumed from route/command registration. No schema change — `orders.status`/`status_reason` and `payments.status` already supported every value this sweep needs.

23 new tests (339 total): `PaymentExpirySweepServiceTest` covers the happy path, the within-window/`processing`/no-longer-`pending` skip cases, Stripe deferring the cancellation, idempotent re-runs, per-candidate error isolation, a failed best-effort Stripe cancel not blocking the local commit, and a `SELECT ... FOR UPDATE` proof against both `payments` and `orders`; `ExpireStalePaymentsTest` covers the Artisan command wrapper. Full backend suite re-run: 339/339 passing, Pint clean.

**Known non-blocking environment gap, pre-existing and not introduced by this change**: this dev environment's `STRIPE_SECRET` is blank, so `payments:expire-stale` currently errors at runtime here (`Stripe\Exception\InvalidArgumentException`) exactly as a live checkout/retry attempt already would outside the test suite — nothing before this command ever resolved `StripePaymentIntentGateway` outside tests. A real Stripe test-mode key in `backend/.env` is needed to exercise this live end-to-end. See also "Known Issues" below.

**Phase 7 — Customer-facing Order History: complete, verified, and committed**, 2026-09-04 (`5bc3c90`, `feat: implement customer order history`) — read-only, authenticated APIs `GET /api/customers/orders` (paginated list, newest-first) and `GET /api/customers/orders/{order}` (detail), reusing the existing `customer` Sanctum guard and `tenant.customer` (`ResolveCustomerContext`/`CustomerContext`) middleware exactly as-is — no new guard, middleware, or Policy. Customer ownership of an order is enforced entirely through server-side scoped queries (`Order::where('store_id', $context->store->id)->where('customer_id', $context->customer->id)`, matching STEP 3D's `RetryPaymentController::resolveOrder()` discipline) — never implicit route-model binding, never a client-supplied `organization_id`/`store_id`/`customer_id`. A cross-customer, cross-store, or cross-organization order id all collapse into the same `404` (never `403`), matching this project's established "don't distinguish absence from unavailability" convention. No Policy and no Service class were added — order history is a plain read scoped by a single ownership FK, not a multi-role authorization matrix, and involves no transactional/concurrency-sensitive write.

A new, dedicated `App\Http\Resources\Customer\CustomerOrderResource` (distinct from the merchant-facing `OrderResource`, mirroring the existing `Resources\Catalog\CatalogProductResource` precedent) omits `organization_id`/`customer_id`/`store_id` and every internal/admin-only field — no raw `Payment`/`Refund` rows, no Stripe identifiers, no `inventory_transactions`. List responses stay lightweight (no `items`/`shipping_address`, matching merchant `OrderResource`'s `index()`); detail eager-loads and includes both. A new `Order::latestPayment(): HasOne` relation (`hasOne(Payment::class)->latestOfMany()`) derives a customer-visible `payment_status` from the most recent payment attempt only — never a stored column, per the pre-existing "payment status is derived, never stored" rule (see "Final Resolution — Order/Payment Status Separation" under Database Decisions below). Status/date filtering and a refunds array were both deliberately deferred, per the approved design (not required by PRD §7.3 for the customer-facing view; trivial to add later mirroring merchant's `Rule::in` status filter).

16 new tests (355 total) in `tests/Feature/Customer/CustomerOrderHistoryTest.php`: authenticated list/detail access; unauthenticated rejection (401); another customer's/another store's/another organization's order all rejected (404); merchant token rejected (401); nonexistent order (404); pagination default of 15 with `meta.total` correct; empty order history; newest-first ordering; list excluding items/shipping-address; detail including both; a negative assertion proving `organization_id`/`customer_id`/`store_id`/Stripe identifiers/raw `payments`/`refunds`/`inventory_transactions` never appear in the response; and a dedicated test proving `payment_status` reflects the **latest** of two `Payment` rows for the same order (an earlier `failed` attempt and a later `succeeded` retry, with `created_at` set explicitly rather than relied on real-clock timing) — not merely the first or any arbitrary one. Full backend suite re-run afterward: 355/355 passing, 1116 assertions, zero regressions. Pint clean across all 197 project files (one cosmetic import-ordering fix applied by Pint itself, Phase 7 tests re-verified green afterward).

**Phase 8 — Frontend Storefront (Blocks 8-0 → 8A → 8C → 8B → 8D): implemented and verified by this documentation-reconciliation pass, 2026-09-06.** This entry was added retroactively — the five commits below landed on 2026-09-05 without a corresponding `project-status.md` update, which this pass corrects after re-inspecting each commit's actual diff (not inferring scope from commit messages) and re-running the full verification suite against HEAD. No application code was changed to produce this entry.

**Block 8-0 — Playwright E2E Scaffolding: complete and verified**, 2026-09-05 (`d01523d`, `test: add Playwright E2E scaffolding`). Establishes the E2E harness ahead of any storefront feature work: `@playwright/test` added, `playwright.config.ts` (Chromium only for now; `baseURL http://localhost:5173`; `webServer` runs `npm run dev`, reusing an already-running dev server outside CI), and one smoke spec (`e2e/smoke.spec.ts`) asserting an unauthenticated visitor is redirected to the admin login page. Talks to the Vite dev server over HTTP only — no MySQL/Redis/Docker backend required for this spec. No storefront code exists yet at this commit.

**Block 8A — Storefront Catalog: complete and verified**, 2026-09-05 (`480027b`, `feat: add storefront catalog`). Adds the public, unauthenticated storefront browsing UI against the existing `GET /api/shop/stores/{store}/products[/{product}]` endpoints (already shipped as a Phase 5 prerequisite) — `src/customer/catalog/{ProductListPage,ProductDetailPage,useCatalog}.tsx`, `src/services/catalogApi.ts` (typed client mirroring `CatalogProductResource`), `src/customer/layout/{StorefrontLayout,HomePage}.tsx`, and store-scoped routing under `/store/:storeId/*` in `App.tsx`. One small, additive **backend** change accompanied this block, confirmed by reading the diff rather than the commit message: `CatalogProductResource` gained a coarse per-variant `in_stock` boolean (`quantity_on_hand > 0`; exact quantity stays merchant-only) and `ProductController` now eager-loads `variants.inventory` to supply it — a display hint only, since checkout independently re-validates availability under a row lock regardless of what this endpoint reports. No schema change; 75 lines of new/changed assertions in `CatalogProductTest.php`. New `e2e/catalog.spec.ts` (list rendering + detail navigation, empty state, API-failure state, nonexistent-product state) — mocked at the network boundary, no live backend required.

**Block 8C — Customer Authentication (frontend): complete and verified**, 2026-09-05 (`b496b0f`, `feat: add customer authentication`). Adds the browser-facing register/login/logout/session-restore flow against the existing `customer`-guard endpoints (`POST /api/customers/auth/{register,login,logout}`, `GET /api/customers/auth/me` — all backend work already shipped by Phase 5's Customer Authentication prerequisite; no backend files changed in this commit). `CustomerAuthContext` mirrors `AuthContext`/`MerchantAuthContext`'s established shape with one deliberate difference: because `customers.email` is unique only per store (not globally), the session token is stored under a **per-store** localStorage key (`ai_commerce.customer_token.{storeId}`), with `storeId` read from the route itself — a design that lets one browser hold independent sessions for two different stores' storefronts, adjusted during render (not a `useEffect`) when the route's `storeId` changes. Added: `CustomerProtectedRoute`, `CustomerLoginPage`, `CustomerRegisterPage`, and a minimal `AccountPage` (explicitly a placeholder — its own in-code comment defers order history to Block 8E). Only `/store/:storeId/{checkout,account}` are gated behind authentication; catalog browsing stays public. New `e2e/customer-auth.spec.ts` (unauthenticated nav state, protected-route redirect, login reflected in nav/account access, logout) — mocked backend, matching 8A's approach.

**Block 8B — Client-side Storefront Cart: complete and verified, with one recorded architecture deviation requiring review before 8E.** 2026-09-05 (`52d7a10`, `feat: add client-side storefront cart`). Adds `CartContext`/`cartStorage.ts` (a validated, versioned localStorage schema — `CART_SCHEMA_VERSION`; corrupt, mismatched, or malformed stored data is dropped rather than crashing the app), `CartPage`, and a `QuantityStepper` component; `ProductDetailPage` gained add-to-cart.

**Deviation from the previously-approved Cart Architecture (Database Design 2.2 / CLAUDE.md "Cart Architecture" / `system-architecture.md` §7), found during this reconciliation, not previously recorded anywhere:** the approved design specifies guest carts in `localStorage` and **authenticated**-customer carts in Redis, keyed server-side by a resolved tenant/customer context, TTL'd. What actually shipped is a single store-scoped `localStorage` cart **shared by guest and authenticated customers alike** — no `/api/cart` endpoint and no Redis cart integration exist anywhere in the repository; logging in or out never touches the cart (no server-side merge-on-login). The module's own in-code docblock (`cartStorage.ts`) explicitly says this deviation should be "recorded" in this file's Block 8B entry — this paragraph is that record, added now rather than at implementation time. Per CLAUDE.md's own development rules ("if implementation conflicts with the approved architecture, stop and report the conflict instead of silently changing the architecture" / "do not silently redesign approved architecture"), **this is flagged as an open, unreconciled conflict — not a re-approval of the new design.** This reconciliation pass is documentation-only and does not resolve it; see "Known Issues" below. Everything the cart stores is explicitly documented in-code as display-only (`displayPrice`, `inStockAtAdd` are add-to-cart-time snapshots, never authoritative — checkout independently re-fetches and revalidates). New `e2e/cart.spec.ts` (add-to-cart, quantity increment/decrement/direct-set, item removal, persistence across reload, per-store cart isolation) — mocked backend, matching 8A/8C.

**Block 8D — Checkout + Stripe: complete and verified**, 2026-09-05 (`a7fdd82`, `feat: add storefront checkout with Stripe`). Adds the storefront checkout UI against the existing `POST /api/checkout` endpoint (Phase 5 STEP 3B/3C — confirmed unmodified by this commit) via `@stripe/react-stripe-js`/`@stripe/stripe-js` (new dependencies) — `CheckoutPage`/`CheckoutForm`, `services/{checkoutApi,stripeClient}.ts`; `/store/:storeId/checkout` is gated behind `CustomerProtectedRoute` (no guest checkout, matching CLAUDE.md). Behaviors verified by direct code reading, not assumed from the commit message: a Stripe `PaymentIntent` status of `processing` is handled as a distinct `paymentPending` state and is never folded into success — the cart is not cleared and no success message renders, since only a later `payment_intent.succeeded` webhook (Phase 5 STEP 3C, unaffected) actually marks the order paid; the client-generated `Idempotency-Key` (`crypto.randomUUID()` per checkout attempt) is deliberately regenerated if the submitted cart/address changes between two submit attempts within the same page mount, so a materially different payload can never reuse a stale key (an unchanged resubmission — e.g. a double-click, or retrying after a card decline — correctly reuses the same key and the same already-created order); the client sends only `product_variant_id`/`quantity` plus address fields — display price/subtotal are never sent, and the authoritative total/order come back in the server's response. `stripePromise` is a module-level singleton (per Stripe's own guidance) that resolves to `null` if `VITE_STRIPE_PUBLISHABLE_KEY` is unset, mirroring the backend's pre-existing blank-`STRIPE_SECRET` gap (see "Known Issues"). New `e2e/checkout.spec.ts` (10 specs: protected-route redirect, cart→checkout navigation, order-summary rendering, request-shape assertion, successful payment, backend-validation failure, Stripe decline, in-flight button disabling, the `processing` non-success state, and the idempotency-key-changes-on-payload-change behavior) — **both the backend API and Stripe.js itself are mocked** (`window.Stripe` is pre-defined via a Playwright init script before the app's real `loadStripe()` call runs); no real Stripe account, live Stripe API, real card data, or real PaymentIntent is used anywhere in this suite, and none of it exercises the live Docker backend.

**Verification performed by this reconciliation pass (2026-09-06), against HEAD, not merely assumed from the commits above:**
- **Backend**: `docker compose exec app php artisan test` → **359 passed, 1127 assertions** (up from the 355/1116 recorded at the Phase 7 entry — the increase is Block 8A's `CatalogProductTest.php` additions; no other backend test file changed). Zero regressions.
- **Frontend build**: `npm run build` (`tsc -b && vite build`) → succeeds, no type or build errors.
- **Frontend lint**: `npm run lint` (`oxlint`) → zero errors; 4 pre-existing `react(only-export-components)` fast-refresh warnings across four unrelated Context files (`admin/auth/AuthContext.tsx`, `customer/auth/CustomerAuthContext.tsx`, `customer/cart/CartContext.tsx`, `merchant/auth/MerchantAuthContext.tsx`) — not introduced by Phase 8, not blocking.
- **Playwright**: `npx playwright test` → **24/24 passed** across `smoke`/`catalog`/`customer-auth`/`cart`/`checkout` specs (Chromium only, per `playwright.config.ts`).
- **Important caveat, stated explicitly rather than left implicit**: every Playwright spec above mocks the backend API at the network boundary, and Block 8D's checkout specs additionally mock Stripe.js — **none of this is a verified live-integration test** against the real Laravel backend, a seeded database, or a real Stripe test-mode account, and no manual browser walkthrough of the storefront is recorded as having been performed. "24/24 passing" describes frontend-logic correctness under mocked conditions, not an end-to-end proof that catalog→cart→checkout works against the live stack.
- `git status --short` was clean (no output) both before this pass began and is required to remain clean (`docs/development/project-status.md` only) after it.

**Phase 8 Block 8B Revision — Authenticated Redis Cart: complete and verified, 2026-09-06.** Supersedes, but does not erase, the original Block 8B entry above — that entry accurately records what actually shipped in `52d7a10`; this entry records the follow-up work that brings it into agreement with the originally-approved Cart Architecture (Database Design 2.2 / CLAUDE.md / `system-architecture.md` §7), per the deviation flagged (not resolved) by the prior documentation-reconciliation pass.

A design review preceded implementation (separate turn, same day): confirmed via direct inspection of `CartContext.tsx`/`cartStorage.ts` that the original implementation's public `useCart()` shape could be preserved unchanged for consumers, scoped the actually-necessary backend surface, and specified the merge algorithm, Redis key/payload shape, and every edge case (stale variant, inactive store, merge failure, Redis unavailability) before any code was written. That review's recommendation (Option A — revise now, before Block 8E) was approved and implemented as follows.

**Backend** — entirely new, no prior cart backend code existed: `App\Services\CartService` (the single Redis mutation/read path — `getCart`/`addItem`/`setItemQuantity`/`removeItem`/`clearCart`/`mergeGuestCart` — mirroring this codebase's established "one service owns the storage" discipline, with Redis's own atomic `HINCRBY`/`HSET`/`HDEL` standing in for the `SELECT ... FOR UPDATE` row-locking pattern used elsewhere); `App\Http\Controllers\Customer\CartController` (`GET/POST/PATCH/DELETE /api/cart[...]`, `POST /api/cart/merge` — all under the existing `auth:customer`+`tenant.customer` middleware, `CustomerContext` supplying ownership server-side exactly like `CheckoutController`/`OrderController`/`RetryPaymentController`; no new middleware, no new Policy, no parallel authorization system); `AddCartItemRequest`/`UpdateCartItemRequest`/`MergeCartRequest` (shape-only validation, matching `CheckoutRequest`'s existing split between request-shape and service-level business validation). A dedicated Redis connection/database (`config/database.php`'s `redis.cart` block, `REDIS_CART_DB`, default `2`) keeps cart storage isolated from the queue's `default` connection and the analytics `cache` connection, per CLAUDE.md's four-distinct-Redis-roles framing — confirmed via `docker compose exec app php artisan tinker` that writes to the `cart` connection are genuinely invisible on `default`. New `config/cart.php` holds `redis_connection` and a `ttl_seconds` sliding-TTL default (`CART_TTL_SECONDS`, 2,592,000 = 30 days) — a provisional value chosen at implementation time, since neither `system-architecture.md` nor any other document fixed one beforehand (§7 explicitly called this "an implementation detail, not fixed here"); the TTL is reissued (not merely set once) on every mutation. Redis stores strictly `{product_variant_id: quantity}` — every read (`GET`, and every mutation's response) hydrates display data fresh from MySQL (`ProductVariant`+`product`+`inventory`+`optionValues.option`, scoped to `store_id` + `CatalogStatus::Active`), and a variant id that no longer resolves is dropped from the response and lazily pruned from the Redis hash (`HDEL`) rather than failing the whole read — mirroring the "lazily materialized" precedent already established for `Inventory` rows. Merge sums via the same atomic `HINCRBY` a plain add uses (guest `A×2` into existing Redis `A×3` → `A×5`, order-independent); no inventory check exists anywhere in this service, by design — checkout's existing locked `InventoryAdjustmentService` claim remains the sole authoritative gate, unchanged.

**A real bug was found and fixed via `docker compose exec app php artisan tinker` verification, not merely assumed**: `CartController::clear()` initially declared a `JsonResponse` return type while calling `response()->noContent()` (which returns a plain `Illuminate\Http\Response`) — a type error caught immediately by the test suite, fixed by correcting the return type.

**21 new backend tests** in `tests/Feature/Customer/CartTest.php` (380 total, up from 359 — Phase 7's reconciliation-pass count): unauthenticated/cross-guard rejection on every route; empty-cart read; add/duplicate-add-sums/set-quantity/remove/clear; merge into an existing cart (sums), into an empty cart (adopts), and with an empty payload (no-op); a stale (soft-deleted) variant dropped from the response *and* pruned from Redis; a variant belonging to another store never hydrating even when directly seeded into the hash (proving store-scoping is enforced at hydration, not merely assumed); cross-customer and cross-organization isolation (both requiring the already-documented `Auth::forgetGuards()` `RequestGuard`-caching workaround between two different-identity requests in one test method — the same gotcha recorded for `CustomerTenantIsolationTest`); client-supplied `customer_id`/`store_id`/`organization_id` proven inert; TTL set and matching `config('cart.ttl_seconds')`; no inventory enforcement on add/update even against a zero-stock variant; and non-numeric `{variant}` route segments rejected (404, via a `whereNumber` route constraint). Full backend suite re-run: **380/380 passing** (up from 359), zero regressions. Pint clean.

**Frontend**: `services/cartApi.ts` (new, mirrors `catalogApi.ts`/`checkoutApi.ts`'s conventions). `CartContext.tsx` was restructured, not rewritten — its public interface is unchanged except two additive fields (`isLoading`, `error`), so `CartPage`/`ProductDetailPage`/`QuantityStepper`/`StorefrontLayout` needed no changes to their core logic (`CartPage` and `CheckoutPage` each gained a small loading/error-aware guard, described below). Internally, `CartContext` now branches on `useCustomerAuth().status`: unauthenticated/loading behaves exactly as the original Block 8B implementation (synchronous, `cartStorage.ts`, unmodified); authenticated is backed by React Query against `cartApi.ts`, mirroring `CustomerAuthContext`'s own `useQuery`/`useMutation` pattern. A login-time merge effect — necessarily living in `CartContext` rather than `CustomerAuthContext`, since `CartProvider` is nested *inside* `CustomerAuthProvider` in `App.tsx`'s routing and so is the only one of the two that can see both — watches `useCustomerAuth().status` and fires `POST /api/cart/merge` exactly once per genuine login/registration event within a mount (distinguished from a same-session token restore via a ref that only arms once this provider has actually observed an `'unauthenticated'` state — a token that resolves straight to `'authenticated'` on first load, i.e. session restore, never arms it, correctly skipping merge). The guest `localStorage` cart is cleared only in the merge's success handler; its failure handler resets the arming ref (so a later retry is possible), leaves `localStorage` untouched, and sets a `mergeError` surfaced via the new `error` field — authentication is never rolled back or otherwise touched by a merge failure. `clearCart()` is now `async`: for an authenticated customer it optimistically clears the displayed cart immediately (before any network call — payment is already confirmed by the time `CheckoutForm` calls this), then best-effort calls `DELETE /api/cart`, swallowing any failure internally so a network hiccup can never leave a confirmed-paid-for cart looking uncleared. `CheckoutPage.tsx` gained an `isLoading` guard (a small addition, not a rewrite) so an authenticated customer's still-in-flight `GET /api/cart` doesn't flash the "cart is empty" state before real data arrives.

**A second real bug was found and fixed via Playwright, not merely reasoned about**: the first implementation of `CheckoutForm.tsx`'s succeeded-payment branch called `await clearCart()` *before* `setStatus('succeeded')`/`onSucceeded()`. Because `clearCart()` optimistically empties `items` synchronously as its first action, this created a real (if narrow) window where `CheckoutPage`'s own `items.length === 0 && !hasSucceeded` guard would transiently be true — `hasSucceeded` not yet set — unmounting `CheckoutForm` (inside `<Elements>`) mid-execution and discarding its in-progress "succeeded" local state before `onSucceeded()` ever ran; a fresh `CheckoutForm` instance then remounted with `status: 'idle'`, rendering the plain form again with an empty (thus disabled) cart — exactly the symptom three Playwright specs (`checkout.spec.ts` D/E/H) caught. Fixed by reordering: `setStatus('succeeded')` and `onSucceeded()` now run *before* `await clearCart()`, so `hasSucceeded` is permanently true before the cart-clearing update can ever reach `CheckoutPage`'s guard.

**`checkout.spec.ts` (Block 8D) was updated, not left stale**: its `seedCart()` helper (writing directly to guest `localStorage`) had no effect once the authenticated customer it tests reads from `/api/cart` instead — replaced with a `mockCart()` helper providing simple in-memory `GET`/`DELETE /api/cart` state, registered *before* `loginAsCustomer()` (the authenticated cart query fires as soon as login resolves, so registering the mock any later risks a race against an unmocked real request). All 10 pre-existing Block 8D specs pass unchanged in behavior/intent after this swap. `customer-auth.spec.ts` additionally gained a `/api/cart` mock (previously absent, tolerated only by accident since an unmocked request's failure didn't happen to break those assertions) for the same determinism reasons its own header comment already commits to.

**9 new Playwright specs** in `e2e/authenticated-cart.spec.ts` (33 total, up from 24): authenticated cart loading hydrated from the API; add/update/remove all persisting through the API (not `localStorage`); login merging a guest cart into an existing authenticated one (`A×3` + guest `A×2, B×1` → `A×5, B×1`), with `localStorage` cleared only after; an empty guest cart making no merge call at all; a merge failure preserving the guest cart, surfacing an alert, and leaving the customer authenticated (this test needed one fix during development — asserting via `page.goto()` after login discards in-memory React state from a hard navigation, unlike a real customer's client-side navigation; fixed by clicking the "Cart" nav link instead); logout not calling `DELETE /api/cart` and the browser returning to an independent, empty guest cart; the authenticated cart surfacing an error rather than silently reading/writing `localStorage` when the API fails; per-store isolation for the authenticated cart; and checkout waiting for the authenticated cart to load. Full Playwright suite re-run: **33/33 passing**. `npm run build`/`npm run lint`: clean (same 4 pre-existing fast-refresh warnings as before, none new).

**Documentation updated to match, not left describing a "future decision"**: `system-architecture.md` §7 rewritten to describe the actually-implemented Redis key/payload/TTL/merge/logout behavior in place of the prior "intended, not yet built"/"left as a future implementation decision" wording; `database-design.md` needed no change (it already deferred all of this detail to `system-architecture.md`); `CLAUDE.md` needed no change (its Cart Architecture section was already correct — the code was what had deviated from it, not the documentation). The "Known Issues" entry recording the original Block 8B deviation is marked resolved below, not deleted, per this project's own "don't rewrite history" convention.

Committed 2026-09-06 as `b1bbcba` (`feat: implement redis-backed authenticated carts`), after a separate final scope audit confirmed the diff contained exactly the 20 files described above and nothing else.

**Phase 8 Block 8E — Order History, Order Detail, and Checkout Confirmation: complete and verified, 2026-09-06.** A read-only design review preceded implementation (separate turn, same day) and made one finding that shaped everything else: **the backend this block needs — `GET /api/customers/orders` and `GET /api/customers/orders/{order}` — already existed, complete and fully tested, since Phase 7.** `App\Http\Controllers\Customer\OrderController`, `App\Http\Resources\Customer\CustomerOrderResource`, the two routes, and `tests/Feature/Customer/CustomerOrderHistoryTest.php`'s 16 tests were all re-verified by reading the actual code (not assumed from the name) and confirmed to already cover ownership scoping, cross-customer/cross-store/cross-organization 404s, pagination, ordering, field selection, and non-leakage of internal fields. **Phase 8E is therefore a frontend-only block, plus one small, explicitly-scoped checkout-integration point — zero backend, schema, payment, or cart changes**, matching the design review's own recommendation and this task's explicit constraints.

**Frontend — Order History and Order Detail**: `services/customerOrdersApi.ts` (typed client for the two existing endpoints, deliberately distinct from `ordersApi.ts` — the merchant-facing shape, a different Resource with no `payment_status` field — mirroring the existing `customerAuth.ts` vs. merchant-auth naming split); `customer/orders/useCustomerOrders.ts` (`useCustomerOrdersList`/`useCustomerOrder`, React Query, query-key namespace `['customer-orders', storeId, ...]` — deliberately distinct from merchant orders' `['stores', storeId, 'orders', ...]` so the two identity domains' caches can never collide in one browser tab); `customer/orders/OrderHistoryPage.tsx` and `customer/orders/OrderDetailPage.tsx`, both directly mirroring the already-proven `merchant/orders/OrderListPage.tsx`/`OrderDetailPage.tsx` templates (loading/error/empty states, Prev/Next pagination bounded by `meta.last_page`) with the merchant-only status-transition button block removed — no status mutation exists anywhere on the customer side. Order History deliberately has **no status filter**, per Phase 7's own explicit design note that this was non-required by PRD §7.3, and per this block's own explicit scope. `{orderId}` in the Order Detail route is used only to know which order to request — `OrderDetailPage` never treats it as proof of ownership; every render branches entirely off what the authenticated API actually returns (including its 404s), the same discipline the backend itself already enforces.

**Frontend — Account page and routing**: `App.tsx` gained `orders` and `orders/:orderId` under the existing `/store/:storeId` element, both behind `CustomerProtectedRoute` (new imports aliased `CustomerOrderHistoryPage`/`CustomerOrderDetailPage` to avoid colliding with the merchant `OrderListPage`/`OrderDetailPage` already imported in the same file — the same aliasing convention already used for the catalog/merchant `ProductDetailPage` collision). `AccountPage.tsx` gained one link ("View your orders →") — the exact gap its own Block 8C comment had explicitly left for this block; nothing else on the page was touched.

**Frontend — Checkout confirmation integration**: `CheckoutForm.tsx`'s `'succeeded'` branch (the *only* branch touched — `'paymentPending'`/processing, Stripe confirmation, PaymentIntent handling, and checkout order creation are all byte-for-byte unchanged) now calls `navigate()` to `/store/{storeId}/orders/{result.data.id}` (with `{state: {justPlaced: true}}`) instead of rendering an inline "Payment successful" card — the order id is server-derived (returned by the `/api/checkout` call that already succeeded to reach this point), used only for routing, never as authorization; `OrderDetailPage` re-verifies ownership itself against the authenticated API regardless. `justPlaced` router state drives a one-time banner only ("Your order has been placed" — phrased to avoid asserting final payment success when `payment_status` isn't yet `succeeded`, since the Stripe webhook that actually flips it is asynchronous relative to this navigation); **the order data itself always comes from `GET /api/customers/orders/{order}`, never from router state or the checkout response**, satisfying the task's explicit "router state must never replace server authorization" constraint. The now-unreachable inline success JSX was removed (dead code once navigation happens synchronously in the same handler) rather than left in place; `status === 'succeeded'` is still tracked internally (guards against a stray resubmit).

**19 new Playwright specs** in `e2e/customer-orders.spec.ts`: opening Order History from the Account page; order summary fields (order number, status, payment status, total/currency) rendering, including a same-text ("Paid") collision between the two independently-derived status badges, asserted by count rather than an ambiguous single locator; empty history; opening an order and its items/shipping-address/status/payment-status all rendering; back-navigation to history; Prev/Next pagination against the real paginator response shape; unauthenticated access redirected to login on both routes; a loading state (needed one iteration — an initial 300ms artificial mock delay was too short for Playwright's own assertion-polling window to reliably observe it before it resolved; fixed by widening to 1000ms); and a failed list request / a 404'd single order both surfacing an alert rather than a blank page or a crash. **3 existing `checkout.spec.ts` specs updated** (D, E, H — every spec that reaches a confirmed-successful payment): each now asserts navigation to `/store/{storeId}/orders/{id}` instead of the removed inline `role="status"` message; **E was extended into the full confirmation-flow assertion the task specifies** — checkout success → navigation → a tracked, separately-mocked `GET /api/customers/orders/55` request actually firing (asserted via `expect.poll`, not inferred from the URL alone) → the order-placed banner → the order detail itself rendering from that separately-mocked response (order number, items, shipping address) → cart-cleared check preserved unchanged. `customer-auth.spec.ts`/`authenticated-cart.spec.ts`/`cart.spec.ts`/`catalog.spec.ts`/`smoke.spec.ts` needed no changes and were re-run to confirm zero regressions. Full Playwright suite: **43/43 passing** (up from 33). `npm run build`: clean. `npm run lint`: clean, same 4 pre-existing fast-refresh warnings, none new. Backend suite re-run to confirm the "no backend changes" claim empirically rather than merely asserted: **380/380 passing, unchanged from before this block**.

**Documentation**: `system-architecture.md` §10 corrected one genuinely stale detail this implementation surfaced by direct comparison — its API resource-groups list named a speculative `/api/me/orders` path that was never what got built; corrected to name the actual, already-shipped `GET /api/customers/orders[/{order}]`. No other wording in that document needed changing (§7 Cart Architecture, updated by the prior Block 8B Revision entry, is unaffected by this block). `CLAUDE.md`, `database-design.md`, and `README.md` were not touched, per this task's explicit instruction.

Committed 2026-09-06 as `ca77060` (`feat: add customer order history and detail`).

**Phase 8 Block 8F — Integration Pass: complete, verified, and committed**, 2026-09-06 (`6341be7`, `feat: complete storefront integration pass`). Approved scope: (1) correct an inaccurate backend test count recorded in the Block 8B Revision entry above (`tests/Feature/Customer/CartTest.php` — corrected back to 380 total, up from 359, matching the actually-run suite; the erroneous `401` figure is not carried forward), and (2) add cross-block integration E2E coverage proving the seams between the already individually-tested Phase 8 blocks (8A catalog, 8B/revision cart, 8C auth, 8D checkout, 8E order history/detail) connect correctly in one continuous browser session — `e2e/customer-journey.spec.ts`, two specs: a guest journey (catalog → add-to-cart → login → cart merge → checkout → order confirmation → order detail) and an already-authenticated journey (login → add-to-cart → checkout → order confirmation → order detail), both driven through the real UI rather than seeded fixtures, mocking only the backend API and Stripe.js at the network boundary — the same discipline every other Phase 8 spec already uses.

**A genuine `CartContext` login/merge race condition was found while writing this coverage, not anticipated in advance**: the authenticated `GET /api/cart` query and the login-time `POST /api/cart/merge` call fired concurrently with no ordering guarantee — if the `GET` resolved after the merge, it could silently overwrite the correctly-merged cart with stale pre-merge data. A narrowly scoped fix (approved before implementation, touching `CartContext.tsx` only) closes it: a render-derived `guestCartPendingMerge` gate suppresses the authenticated cart `GET` while a non-empty guest cart is still pending merge, lifting naturally on success (the guest cart empties) or via a new `mergeFailed` state on failure (so the authenticated cart still loads rather than staying blocked indefinitely). The public `useCart()` interface is unchanged; no checkout, API-contract, or backend code was touched.

**Verification**: targeted cart/auth specs 18/18; the two new customer-journey specs 2/2; re-run with `--repeat-each=15` to stress-test the race-condition fix, 0 failures; full Playwright suite 45/45 (up from 43); frontend build clean; frontend lint clean (same 4 pre-existing `react(only-export-components)` fast-refresh warnings, none new); backend unaffected (no backend files in this commit) — 380/380 passing, 1198 assertions. `git show --stat 6341be7` confirms exactly three files changed: this document's test-count correction, the new spec file, and `CartContext.tsx`. **This closes Phase 8 — Frontend Storefront in full** (Blocks 8-0, 8A, 8C, 8B as revised, 8D, 8E, 8F all complete and verified).

**Phase 9A — Organization Lifecycle Management: complete and verified**, 2026-09-07 (`a3a0ccf`, `feat: implement organization lifecycle management`). The first Commerce Completeness increment (see "Development Strategy — Commerce-Complete Before AI" above) — closes the previously-open "Platform Admin organization approve/reject/suspend lifecycle (structurally designed, never built)" gap. Implements the four Platform-Admin-triggered edges on `organizations.status`: **approve** (`pending → active`), **reject** (`pending → rejected`, requires a reason), **suspend** (`active → suspended`, requires a reason), and **reactivate** (`suspended → active`) — matching Database Design 2.1's pre-existing schema (`approved_at`/`approved_by_platform_admin_id`, `rejected_at`/`rejected_by_platform_admin_id`, `suspended_at`/`suspended_by_platform_admin_id`, `status_reason`) exactly, with no migration needed. `App\Support\OrganizationLifecycleTransitions` is a pure (from, to) whitelist mirroring `MerchantOrderStatusTransitions`' shape — explicitly excludes `rejected → active` (no re-application workflow exists) and every other unlisted pair, verified by a full 4×4 sweep over every `OrganizationStatus` combination. `App\Services\OrganizationLifecycleService` mirrors `OrderStatusUpdateService`'s proven shape (`DB::transaction()` + `SELECT ... FOR UPDATE` on the `organizations` row + re-validate against the whitelist + apply audit columns + save); because **approve** and **reactivate** both target `active` from two different starting states (`pending` and `suspended` respectively), each public method additionally pins its own expected starting status so calling `approve()` on a suspended org (or `reactivate()` on a pending one) is rejected even though the bare whitelist alone would allow the corresponding edge. `reactivate()` deliberately reuses the existing `approved_at`/`approved_by_platform_admin_id` pair rather than a new column, and leaves `suspended_at`/`suspended_by_platform_admin_id`/`status_reason` untouched, preserving the prior suspension as history — consistent with this schema's existing single-most-recent-event-per-column design (no `audit_logs` table). `App\Http\Controllers\Platform\OrganizationController` (`index`/`show`/`approve`/`reject`/`suspend`/`reactivate`) resolves organizations globally, with no tenant-context middleware — Platform Admin sits above the Organization/Store hierarchy per `CLAUDE.md`'s "structurally separate identity domain" rule — and has **no `OrganizationPolicy`**: per the approved design, `PlatformAdmin` has no role column/tiered capability system for MVP, so `auth:platform_admin` guard membership alone is the complete authorization check. Routes: `GET /api/platform/organizations`, `GET /api/platform/organizations/{organization}`, `POST .../approve`, `POST .../reject`, `POST .../suspend`, `POST .../reactivate` — all behind the existing `auth:platform_admin` group. `OrganizationRejectRequest`/`OrganizationSuspendRequest` each require a non-empty `reason` string; `approve`/`reactivate` take no body. `OrganizationResource` exposes status, the four audit-column pairs, and `status_reason`. Frontend: `/admin/organizations` (list, filterable by status) and `/admin/organizations/:organizationId` (detail, with approve/reject/suspend/reactivate actions gated on the org's current status), wired into the Platform Admin back-office shell — the "Organizations" nav item moves out of `COMING_SOON_ITEMS` for the first time since Phase 4 Block 1. New tests: `OrganizationLifecycleTransitionsTest` (whitelist sweep — the four approved edges plus eight explicitly-named rejected transitions, including `rejected → active`), `OrganizationLifecycleServiceTest` (service-level transition/locking/audit-column behavior, 10 test methods), `OrganizationLifecycleTest` (HTTP-level feature coverage — all four actions, invalid-transition 422s, reason validation, unauthenticated/cross-guard rejection, 19 test methods). **No new migration, no `OrganizationPolicy`, no changes to Merchant/Customer auth, Store/Product/Inventory/Order management, or any Phase 5–8 checkout/cart/storefront code.**

**Phase 9B — Category Management: complete and verified**, 2026-09-07 (`c6727d3`, `feat: implement category management`). The second Commerce Completeness increment, immediately following Phase 9A. Full merchant CRUD on the pre-existing `categories` table (in schema since Phase 2C/Database Design 2.3, previously unused by any management endpoint — only ever populated via `CategoryFactory` as catalog test-fixture infrastructure). `App\Http\Controllers\Merchant\CategoryController` (`index`/`store`/`show`/`update`/`destroy`) reuses `tenant.merchant.store` verbatim — no new middleware, Category is directly store-scoped like Product; `{category}` is resolved and store-scoped at the controller level (`resolveCategory()`), the same discipline as `{product}`/`{variant}`/`{order}`, never implicitly route-bound. `App\Policies\CategoryPolicy` follows `ProductPolicy`'s exact discipline: `viewAny`/`view` open to any role with store access, `create`/`update`/`delete` gated on `OrganizationRole::atLeast(StoreAdmin)` + store access + `organization.status === active` — Staff gets read-only access, found and authorized by Laravel's naming-convention auto-discovery (`Category` → `CategoryPolicy`, no `AppServiceProvider` registration needed, unlike `InventoryPolicy`'s Block 4B exception). Category creation auto-generates a unique-per-store slug (`uniqueSlug()`, suffix-on-collision, mirroring `products.slug`'s mutate-on-delete/uniqueness convention). **Active-product deletion protection**: `CategoryController::destroy()` rejects (422, via the new `CategoryHasActiveDependentsException`) deleting a category still referenced by any non-soft-deleted `Product` — `products.category_id`'s `nullOnDelete()` FK action never fires on a soft-delete (an `UPDATE`, not a `DELETE`), so this application-layer guard is what actually prevents orphaning a still-referenced category; a category whose only referencing products are themselves soft-deleted is not blocked. This closes part of the scope the Block 3 store-deletion guard had explicitly left open ("Categories and Customers are deliberately excluded from this guard... revisit once Category management... exist[s]" — see "Store-deletion dependent-resource guard" above); the store-deletion guard itself was **not** modified in this block. **Product category selector integration**: `ProductCreatePage.tsx` and `ProductDetailPage.tsx` (create and edit forms) both gained a `category_id` `<select>` populated from `useCategoriesList(storeId)`, with an explicit "No category" (`null`) option; `ProductDetailPage`'s read view displays the resolved category name (`—` when unset). Tenant isolation: a category id belonging to a different store (including a different store in the same organization) is unreachable and never leaks via 404 response bodies, matching `ProductTenantIsolationTest`'s established pattern. New tests: `CategoryPolicyTest` (RBAC matrix, 8 test methods), `CategoryManagementTest` (CRUD, slug generation/collision, active-product-deletion-blocked/archived-product-allowed, validation, 19 test methods), `CategoryTenantIsolationTest` (cross-org/cross-store isolation, spoofed ids ignored, 5 test methods). **No new migration** (schema already existed since Phase 2C)**, no changes to the Block 3 store-deletion guard, no changes to Platform Admin, Checkout/Payment/Inventory, or any Phase 5–8 code.**

**Test counts (Phase 9A/9B, by static inspection of the committed test files — not re-run as part of this documentation-only reconciliation pass)**: 32 new test methods for Phase 9A (3 in `OrganizationLifecycleTransitionsTest` — one of which is a full 4×4 combinatorial sweep over every `OrganizationStatus` pair, the other two driven by a 4-case and an 8-case `#[DataProvider]` — 10 in `OrganizationLifecycleServiceTest`, 19 in `OrganizationLifecycleTest`), and 32 new test methods for Phase 9B (19 in `CategoryManagementTest`, 5 in `CategoryTenantIsolationTest`, 8 in `CategoryPolicyTest`). The prior confirmed backend total was 380/380 (Phase 8 Block 8F, `6341be7`); the new grand total was not independently re-run in this pass and should be confirmed by `php artisan test` before being cited as authoritative.

**Phase 9C — Customer Management: designed, reviewed, approved, implemented, fully verified, and committed**, 2026-09-08 (`608f397`, `feat: implement customer management`). The third Commerce Completeness increment, following a full Discovery → Design → Review → Approval → Implementation → Verification → Commit cycle (a Design Report was produced and approved before any code was written, matching this project's established incremental-by-functional-area approach). Closes the "merchant-facing customer management (`PRD.md` §10 — no route/UI exists)" gap previously tracked under Development Strategy Scope B. **Deliberately read-only** (approved scope explicitly excludes Customer create/update/delete, `CustomerAddress`, CRM/notes/tags/segmentation/merge/export, and marketing automation — none of that was implemented). `App\Http\Controllers\Merchant\CustomerController` (`index`/`show` only) reuses `tenant.merchant.store` verbatim — no new middleware, Customer is directly store-scoped (`customers.store_id`/`organization_id` are columns on the row itself, confirmed from the schema rather than assumed); `{customer}` is resolved and store-scoped at the controller level (`resolveCustomer()`), the same discipline as `{product}`/`{order}`/`{category}`, never implicitly route-bound. `App\Policies\CustomerPolicy` has only `viewAny`/`view` (no `create`/`update`/`delete` methods exist at all), delegating to `TenantAccess::roleFor()`/`canAccessStore()` exactly like `OrderPolicy`'s read-only precedent — Owner, Store Admin, and Staff can all read; no new authorization mechanism was introduced. `App\Http\Resources\CustomerResource` exposes exactly `id`/`store_id`/`name`/`email`/`phone`/`created_at`/`order_count`/`total_spent` — `password` (already `#[Hidden]` on the model), `stripe_customer_id`, `email_verified_at`, `organization_id`, `deleted_at`, `addresses`, `payment_methods`, `orders`, and `refunds` are all confirmed absent. Routes: `GET /api/stores/{store}/customers`, `GET /api/stores/{store}/customers/{customer}`, nested inside the existing `tenant.merchant.store` group. **List**: `search` (inline-validated, matching `OrderController::index()`'s own single-field-query-param precedent — no new FormRequest) matches `name` OR `email` via `LIKE` (not `phone`); `paginate(15)`; default `orderBy('created_at', 'desc')`. **Aggregates, both database-side, no PHP loop, verified via direct SQL inspection to be exactly 2 queries (a `COUNT` + one `SELECT`) regardless of row count**: `order_count` is `withCount('orders')`, unscoped — counts **all** of a customer's orders regardless of status (approved semantics: activity, not revenue); `total_spent` is `withSum(['orders as total_spent' => fn ($q) => $q->whereNotIn('status', ['pending', 'cancelled'])], 'total')` — sums `orders.total` for every other status (including `refunded`, per the approved literal reading of "excludes only pending/cancelled" — currently inert, since no flow reaches `refunded` yet), formatted to a two-decimal string (`"0.00"`, `"125.50"`) in the Resource since a raw aggregate isn't routed through `Order`'s own `decimal:2` cast. **No `refunds` table is read, joined, or subtracted anywhere — refund-aware accounting was explicitly out of scope and was not implemented.** `database/factories/OrderFactory.php` gained `forCustomer(Customer $customer)` (test-infrastructure only, needed because the pre-existing `forStore()` always creates a fresh `Customer` and the aggregate tests need several orders against the *same* customer) plus a small, behavior-preserving `applyOrderFields()` extraction that `forStore()` now also uses. Frontend: `services/customersApi.ts` (`listCustomers`/`getCustomer` only — no mutation functions exist), `merchant/customers/useCustomers.ts` (`useCustomersList`/`useCustomer`, React Query, key `['stores', storeId, 'customers', ...]`), `merchant/customers/CustomerListPage.tsx` (search input with a local 300ms debounce — no new dependency — loading/error/empty states, Prev/Next pagination) and `CustomerDetailPage.tsx` (read-only — no form, no edit/delete UI), both mirroring `OrderListPage.tsx`/`OrderDetailPage.tsx`'s established structure; `App.tsx` gained `:storeId/customers` and `:storeId/customers/:customerId`; `StoreDetailPage.tsx` gained a fourth "Customers →" nav tile (`sm:grid-cols-3` → `sm:grid-cols-4`). **No migration** (schema already existed since Phase 5's Customer Authentication prerequisite)**, no dependency added or changed, no changes to Phase 9A/9B code, Checkout/Payment/Inventory, or any other Phase 5–8 code.**

**Verification**: **32 new test methods, 90 assertions, all passing** — `CustomerPolicyTest` (8), `CustomerManagementTest` (10), `CustomerTenantIsolationTest` (6 — cross-org 404, cross-store-same-org 404, unassigned-store 403, spoofed `store_id`/`organization_id` query params ignored, platform-admin-token 401, customer-guard-token 401), `CustomerAggregatesTest` (8 — `order_count` counts all statuses, `total_spent` excludes pending, excludes cancelled, qualifying totals summed correctly across statuses, zero-orders customer returns `0`/`"0.00"`, pending-and-cancelled-only customer returns a positive count but `"0.00"`, per-row list correctness, and a query-count regression proof that the list endpoint's query count does not scale with the number of customers). **A corrected figure, recorded here to supersede an earlier inaccurate report**: this test count was first mis-reported as "42 passed / 108 assertions" — that filter incidentally also matched a pre-existing, unrelated file (`tests/Feature/Customer/CustomerTenantIsolationTest.php`, Phase 5's customer-authentication tenant-isolation tests) sharing a class basename with the new `tests/Feature/Merchant/CustomerTenantIsolationTest.php`; **32 passed / 90 assertions is the correct, independently re-verified figure for Phase 9C's four test files alone.** Full backend suite re-run twice independently: **486 passed, 1493 assertions**, zero regressions (one `CatalogProductTest` failure was observed on a single run — confirmed non-reproducible and unrelated to Phase 9C: 22/22 passing in isolation, 486/486 on a repeat full run — a pre-existing flaky assertion against literal substring `"42"` in unrelated Faker-generated response content, not a regression). `./vendor/bin/pint --test`: clean, 230 files. Frontend `tsc -b`: 0 errors. `npm run lint` (oxlint): 0 errors, same 4 pre-existing `react(only-export-components)` warnings, 0 new. `npm run build`: succeeds. A scope-leak grep across every new/modified file for refunds/notifications/analytics/reports/discounts/tax/password-reset/rate-limiting/segmentation/CRM/export/merge/tags/notes/audit-log/`CustomerAddress` found only docblock comments documenting these as explicitly out of scope and test assertions proving their absence — no accidental implementation of any of them. `git status --short`/`git diff --stat` confirmed exactly the 15 expected files changed before commit.

**Phase 9D — Refunds: designed, reviewed, approved, implemented, fully verified, and committed**, 2026-09-08 (`0b0e59e`, `feat: implement refunds`). The fourth Commerce Completeness increment, following a full Discovery → Design → Review → Approval → Implementation → Verification → Commit cycle across four separate design/clarification passes before any code was written — closing the "refund initiation flow (schema-only today)" gap tracked under Development Strategy Scope B since Phase 2E. **Full refunds only** (approved scope; partial refunds explicitly deferred) — `App\Http\Requests\Merchant\RefundCreateRequest` accepts only `idempotency_key` (required) and `reason` (nullable); the refund amount is always derived server-side from the succeeded `Payment.amount`, never client-supplied. `App\Services\RefundService::refund()` mirrors `PaymentRetryService`'s proven shape exactly: locks the `orders` row (`lockForUpdate()`), re-verifies eligibility against the locked read (`Order.status` ∈ {paid,processing,shipped,completed}, a `Succeeded` `Payment` exists, no already-active `Refund` for that payment), derives a deterministic Stripe idempotency key (`hash('sha256', "refund:{$payment->id}:{$idempotencyKey}")`), and — the same deliberate, already-approved exception to this project's general "never hold a lock across a network call" rule that `PaymentRetryService` established — holds that lock across the Stripe API call itself, so a second concurrent request can never reach Stripe while one is already active. `App\Services\StripeRefundGateway` (interface) / `StripeApiRefundGateway` (implementation) mirror `StripePaymentIntentGateway`'s exact gateway-seam pattern; refunds are created against **PaymentIntent**, never Charge (`payments.stripe_charge_id` was confirmed dormant/unpopulated everywhere in this codebase during design). The local `Refund` row is always inserted `status = pending`, regardless of Stripe's synchronous response — **`StripePaymentWebhookService` is the sole authority for the succeeded/failed transition**, extended (not replaced) to also own `refund.created`/`refund.updated` (added to a new `SUPPORTED_REFUND_EVENT_TYPES` constant, dispatched from the same single `process()` entry point and the same outer `DB::transaction()` as the pre-existing `payment_intent.*` handling — `recordEvent()`'s `stripe_webhook_events` insert, the `Refund` status transition, the `Order → refunded` transition, and inventory restoration all share one transaction, so any failure anywhere in that sequence rolls back everything, including the webhook dedup row). **`charge.refunded` is deliberately not used** — verified against Stripe's own official API documentation during design (not assumed): that event's `Charge` object requires parsing a nested, expandable `refunds` list, whereas `refund.created`/`refund.updated` deliver a `Stripe\Refund` object directly, with `payment_intent`/`id`/`status` immediately available — the Payment lookup uses `$stripeRefund->payment_intent`. Stripe→local status mapping is an explicit `match` (`pending`/`requires_action` → `RefundStatus::Pending`, `succeeded` → `Succeeded`, `failed`/`canceled` → `Failed`, unrecognized → `null`, logged and left unchanged) — **never `RefundStatus::from()`**, which would throw an uncaught `ValueError` for `requires_action`/`canceled`. A terminal-state guard (`Succeeded`/`Failed`) makes a redelivered or out-of-order event a safe no-op regardless of Stripe event id. Only a transition into `Succeeded` triggers `Order → refunded` (from one of the four eligible statuses, defensively logged-and-skipped otherwise — never force-overwritten) and **synchronous, inline inventory restoration** via the existing `InventoryAdjustmentService` (reason `Refund`, mirroring `releaseInventoryForPayment()`'s exact shape) — deliberately not queued, since Jobs/Queue infrastructure remains unintroduced by design (§12 of the approved design explicitly recommended this deviation from `database-design.md`'s literal "queue the... jobs" phrasing). A dashboard-initiated refund (no prior local `Refund` row) is found-or-created directly from the webhook, with `initiated_by_user_id = null`. `App\Policies\RefundPolicy` follows `OrderPolicy`'s discipline: `create` gated on `OrganizationRole::atLeast(StoreAdmin)` (Owner and Store Admin only, Staff denied) + store access + active organization; `view` open to all three roles. **No `refunds.idempotency_key` migration** — an explicit, approved design decision: the existing `unique(stripe_refund_id)` constraint plus Stripe's own idempotency-key cache is sufficient, matching `PaymentRetryService`'s own established precedent of never short-circuiting locally before reaching Stripe. `App\Http\Resources\RefundResource` exposes only `id`/`order_id`/`payment_id`/`amount`/`reason`/`status`/`created_at` — no `stripe_refund_id`, no `initiated_by_user_id`. `OrderResource` gained a `refunds` field via `whenLoaded()`, mirroring the existing `items`/`shipping_address` convention exactly. Frontend: `services/refundsApi.ts` (`createRefund()` only), a `useCreateRefund` hook added to the existing `merchant/orders/useOrders.ts`, and a refund action/confirmation form/history block added directly to the existing `OrderDetailPage.tsx` — visible only to Owner/Store Admin on a refundable order status, explaining the action is a full refund with an optional reason field, loading/error states, and automatic refetch via React Query invalidation on success; **no new route or page**. **No migration, no new dependency, no Jobs/Queue, no Notifications, no Analytics, no Discounts/Tax, no Customer CRUD/`CustomerAddress`, no Product Variants, no AI/Reports, no CI/CD, no rate limiting, no password reset** — all confirmed absent via targeted scope-leak greps during both the implementation and independent verification passes.

**Verification**: **focused Phase 9D tests: 45 passed, 140 assertions** (8 `RefundPolicyTest`, 19 `RefundManagementTest`, 5 `RefundTenantIsolationTest`, 13 `StripeRefundWebhookTest` — including a forced-mid-loop-failure test proving the entire webhook transaction, not just the failing inventory claim, rolls back: prior successful claims, the `Refund`/`Order` status changes, and the `stripe_webhook_events` dedup row all revert together). **Full backend suite: 531 passed, 1633 assertions, 0 failures** — independently re-run twice during final verification for stability, both clean (486 pre-Phase-9D baseline + 45 new). `./vendor/bin/pint --test`: clean, 244 files. Frontend `tsc -b`: 0 errors. `npm run lint` (oxlint): 0 errors, same 4 pre-existing `react(only-export-components)` warnings, 0 new. `npm run build`: succeeds. An independent final-verification pass re-read every changed file fresh (not from the implementation report) and re-ran every check, confirming all 13 design-conformance points (full-refund-only, no `idempotency_key` column, PaymentIntent-based, `refund.created`/`refund.updated` only, webhook-authoritative, synchronous inline restoration, single outer transaction, no swallowed exceptions, terminal guard, Owner/Store-Admin-only creation, no Jobs, no partial refunds, no Phase 9E scope) — PASS on every point. `git status --short`/`git diff --cached --name-only` confirmed exactly the 23 expected files staged and committed.

**Phase 9E-1 — G3-A (Detect & Alert): designed, reviewed, approved, implemented, verified, signed off, and committed**, 2026-09-09 (`df6ce9a`, `feat: add late payment closure alert`). Resolves the G3 open item as tracked in this document's own "Open Decisions" section below (that entry had gone stale — `database-design.md`'s own authoritative "Open Decisions" section had already recorded the *design* as CLOSED in Database Design 2.6, since inventory is claimed atomically at checkout rather than at payment success; what remained open was a genuine, narrower implementation gap against `database-design.md` §14's explicit promise that a late `payment_intent.succeeded` webhook landing on an already-non-`Pending` Order would be "logged for manual reconciliation" with an "alarm-worthy" `status_reason` — confirmed via code inspection to be unimplemented before this phase). Followed a full Discovery (read-only G3 Policy Analysis) → Design (G3-A/G3-B design proposal) → Approval (G3-A only) → Implementation → Verification → Sign-off → Commit cycle, each step performed read-only until the prior step was explicitly approved. **Scope: detection/alerting only, no compensating action** — when a `payment_intent.succeeded` webhook resolves for a Payment whose Order has already left `Pending` (via merchant cancellation or the Phase 6 expiry-sweep's own documented, accepted residual race), `Payment.status` still transitions to `Succeeded` (Stripe remains the sole source of truth for Payment status) and `Order.status` is **never** changed or reopened, but `Order.status_reason` is set to the new literal value `payment_succeeded_after_closure` and exactly one `Log::critical()` is emitted with `order_id`/`payment_id`/`store_id`/`organization_id`/`order_status`/`previous_status_reason` (no payment/card data). Implemented as a single new private method, `StripePaymentWebhookService::recordPaymentSucceededAfterClosure()`, called from the one existing early-return branch inside `transitionOrderToPaid()` — no other line in that method, or anywhere else in the file, was touched. **No new inventory mutation, no Stripe API call, no refund, and no migration** — `orders.status_reason` is the same pre-existing nullable `varchar(255)` column already carrying `expired`/`item_no_longer_available`/arbitrary merchant-supplied reasons (confirmed via `php artisan db:table orders` and the pre-existing `2026_08_27_110100_create_orders_table.php` migration). Idempotency is entirely structural, not a new mechanism: exact Stripe event redelivery is blocked by the pre-existing `stripe_webhook_events.stripe_event_id` unique constraint, and a *different* event id for an already-`Succeeded` Payment is blocked by the pre-existing `handleSucceeded()` terminal-status guard — both left completely unmodified — with a third, redundant `status_reason`-equality check inside the new method itself purely as defense-in-depth. **G3-B (a narrowly-scoped follow-up letting Owner/Store Admin manually refund an order carrying this specific alarm reason) was designed at the proposal stage only and is explicitly NOT implemented** — no change to `RefundService`, `RefundPolicy`, `StripeRefundGateway`, `StripeApiRefundGateway`, `RefundController`, or the frontend `OrderDetailPage` refund UI. No frontend, migration, dependency, Jobs, Notifications, or Analytics changes of any kind.

**Verification**: **focused: 31 passed, 132 assertions** (25 pre-existing `StripeWebhookTest` cases + 6 new G3-A tests — alert-on-merchant-cancellation, alert-on-expiry-sweep-style-cancellation with the prior `status_reason` overwritten, exact-event-id redelivery producing no duplicate alert, a different event id against an already-`Succeeded` Payment also producing no duplicate alert, the normal `Pending → Paid` path proven unaffected, and a dedicated inventory-non-mutation regression proof). **Full backend suite: 537 passed, 1662 assertions, 0 failures** — independently re-run twice (once during implementation, once during a separate read-only verification pass), both clean (531 pre-Phase-9E-1 baseline + 6 new). `./vendor/bin/pint --test`: clean, 244 files. A dedicated read-only verification pass re-inspected the actual `git diff` fresh (not from the implementation report), confirmed the diff touches only three points in one file (a new constant, a two-line call-site addition to the existing non-`Pending` branch, and one new method) with zero other lines changed, confirmed `git diff --stat` was empty for every out-of-scope file (`RefundService`, `RefundPolicy`, `StripeRefundGateway`, `StripeApiRefundGateway`, `InventoryAdjustmentService`, `RefundController`, all of `frontend/`, `backend/database/migrations/`, both `composer.json`/`composer.lock` and `package.json`/`package-lock.json`), and confirmed `app/Jobs` still has no tracked files. `git status --short`/`git show --name-only --format= HEAD` confirmed exactly the 2 expected files (`StripePaymentWebhookService.php`, `StripeWebhookTest.php`) staged and committed as `df6ce9a`, with this document itself deliberately excluded from that commit per instruction (reconciled separately, in this pass). One minor, non-blocking observation from the verification pass: the third-layer `status_reason`-equality defense-in-depth guard is not independently exercised by any test, since the two structural guards above it (webhook-event dedup, Payment terminal-status) already prevent every constructed scenario from reaching it twice — consistent with its own docblock describing it as intentionally redundant, not a coverage gap in the approved behavior itself.

**G3 family status, for orientation (all four sub-items now resolved, distinguished explicitly since each has a different scope and mechanism):**
- **Original G3 race** (payment succeeding after inventory was already depleted at checkout time) — **CLOSED by Database Design 2.6's architecture itself**, not by any 9E-phase: inventory is claimed atomically, under lock, in the same transaction as pending-order creation, before Stripe can ever resolve — the race is structurally impossible, not merely detected. See "Open Decisions" below for the full closure record.
- **G3-A, merchant-cancellation case** — handled by **Phase 9E-1**, **COMPLETE**. A late `payment_intent.succeeded` webhook landing on a merchant-cancelled (still-non-terminal-Payment) Order sets `Order.status_reason = payment_succeeded_after_closure` and logs `Log::critical()`; `Payment.status` **does** transition to `Succeeded` in this case (Stripe is the source of truth, and the Payment was never marked terminal beforehand).
- **G3-B, manual compensation** — handled by **Phase 9E-2**, **COMPLETE**. Lets Owner/Store Admin manually refund the exact G3-A merchant-cancellation case (`Order.status = Cancelled` + `status_reason = payment_succeeded_after_closure` + a `Succeeded` Payment) through the existing, approved refund flow — never a general "Cancelled orders are refundable" rule. Includes inventory double-credit protection in the shared refund-restoration path.
- **G3-A extension, expiry-sweep late-success case** — handled by **Phase 9E-3**, **COMPLETE**. A late `payment_intent.succeeded` webhook landing on an expiry-sweep-cancelled Payment (`Payment.status = Canceled`, `failure_reason = expired`) sets `Order.status_reason = payment_succeeded_after_expiry_cancellation` and logs `Log::critical()` — but, unlike the G3-A merchant-cancellation case, **`Payment.status` deliberately stays `Canceled`** (the terminal-status invariant is preserved by design; see the Phase 9E-3 entry below for the full rationale). Because the status_reason and Payment state differ from the G3-A case, this Order is **not** automatically eligible for the G3-B refund flow — `RefundService::isRefundableOrderState()` only recognizes `payment_succeeded_after_closure`, and no Succeeded Payment exists for this case regardless. No manual compensation path exists yet for this specific scenario — see "Open Decisions" below for the explicitly-unapproved follow-up item this leaves open.

**Phase 9E-2 — G3-B (Manual Compensation): designed, reviewed, approved, implemented, verified, signed off, and committed**, 2026-09-09 (`84a73c3`, `feat: complete G3 refund and late payment handling` — committed jointly with Phase 9E-3, see below, as one combined code commit following two separate, independently-approved design/implementation cycles). Closes the G3-B item this document's own "Open Decisions" section had tracked as open since the Phase 9E-1 reconciliation. Followed the same full Discovery → Design → Approval → Implementation → Verification → Sign-off → Commit cycle already established for 9E-1. **Scope: a narrowly-scoped manual refund path for the exact G3-A merchant-cancellation alarm — explicitly never a general rule permitting refunds on arbitrary `Cancelled` orders.** `RefundService` gained a new private helper, `isRefundableOrderState()`, replacing the bare `REFUNDABLE_ORDER_STATUSES` membership check at its single call site: an order is eligible for refund exactly when its status is in the pre-existing `{Paid, Processing, Shipped, Completed}` set (unchanged, `Completed` included), **or** its status is `Cancelled` **and** its `status_reason` is exactly `payment_succeeded_after_closure` (the G3-A alarm value) — the existing, separate "a Succeeded Payment exists" check still applies unconditionally afterward, regardless of which branch matched. All existing refund row-locking, active-refund protection, Stripe idempotency-key derivation, and concurrency discipline were reused completely unmodified. **Inventory double-credit protection**: `StripePaymentWebhookService::restoreInventoryForRefund()` gained a per-claim guard — before re-crediting a `Checkout` claim, it now checks whether a `Release`-reason row already exists for the same `(order_item_id, payment_id)` pair (relying on `inventory_transactions.dedup_key`'s existing uniqueness guarantee for that exact pair) and skips restoration if so, logging `Log::info()` rather than mutating inventory a second time; for every pre-existing Phase 9D refund this check is provably always false (a still-refundable order can never have a prior release for its own succeeded payment), so this is a pure safety addition with zero behavior change to any previously-tested refund. On successful resolution, `transitionOrderToRefunded()` gained a matching carve-out: when the order is `Cancelled` with the G3-A alarm `status_reason`, it now sets `status_reason = payment_refunded_after_closure` (a new, distinct "resolved" marker) instead of `Order.status = Refunded` — the order correctly stays `Cancelled`, since nothing was ever fulfilled to "un-refund." Frontend: `ordersApi.ts` gained a shared `PAYMENT_SUCCEEDED_AFTER_CLOSURE_REASON` constant; `OrderDetailPage.tsx`'s `canRefund` gained the matching `isG3bEligible` OR-clause plus a distinct confirmation message for this specific scenario — the backend remains fully authoritative regardless of what the frontend computes. **No change to `RefundPolicy`, `RefundController`, `RefundCreateRequest`, `RefundResource`, `StripeRefundGateway`, `StripeApiRefundGateway`, any migration, or any dependency.**

**Verification**: **focused (`Refund*` + `StripeRefundWebhookTest`): 59 passed, 197 assertions** at implementation time (7 new eligibility tests in `RefundManagementTest`, 7 new inventory/status_reason/end-to-end tests in `StripeRefundWebhookTest`, plus the full pre-existing Refund/webhook suites re-run). **Full backend suite (at the time): 551 passed, 1719 assertions, 0 failures** (537 pre-Phase-9E-2 baseline + 14 new). `./vendor/bin/pint --test`: clean. Frontend `tsc -b`: 0 errors; `npm run lint`: 0 errors, same 4 pre-existing warnings, 0 new; `npm run build`: succeeds. A dedicated read-only verification pass re-confirmed the diff was scoped to exactly the reported files, that `RefundPolicy`/`RefundController`/`StripeRefundGateway`/migrations/dependencies were untouched, and that G3-B does not expand eligibility beyond the single named carve-out.

**Phase 9E-3 — G3-A Extension (Expiry-Sweep Late Payment Detection): designed, reviewed, approved, implemented, verified, signed off, and committed**, 2026-09-09 (`84a73c3`, `feat: complete G3 refund and late payment handling` — the same commit as Phase 9E-2 above). Closes a distinct gap discovered *while building* Phase 9E-2's end-to-end test: `PaymentExpirySweepService` (Phase 6) marks `Payment.status = Canceled` (with `failure_reason = 'expired'`) in the **same** transaction that releases inventory and cancels the Order — so a genuinely later `payment_intent.succeeded` webhook (the exact residual race `database-design.md` §14 names explicitly) was being silently absorbed by `handleSucceeded()`'s pre-existing Payment terminal-status guard, before G3-A's own alarm logic could ever run. **Phase 9E-1's G3-A alarm never actually covered this case** — only the merchant-cancellation case, where Payment is left non-terminal. Followed its own full Discovery (read-only, confirmed the gap and analyzed remediation options) → Design (evaluated and explicitly rejected transitioning `Payment.status` back to `Succeeded`) → Approval → Implementation → Verification → Sign-off → Commit cycle. **Scope: detection/alerting only, and — critically — the terminal Payment-status invariant is deliberately preserved, not corrected.** `StripePaymentWebhookService::handleSucceeded()` gained a new, narrow condition checked *before* the pre-existing blanket terminal-status guard: `Payment.status === Canceled AND Payment.failure_reason === 'expired'` — `'expired'` being `PaymentExpirySweepService`'s own hardcoded literal, the only place in the codebase that ever writes it, making this pairing an unambiguous signal for "canceled by our own expiry sweep" as opposed to any genuinely Stripe-reported cancellation. When detected: **`Payment.status` remains `Canceled`** (it is never transitioned to `Succeeded` — Stripe may have succeeded on its own side, but the local record is deliberately left showing `Canceled`, preserving the same terminal-status invariant every other Payment/Refund transition in this codebase relies on); **`Order.status` remains `Cancelled`** (never reopened); **only `Order.status_reason` changes**, to the new, distinct literal `payment_succeeded_after_expiry_cancellation` (deliberately *not* `payment_succeeded_after_closure` — that value's established meaning implies a locally-`Succeeded` Payment and is what makes an order G3-B-eligible, neither of which is true here); `paid_at`/`cancelled_at` are untouched; and exactly one `Log::critical()` is emitted with `order_id`/`payment_id`/`store_id`/`organization_id`/`order_status`/`payment_status`/`previous_status_reason`/`payment_expired: true` (no card/payment data). **No inventory mutation, no Refund row, no Stripe API call of any kind, no Order reopening.** The resulting behavior is exactly: *Stripe may have succeeded → the local Payment record remains `Canceled` → the system records an alarm-worthy `status_reason` → a critical log is emitted → manual reconciliation is required* — the local/Stripe divergence is surfaced, never auto-corrected. Because this new `status_reason` is distinct from G3-A's own value and no Succeeded Payment exists for this case, **this scenario is deliberately not eligible for the existing G3-B manual-refund flow** — `RefundService::isRefundableOrderState()` was not touched by this phase and does not recognize the new literal. Implemented as two new private methods, `isExpirySweepCanceledPayment()` (the detection predicate) and `recordLatePaymentSucceededAfterExpirySweep()` (the alarm, with its own `status_reason`-equality idempotency guard — this branch, unlike G3-A's own, never transitions `Payment.status`, so it does not inherit a second "for free" idempotency layer from a Payment-status change and needed this guard as its sole repeat-suppression mechanism). All pre-existing webhook-event persistence, Stripe event deduplication, Payment row locking, and the existing outer transaction boundary were reused completely unmodified — no new transaction was introduced. **No migration, no dependency, no frontend change, no change to `PaymentExpirySweepService` itself, and no automatic refund, re-fulfillment, or compensation of any kind for this case** — a future compensation increment for this specific scenario is explicitly unapproved and unscoped; see "Open Decisions" below.

**Verification**: **focused (`StripeWebhookTest` + `StripeRefundWebhookTest`): 57 passed, 270 assertions** (6 new unit-style tests in `StripeWebhookTest` — core detection, ordinary-non-expiry-Canceled non-fire, exact-event-id redelivery, different-event-id-after-first-detection, merchant-cancellation-G3A-unaffected, no-inventory/no-refund — plus a full realistic end-to-end test in `StripeRefundWebhookTest` driving a real checkout → real expiry sweep → real late webhook → a direct `RefundService::refund()` call proving G3-B ineligibility). **Full backend suite: 557 passed, 1759 assertions, 0 failures** (551 pre-Phase-9E-3 baseline + 6 net new). `./vendor/bin/pint --test`: clean, 3 changed files (and clean across all 5 files staged for the joint commit). A dedicated read-only verification pass re-inspected the diff fresh, confirmed the ordering of the new check relative to the pre-existing terminal-status guard, confirmed the exact structured log fields, and confirmed `Payment.status` is never assigned anywhere in the new code. `git status --short`/`git show --name-only` confirmed exactly the 7 expected files (spanning both Phase 9E-2 and 9E-3) staged and committed together as `84a73c3`, with this document itself deliberately excluded from that commit (reconciled separately, in this pass).

**Phase 9E-4 — Expiry-Sweep Late-Success Compensation: designed, reviewed, approved, implemented, and verified; not yet committed.** Closes the one item "Open Decisions" had left open since Phase 9E-3 — a compensating-refund path for an order whose `status_reason` is `payment_succeeded_after_expiry_cancellation` (Payment canceled by the expiry sweep, but Stripe later reported the PaymentIntent succeeded). Followed the full Discovery → Design → Approval → Implementation → Verification cycle established by every prior G3 sub-phase. **Approved design: admin-triggered only** — the `payment_intent.succeeded` webhook still only detects and records the alarm (9E-3, unchanged); no Stripe call is made synchronously from the webhook, consistent with this codebase's "webhook handlers stay fast" discipline and with the precedent already set by G3-B. **`Payment.status` is never touched by this phase** — it remains `Canceled` even after a successful compensating refund, preserving the same terminal-status invariant every prior G3 sub-phase has protected; the "Stripe succeeded, then was refunded" fact is reconstructed from the `Payment` row (`Canceled`/`expired`) plus a linked `Refund` row plus `Order.status_reason`'s own transition, never from a single flag.

`RefundService` gained a new, **structurally separate** entry point, `refundLateSucceededExpiredPayment()` — deliberately not a branch inside the existing `refund()`/`isRefundableOrderState()` (which G3-B already extended and which this phase leaves completely untouched), since `refund()`'s Payment lookup requires `PaymentStatus::Succeeded`, which this case's Payment can never satisfy by design. The new method independently re-verifies, under its own `orders` row lock: `Order.status === Cancelled`, `Order.status_reason === 'payment_succeeded_after_expiry_cancellation'`, a `Payment` with `status === Canceled AND failure_reason === 'expired'` carrying a Stripe PaymentIntent, and no existing active `Refund` — then reuses the identical Stripe-call/idempotency-key-derivation/Refund-row-creation shape `refund()` already established (the local `Refund` row is still never created optimistically before Stripe confirms the request). `StripePaymentWebhookService::transitionOrderToRefunded()` gained a second, parallel branch (alongside the existing G3-B one) that recognizes this case's alarm `status_reason` and resolves it to a new, distinct literal, `payment_refunded_after_expiry_cancellation`, without ever setting `Order.status` to `Paid` or `Refunded` — the order stays `Cancelled` throughout, exactly like the G3-B case. **`restoreInventoryForRefund()` required no code change at all**: its existing 9E-2 double-credit guard (checking, per `order_item_id`/`payment_id`, whether a `Release` row already exists before re-crediting) already produces the correct refund-only outcome for this case too, since the expiry sweep's own `Release` rows already exist — confirmed by a dedicated new test, not just by inspection.

`RefundController::store()` reuses the single existing `POST /api/stores/{store}/orders/{order}/refund` endpoint (per the approved design, no second endpoint was introduced) — a new private `isExpirySweepLateSuccessCase()` pre-check (comparing the order's own already-public `status`/`status_reason`, mirroring the frontend's identical check) decides whether to dispatch to `refund()` or to the new method; this pre-check is non-authoritative, since both target methods independently re-verify their own full eligibility under their own lock regardless of which one is called. No change to `RefundPolicy`, `RefundCreateRequest`, or `RefundResource`. Frontend: `ordersApi.ts` gained a `PAYMENT_SUCCEEDED_AFTER_EXPIRY_CANCELLATION_REASON` constant; `OrderDetailPage.tsx`'s `canRefund` gained a parallel `isExpirySweepCompensationEligible` OR-clause and a third confirmation-copy branch — same single "Refund order" button and confirmation flow, no new page, no new dashboard/filter (explicitly out of scope per the approved design).

**Explicitly out of scope, per the approved design** (none of this was implemented): automatic/webhook-synchronous refund; queue/Job infrastructure; re-fulfillment or any new inventory-claim identity; partial refunds; Analytics; a fix for the separate, adjacent finding that merchant-cancelling a `Pending` order does not itself release its inventory claim (a distinct gap in `OrderStatusUpdateService`, unrelated to the expiry-sweep trigger this phase addresses — left for a future item); a new reconciliation dashboard/filter/badge (the case remains queryable today via a plain `orders.status`/`status_reason` filter, with no schema change); and no database migration (none was required — see the full Design Proposal for the field-by-field justification).

**Verification**: **focused (`RefundManagementTest` + `RefundTenantIsolationTest` + `RefundPolicyTest` + `StripeRefundWebhookTest` + `StripeWebhookTest`): 111 passed, 436 assertions** (15 new tests: 10 in `RefundManagementTest` covering the success path for both Owner/Store Admin, RBAC rejection, independent Order-side and Payment-side eligibility failures, already-resolved rejection, active-refund 409, and Stripe failure/already-refunded mapping; 2 tenant-isolation tests in `RefundTenantIsolationTest`; 3 in `StripeRefundWebhookTest` covering webhook resolution with the double-credit guard proven, duplicate-webhook idempotency, and a full real-checkout → real-expiry-sweep → real-late-webhook → real-admin-refund → real-refund-webhook end-to-end chain). **Full backend suite: 572 passed, 1826 assertions, 0 failures** (557 pre-Phase-9E-4 baseline + 15 new). `./vendor/bin/pint --test`: clean, 244 files. Frontend `tsc -b`: 0 errors; `npm run lint`: 0 errors, same 4 pre-existing warnings, 0 new; `npm run build`: succeeds. `git status --short` confirms exactly 8 files changed (3 backend services/controllers, 3 backend test files, 2 frontend files) — no unrelated file touched, no migration, no dependency change.

## Current Task

None in progress. Phase 8 Block 8E (Order History, Order Detail, checkout confirmation integration) was committed as `ca77060` (`feat: add customer order history and detail`). Phase 8 Block 8F — Integration Pass (cross-block customer-journey E2E coverage plus the `CartContext` login/merge race-condition fix found during it) was committed as `6341be7` (`feat: complete storefront integration pass`). **Phase 0 through Phase 7 remain complete as previously recorded. Phase 8 — Frontend Storefront (Blocks 8-0, 8A, 8C, 8B as revised — committed `b1bbcba` — 8D, 8E, and 8F) is complete.**

**Phase 9A — Organization Lifecycle Management is complete**, committed `a3a0ccf` (`feat: implement organization lifecycle management`) — Platform Admin approve/reject/suspend/reactivate on `organizations.status`, with backend services/policy-equivalent guard/routes/tests and the `/admin/organizations` frontend. **Phase 9B — Category Management is complete**, committed `c6727d3` (`feat: implement category management`) — merchant Category CRUD, `CategoryPolicy`/RBAC, store-scoped tenant isolation, active-product deletion protection, and Product-form category-selector integration, with backend/frontend/tests. **Phase 9C — Customer Management is complete**, committed `608f397` (`feat: implement customer management`) — merchant-facing, read-only, store-scoped Customer list/detail, searchable by name/email, paginated, with database-side `order_count` (all statuses) and `total_spent` (excludes pending/cancelled, no refund-aware accounting, no `CustomerAddress` exposure) aggregates, `CustomerPolicy`/RBAC, full tenant isolation, and backend/frontend/tests (32 tests, 90 assertions — see the "Phase 9C" entry above for the corrected count and full detail). **Phase 9D — Refunds is complete**, committed `0b0e59e` (`feat: implement refunds`) — merchant full-refund initiation (`RefundService`, `RefundPolicy`, `RefundController`, `StripeRefundGateway` against PaymentIntent), `refund.created`/`refund.updated` webhook-authoritative reconciliation (`charge.refunded` deliberately not used), synchronous inline inventory restoration, `Order → refunded` transition, refund history in `OrderResource`, merchant refund UI on `OrderDetailPage`, full tenant isolation, and backend/frontend/tests (45 tests, 140 assertions — see the "Phase 9D" entry above for full detail). **Phase 9E-1 — G3-A (Detect & Alert) is complete**, committed `df6ce9a` (`feat: add late payment closure alert`) — closes the implementation half of the G3 gap for the merchant-cancellation case: a late `payment_intent.succeeded` webhook landing on an already-non-`Pending`, still-non-terminal-Payment Order sets `Order.status_reason = 'payment_succeeded_after_closure'` and emits exactly one `Log::critical()`, with Payment transitioning to `Succeeded`, Order status never changed, and no inventory or Stripe-refund side effects (6 tests, part of the 31 focused / 537 full-suite totals at the time — see the "Phase 9E-1" entry above for full detail). **Phase 9E-2 — G3-B (Manual Compensation) is complete**, jointly committed with Phase 9E-3 as `84a73c3` (`feat: complete G3 refund and late payment handling`) — a narrowly-scoped manual refund path (`RefundService::isRefundableOrderState()`) letting Owner/Store Admin refund exactly the G3-A merchant-cancellation alarm case, plus inventory double-credit protection in `restoreInventoryForRefund()` and a `payment_refunded_after_closure` resolution marker — never a general Cancelled-order refund rule (see the "Phase 9E-2" entry above for full detail). **Phase 9E-3 — G3-A Extension (Expiry-Sweep Late Payment Detection) is complete**, committed in the same `84a73c3` — closes the *other* half of the original G3 gap, the expiry-sweep race `database-design.md` §14 names explicitly: a late `payment_intent.succeeded` webhook for an expiry-sweep-canceled Payment now sets `Order.status_reason = 'payment_succeeded_after_expiry_cancellation'` and logs `Log::critical()`, while **`Payment.status` deliberately remains `Canceled`** (never corrected to `Succeeded`) and this case is **not** G3-B-eligible (see the "Phase 9E-3" entry above for full detail, and the "G3 family status" summary above the Phase 9E-2 entry for how all four G3 sub-items now relate). See the "Phase 9A" through "Phase 9E-4" entries above for full detail on each. **Phase 9E-4 (Expiry-Sweep Late-Success Compensation) is complete**, committed as `76c09f6` (`feat: add expiry-sweep late-success compensation`). The entire G3 family (both the merchant-cancellation and expiry-sweep triggers, detection and compensation for each) is closed.

**Phase 9F — Analytics v1 is complete**, committed `08f7eb7` (`feat: add store-scoped analytics v1`), preceded by `695416e` (`fix: align customer total spent with net sales`, adding `App\Support\SalesClassification` as the single authoritative "what counts as a gross sale" definition, reused by both `CustomerController`'s aggregates and `AnalyticsService`). Four store-scoped, read-only endpoints (`GET /api/stores/{store}/analytics/{sales,orders,products,customers}`), gated by a new `AnalyticsPolicy` (`viewAnalytics` ability, registered explicitly in `AppServiceProvider` since Analytics has no Eloquent model to auto-discover a policy from) — **Owner/Store Admin only, Staff receives 403** (the only read-only capability in this codebase Staff is excluded from, per `PRD.md` §20.3). `AnalyticsService` is the single implementation behind every endpoint, deliberately positioned as the same implementation a future AI tool layer must consume unmodified rather than duplicating SQL. `AnalyticsDateRange` resolves five fixed presets (`today`/`last_7_days`/`last_30_days`/`this_month`/`last_month`) — **there is no arbitrary custom date-range query parameter**, which means "returning customers" cannot be produced with a nonzero value from data created entirely within one calendar day (both a same-period and an earlier-period gross-sale order are required for a customer to count as returning). Frontend: `/merchant/stores/:storeId/analytics` (`AnalyticsPage.tsx`). Backend tests: `AnalyticsTest`, `AnalyticsTenantIsolationTest`, `AnalyticsDateRangeTest` (unit). This closes the last prerequisite "Development Strategy — Commerce-Complete Before AI" (above) named before AI work could begin — no AI-family work has started.

**PHPUnit test-database-isolation incident and fix (2026-09-12, uncommitted — `backend/phpunit.xml` only):** a `php artisan test` invocation run from inside the `app` Docker container connected to the real development MySQL database (`ai_commerce`) instead of the isolated SQLite `:memory:` database `phpunit.xml` specifies, because `docker-compose.yml` injects real `DB_CONNECTION=mysql`/`DB_DATABASE=${MYSQL_DATABASE}` environment variables into the `app` container, and PHPUnit's `<env>` directives silently no-op against an already-set environment variable unless `force="true"` is given (confirmed directly against PHPUnit 12's own `TextUI\Configuration\PhpHandler::handleEnvVariables()` source, and reproduced/fixed via an isolated, non-Laravel-bootstrapping PHP script exercising PHPUnit's own `XmlConfiguration\Loader`/`PhpHandler` classes). `RefreshDatabase` (used by 46 of 51 backend test files) then ran a `migrate:fresh` against the real database, wiping all rows (schema itself was left fully intact — 31/31 migrations, single batch). **Fix**: `backend/phpunit.xml`'s `DB_CONNECTION`/`DB_DATABASE` `<env>` entries now carry `force="true"`, guaranteeing PHPUnit's SQLite override always wins regardless of the container's real environment. Verified three ways: (1) the isolated PhpHandler-based simulation described above, confirming `mysql`/`ai_commerce` → `sqlite`/`:memory:` under the fixed config; (2) one targeted `RefreshDatabase` test run directly (`IdempotencyKeyTest::test_the_same_idempotency_key_is_allowed_for_different_customers`, passed) with MySQL row counts and the `migrations` table's batch number confirmed byte-for-byte identical before/after; (3) the **full 607-test suite** re-run afterward (all passing) with the same before/after MySQL verification, confirming the fix holds under full-suite load, not just a single test. The manual E2E dataset destroyed by the original incident (Organization/Store/Categories/Products/Inventory/Customers/Store Admin/Staff — see "Completed" entries and this document's prior "Manual E2E Test Data Plan" discussion) has been recreated via the same real API/tinker flows as originally approved.

## Next Step

**Immediate**: this document, `README.md`, and `backend/phpunit.xml` are being reconciled as part of preparing a `v0.1.1` release checkpoint — a full backend-suite + frontend build/lint pass, a live (non-Stripe) manual verification pass, and this documentation pass are being done before any release commit/tag is created, per the user's explicit release workflow. **STRIPE_KEY/STRIPE_SECRET/STRIPE_WEBHOOK_SECRET remain blank in this local environment** — checkout, orders, payments, refunds, the G3 flows, and most of Analytics (which requires paid orders to show non-zero data) could not be exercised live in this pass and remain verified by the automated suite only; this is an accepted, explicitly-documented release scope, not an oversight.

**Beyond the v0.1.1 checkpoint, no further scope is approved.** See "Development Strategy — Commerce-Complete Before AI" above: Analytics (the last named commerce-completeness prerequisite) is now done; whatever is implemented next — further Commerce Completeness items, Production Readiness/CI-CD, or the start of AI work — must go through its own Discovery → Design → Review → Approval → Implementation → Verification → Commit cycle before any code is written; this document does not presume or select a next phase here. Still open or deferred: Job/queue infrastructure for post-payment analytics/notifications (`database-design.md` §4 — no Job class exists anywhere in this codebase yet); partial refunds (`refunds.amount` already accepts any value, so a future partial refund needs only new business logic, no schema change); the separate, adjacent finding that merchant-cancelling a `Pending` order does not itself release its inventory claim; Customer CRUD, `CustomerAddress`, and any CRM/segmentation/export/merge capability; Reports, AI, CI/CD, discounts, tax, password reset, and rate limiting (none implemented); and `reports` (AI-generated artifacts), the only table left unmigrated from the full schema. Do not begin any further implementation without explicit approval.

## Phase 2A (Database Migrations)

**Status: complete and verified**, 2026-08-26. Six migration files under `backend/database/migrations/`, run against the `mysql` container's `ai_commerce` database in batch 2:

| Migration | Table / change | Key facts verified in the running database |
|---|---|---|
| `2026_08_26_220100_create_platform_admins_table` | `platform_admins` (new) | `email` unique; no `organization_id`/`store_id`; no `role`; `deleted_at` present (soft delete). |
| `2026_08_26_220200_create_organizations_table` | `organizations` (new) | `status` varchar default `pending`; `status_reason` nullable; `approved_at`/`rejected_at`/`suspended_at` nullable; `approved_by_platform_admin_id`/`rejected_by_platform_admin_id`/`suspended_by_platform_admin_id` each FK → `platform_admins.id` `ON DELETE SET NULL`; index on `status`; unique `slug`; `deleted_at` present. |
| `2026_08_26_220300_create_stores_table` | `stores` (new) | `organization_id` FK → `organizations.id` `ON DELETE RESTRICT`; `status` default `active`; unique `(organization_id, slug)`; index `organization_id`; `deleted_at` present. |
| `2026_08_26_220400_add_soft_deletes_to_users_table` | `users` (altered) | `deleted_at` added to the existing default-Laravel `users` table; no other column changed. |
| `2026_08_26_220500_create_organization_user_table` | `organization_user` (new) | `organization_id`/`user_id` FKs `ON DELETE CASCADE`; `role` varchar not null; unique `user_id` (one org per user, MVP); index `organization_id`. |
| `2026_08_26_220600_create_store_user_table` | `store_user` (new) | `user_id`/`store_id` FKs `ON DELETE CASCADE`; no role column; unique `(user_id, store_id)`; index `store_id`. |

Verification method: `migrate --pretend` reviewed before running; `migrate` executed; `php artisan db:table <table>` run against all six tables and cross-checked column-by-column against `docs/database/database-design.md`; `migrate:status` confirms all six in batch 2 as `Ran`; `php artisan test` passes (2/2 — Laravel defaults only, no schema-specific tests written yet, consistent with migrations-only scope). No conflicts found between the implementation and Database Design 2.1. `organization_user`/`store_user` column/FK/index choices were inferred (the design doc says "unchanged — see prior design," not reproduced there) — flagged, not objected to.

Not yet done (by design, next phases): Eloquent models, the mutate-on-delete soft-delete observers, RBAC policies, TenantContext middleware, any seeders, and Phase 2B onward's tables.

## Phase 2B (Database Migrations)

**Status: complete and verified**, 2026-08-26. Two migration files under `backend/database/migrations/`, run against the `mysql` container's `ai_commerce` database in batch 3:

| Migration | Table / change | Key facts verified in the running database |
|---|---|---|
| `2026_08_26_220700_create_customers_table` | `customers` (new) | `organization_id`/`store_id` FKs → `organizations.id`/`stores.id`, both `ON DELETE RESTRICT` (previously-unresolved decision, confirmed by you this phase); unique `(store_id, email)`; unique `stripe_customer_id` (nullable-safe); index `organization_id`; `deleted_at` present (soft delete). |
| `2026_08_26_220800_create_customer_addresses_table` | `customer_addresses` (new) | `customer_id` FK → `customers.id`, `ON DELETE CASCADE`; no `organization_id`/`store_id` (scoped via `customers` parent); no unique constraint (the "one `is_default` per customer" rule is an application invariant, not DB-enforced, per the design); index `customer_id`; no `deleted_at` (hard-delete child, not in the soft-delete table list). |

Verification method: `php -l` on both files; `migrate --pretend` reviewed before running; `migrate` executed; `php artisan db:table <table>` run against both tables and cross-checked column-by-column against `docs/database/database-design.md`; `migrate:status` confirms both in batch 3 as `Ran`; `php artisan test` passes (2/2 — Laravel defaults only). No conflicts found between the implementation and Database Design 2.1.

Not yet done (by design, next phases): Eloquent models, the address-service `is_default` invariant, RBAC/policies, TenantContext middleware, any seeders, and Phase 2C onward's tables.

## Phase 2C (Database Migrations)

**Status: complete and verified**, 2026-08-27. Seven migration files under `backend/database/migrations/`, run against the `mysql` container's `ai_commerce` database, all in batch 4:

| Migration | Table / change | Key facts verified in the running database |
|---|---|---|
| `2026_08_27_100100_create_categories_table` | `categories` (new) | `organization_id`/`store_id` FKs → `organizations.id`/`stores.id`, both `ON DELETE RESTRICT` (inferred from the design's undocumented ON DELETE for these two columns, applying the same convention already established for `customers` in Phase 2B — see "Deviations" below); unique `(store_id, slug)`; `deleted_at` present (soft delete — Database Design 2.3 correction). |
| `2026_08_27_100200_create_products_table` | `products` (new) | `organization_id`/`store_id` FKs `ON DELETE RESTRICT` (same inferred convention); `category_id` FK → `categories.id`, nullable, `ON DELETE SET NULL` (explicitly documented); unique `(store_id, slug)`; indexes `(store_id, status)`, `(store_id, category_id)`; `deleted_at` present. |
| `2026_08_27_100300_create_product_options_table` | `product_options` (new) | `product_id` FK → `products.id`, `ON DELETE CASCADE`; unique `(product_id, name)`; no tenant columns (pure child of `products`, per design); no `deleted_at` (not in the soft-delete table list). |
| `2026_08_27_100400_create_product_option_values_table` | `product_option_values` (new) | `product_option_id` FK → `product_options.id`, `ON DELETE CASCADE`; unique `(product_option_id, value)`; no tenant columns; no `deleted_at`. |
| `2026_08_27_100500_create_product_variants_table` | `product_variants` (new) | `organization_id`/`store_id` FKs `ON DELETE RESTRICT` (inferred convention, direct tenant columns per design's stated rationale); `product_id` FK → `products.id`, `ON DELETE CASCADE`; unique `(store_id, sku)` and `(product_id, option_signature)`; index `(store_id, status)`; `deleted_at` present. `option_signature` column created as a plain `varchar(255) not null` — no generation logic implemented (deliberately deferred to the service layer, per Database Design 2.3's documented application-layer invariant). |
| `2026_08_27_100600_create_product_variant_option_values_table` | `product_variant_option_values` (new) | `product_variant_id` FK → `product_variants.id`, `ON DELETE CASCADE`; `product_option_value_id` FK → `product_option_values.id`, `ON DELETE RESTRICT` (the deliberately asymmetric pair, confirmed); unique `(product_variant_id, product_option_value_id)` under an explicit shorter constraint name, `pvov_variant_id_option_value_id_unique` (MySQL's default 64-character identifier limit rejected Laravel's auto-generated name — see "Deviations" below); no `deleted_at`. |
| `2026_08_27_100700_create_product_images_table` | `product_images` (new) | `product_id` FK → `products.id`, `ON DELETE CASCADE`; `product_variant_id` FK → `product_variants.id`, nullable, `ON DELETE CASCADE` (not `SET NULL`, per design); indexes `(product_id, sort_order)`, `(product_variant_id, sort_order)`; no unique constraint (the `is_primary` one-per-parent rule is an application invariant, not DB-enforced, per the design); no `deleted_at`. |

**Deviations from the literal design doc (both flagged, neither a design change):**
1. **`organization_id`/`store_id` `ON DELETE` behavior on `categories`/`products`/`product_variants`** — Database Design 2.3 documents these columns as `FK, not null` but doesn't state an explicit `ON DELETE` action (the same gap that existed for `customers` in Phase 2B, where `RESTRICT` was explicitly decided). Implemented as `RESTRICT`, consistent with that established, documented precedent (`stores.organization_id`, `customers.organization_id`/`store_id`) for every top-level tenant-scoped table in this schema. Not an invented behavior — an applied convention.
2. **`product_variant_option_values`'s unique constraint needed an explicit shorter name.** Laravel's auto-generated identifier for `unique(product_variant_id, product_option_value_id)` (`product_variant_option_values_product_variant_id_product_option_value_id_unique`, 82 characters) exceeds MySQL's 64-character identifier limit and failed at migration time (`SQLSTATE[42000]: ... Identifier name ... is too long`). Fixed by supplying an explicit constraint name, `pvov_variant_id_option_value_id_unique` — same columns, same semantics, same enforcement; only the constraint's name differs from what an unnamed call would have generated. The partially-created table (both FKs applied, unique constraint failed) was dropped and the migration re-run cleanly before this was recorded as `Ran`.

**Verification method**: `php -l` on all 7 files (inside the `app` container); `migrate --pretend` reviewed before running (SQL matched Database Design 2.3 exactly, table-by-table); `migrate` executed (failed once on `product_variant_option_values` per Deviation 2 above, fixed, re-run); `php artisan db:table <table>` run against all 7 tables and cross-checked column-by-column, FK-by-FK, index-by-index against `docs/database/database-design.md` — full match; `migrate:status` confirms all 7 as `Ran`, batch 4; **rollback verified**: all 7 migrations' `down()` methods were exercised via `migrate:rollback` (in two steps, respecting FK dependency order) and confirmed to drop cleanly, then re-migrated to restore the final state; Phase 2A/2B tables (batches 1–3) confirmed untouched throughout (`migrate:status` before/after, and row counts — all zero, no seed data exists yet); `php artisan test` passes (2/2 — Laravel defaults only, no schema-specific tests exist yet).

Not yet done (by design, next phases): Eloquent models, the three Database Design 2.3 application-layer invariants (variant option-value cardinality enforcement, `option_signature` generation, product-soft-delete query scoping — all now documented, none implemented), RBAC/policies, TenantContext middleware, any seeders, and Phase 2D onward's tables.

## Phase 2D (Database Migrations)

**Status: complete and verified**, 2026-08-27. Three migration files under `backend/database/migrations/`, run against the `mysql` container's `ai_commerce` database, all in batch 5:

| Migration | Table / change | Key facts verified in the running database |
|---|---|---|
| `2026_08_27_110100_create_orders_table` | `orders` (new) | `organization_id`/`store_id`/`customer_id` FKs, all `ON DELETE RESTRICT` (explicitly documented in Database Design 2.4, not inferred this time); unique `order_number`; indexes `(store_id, status, created_at)`, `(store_id, customer_id, created_at)` (the 2.4-widened index), `(organization_id, created_at)`; no `shipping_total`/`payment_status`/`fulfillment_status`/`deleted_at` columns (confirmed absent via `information_schema` query, not just by omission from the migration). |
| `2026_08_27_110200_create_order_items_table` | `order_items` (new) | `order_id` FK `ON DELETE CASCADE`; `product_id`/`product_variant_id` FKs, both nullable, `ON DELETE SET NULL`; `selected_options` json nullable; a raw `CHECK (quantity > 0)` constraint added via `DB::statement` (Laravel's MySQL grammar has no native `check()` — confirmed by inspecting `vendor/laravel/framework`), verified present via `information_schema.CHECK_CONSTRAINTS` and confirmed it survives a full rollback/re-migrate cycle. |
| `2026_08_27_110300_create_order_addresses_table` | `order_addresses` (new) | `order_id` FK `ON DELETE CASCADE`; unique `(order_id, type)`; only `created_at` present, no `updated_at` (immutable-after-creation, matching the design); uses the Database Design 2.4-approved field set (`recipient_name`/`line1`/`line2`/`state`) — a mid-task clarification was needed and resolved before implementation (see "Deviations" below). |

**Deviations from the literal task instructions (both resolved before implementation, neither a design change):**
1. **The task message's literal column lists for `orders` and `order_addresses` conflicted with the already-approved Database Design 2.4.** `orders`' list omitted `status_reason` (present in the approved design); `order_addresses`' list used a different, unreviewed field set (`first_name`/`last_name`/`company`/`address_line1`/`address_line2`/`province`) that didn't match the approved design or the already-migrated `customer_addresses` table it's meant to mirror. Per CLAUDE.md's rule to stop and report conflicts rather than silently resolve them, this was raised via a clarifying question before any file was written. Resolved: implement exactly per the approved Database Design 2.4 in both cases (`status_reason` included; `recipient_name`/`line1`/`line2`/`state` used) — no design change, no schema deviation from what was already approved.
2. **`orders.type`'s stray `->default('shipping')`, added then removed before validation.** While writing the migration, a default value was added to `order_addresses.type` that isn't specified anywhere in Database Design 2.4 (only "not null" is documented, no default). Caught and removed during self-review, before running `migrate --pretend`. Mentioned here only because the task asked not to silently introduce additional constraints — this one was caught and reverted within the same turn, never reached the database.

**Verification method**: `php -l` on all 3 files; `migrate --pretend` reviewed before running (SQL matched Database Design 2.4 exactly, table-by-table, including the widened index); `migrate` executed cleanly on the first attempt (batch 5, all 3 `Ran`); `php artisan db:table <table>` run against all 3 tables and cross-checked column-by-column, FK-by-FK, index-by-index against `docs/database/database-design.md`; the `CHECK` constraint (not visible in `db:table`'s output) verified separately via a direct `information_schema.TABLE_CONSTRAINTS`/`CHECK_CONSTRAINTS` join; a direct `information_schema.COLUMNS` query confirmed none of `shipping_total`/`payment_status`/`fulfillment_status`/`deleted_at` exist on any of the 3 tables; **rollback verified**: all 3 `down()` methods exercised via `migrate:rollback --step=3` (correct reverse-FK order: `order_addresses` → `order_items` → `orders`) and confirmed to drop cleanly, then re-migrated to restore the final state, with the `CHECK` constraint re-verified present afterward; `migrate:status` confirms all 3 in batch 5 as `Ran`; Phase 2A/2B/2C tables (batches 1–4) confirmed untouched throughout (`migrate:status` before/after, and row counts on all 15 prior tables — all zero, no seed data exists yet); `php artisan test` passes (2/2 — Laravel defaults only, no schema-specific tests exist yet).

Not yet done (by design, next phases): Eloquent models, checkout/order-creation service logic (including the atomic `orders`+`order_items`+`order_addresses` transaction), RBAC/policies, TenantContext middleware, any seeders, and Phase 2E onward's tables (`payments`, `refunds`, `stripe_webhook_events`).

## Phase 2E (Database Migrations)

**Status: complete and verified**, 2026-08-27. Four migration files under `backend/database/migrations/`, run against the `mysql` container's `ai_commerce` database, all in batch 6:

| Migration | Table / change | Key facts verified in the running database |
|---|---|---|
| `2026_08_27_120100_create_payment_methods_table` | `payment_methods` (new) | `customer_id` FK `ON DELETE CASCADE`; unique `stripe_payment_method_id`; `exp_month`/`exp_year` as unsigned tinyint/smallint per the design; `deleted_at` present (soft delete — a removed card can still be referenced historically by `payments.payment_method_id`). |
| `2026_08_27_120200_create_payments_table` | `payments` (new) | `organization_id`/`store_id`/`order_id` FKs `ON DELETE RESTRICT` (org/store per the newly-approved I-2 decision; order per the pre-existing explicit design); `payment_method_id` FK nullable `ON DELETE SET NULL`; unique `stripe_payment_intent_id`; indexes `(order_id, status)`, `(store_id, status)`; `status` and `currency` correctly carry **no default** (none specified anywhere in Database Design 2.4 or the approval — confirmed via `information_schema`, not silently added). |
| `2026_08_27_120300_create_refunds_table` | `refunds` (new) | Implemented exactly per the approved I-1 schema: `organization_id`/`store_id`/`order_id`/`payment_id` FKs `ON DELETE RESTRICT`, `initiated_by_user_id` nullable FK → `users`, `ON DELETE SET NULL`; unique `stripe_refund_id`; `status` defaults to `'pending'` (the one column in this whole phase with an explicit default, per I-1); indexes `(order_id, status)`, `(store_id, status)`. This closes the design-review gap (I-1) where `refunds` previously had no formal column-type specification anywhere in `database-design.md`. |
| `2026_08_27_120400_create_stripe_webhook_events_table` | `stripe_webhook_events` (new) | Implemented exactly per the approved I-3 schema: no foreign keys (standalone, tenant-independent by design); unique `stripe_event_id`; `processed_at` **not nullable** (confirmed via `information_schema` — no "nullable" tag in `db:table`'s output either); `payload` json nullable; only `created_at`, no `updated_at`; index `(type, processed_at)`. |

**Verification method**: `php -l` on all 4 files; `migrate --pretend` reviewed before running (SQL matched Database Design 2.4 plus the approved I-1/I-2/I-3 decisions exactly, table-by-table); `migrate` executed cleanly on the first attempt (batch 6, all 4 `Ran`); `php artisan db:table <table>` run against all 4 tables and cross-checked column-by-column, FK-by-FK, index-by-index; a direct `information_schema.COLUMNS` query cross-verified exact nullability and defaults for every column across all 4 tables (catching that `payments.status`/`currency` correctly have no default, unlike `refunds.status`); a direct `information_schema.COLUMNS` query on `orders` confirmed no Stripe-specific or `payment_status` column exists there; **rollback verified**: all 4 `down()` methods exercised via `migrate:rollback --step=4` (correct reverse-FK order: `stripe_webhook_events` → `refunds` → `payments` → `payment_methods`) and confirmed to drop cleanly, then re-migrated to restore the final state; `migrate:status` confirms all 4 in batch 6 as `Ran`; Phase 2A/2B/2C/2D tables (batches 1–5, 18 tables) confirmed untouched throughout (`migrate:status` before/after, and row counts on all 18 — all zero, no seed data exists yet); `php artisan test` passes (2/2 — Laravel defaults only, no schema-specific tests exist yet); `git diff` confirmed empty on every Phase 2A–2D migration file and on `backend/app`/`backend/routes` (no application code touched).

**Residual documentation gap — since closed.** At the time this phase's migrations were implemented, `docs/database/database-design.md` still contained only a prose-level description of `refunds` and a partially-detailed `stripe_webhook_events` spec — this turn's instructions restricted documentation updates to this file only. A **separate, subsequent documentation-only turn brought `database-design.md` to version 2.5**, bringing it into full agreement with these migrations: `refunds` gained a complete formal column table, `stripe_webhook_events` gained exact types/nullability, and `payments.organization_id`/`store_id`'s `ON DELETE RESTRICT` was made explicit — closing this gap. `database-design.md` is now the authoritative record of that detail; this file's own top-of-document version reference has been updated accordingly (see "Current Phase" above).

## Phase 2F (Database Migrations)

**Status: complete and verified**, 2026-08-27. Two migration files under `backend/database/migrations/`, run against the `mysql` container's `ai_commerce` database, both in batch 7:

| Migration | Table / change | Key facts verified in the running database |
|---|---|---|
| `2026_08_27_130100_create_inventory_table` | `inventory` (new) | `product_variant_id` FK `ON DELETE CASCADE`, unique (true 1:1 with `product_variants`); `quantity_on_hand` (int, default 0) with a raw `CHECK (quantity_on_hand >= 0)` constraint (Laravel has no native `check()`, same pattern as Phase 2D's `order_items.quantity`), verified via `information_schema.CHECK_CONSTRAINTS`; `low_stock_threshold` nullable; no `organization_id`/`store_id`/`product_id`/`reserved_quantity`/`available_quantity`/`deleted_at` — confirmed absent via a direct `information_schema.COLUMNS` query against the full column set, not just by omission from the migration. |
| `2026_08_27_130200_create_inventory_transactions_table` | `inventory_transactions` (new) | `product_variant_id` FK `ON DELETE RESTRICT`; `order_id`/`order_item_id` FKs, both nullable, `ON DELETE RESTRICT`; `created_by_user_id` FK → `users`, nullable, `ON DELETE SET NULL`; unique `(order_item_id, reason)` (MySQL's NULL-distinct semantics correctly allow unlimited manual adjustments where `order_item_id` is null); indexes `(product_variant_id, created_at)`, `order_id`, `order_item_id`; only `created_at`, no `updated_at` and no `deleted_at` (append-only ledger); no `before_quantity`/`after_quantity`/tenant columns — confirmed absent via the same full-column-set query. |

**Verification method**: `php -l` on both files; `migrate --pretend` reviewed before running (SQL matched the approved schema exactly, and every generated identifier name checked against MySQL's 64-character limit — longest was 58 characters, no shortening needed this time, unlike Phase 2C); `migrate` executed cleanly on the first attempt (batch 7, both `Ran`); `php artisan db:table` on both tables; a direct `information_schema.COLUMNS` query listing the complete column set for both tables (confirming exactly the approved columns, nothing extra, nothing missing); the `CHECK` constraint verified via `information_schema.CHECK_CONSTRAINTS`; **rollback verified**: both `down()` methods exercised via `migrate:rollback --step=2` (correct reverse order: `inventory_transactions` → `inventory`) and confirmed to drop cleanly, then re-migrated to restore the final state, with the `CHECK` constraint re-verified present afterward; `migrate:status` confirms both in batch 7 as `Ran`; Phase 2A–2E tables (batches 1–6, 22 tables) confirmed untouched throughout (`migrate:status` before/after, and row counts on all 17 named tables — all zero, no seed data exists yet); `php artisan test` passes (2/2 — Laravel defaults only, no schema-specific tests exist yet); `git diff` confirmed empty on every prior migration file and on `backend/app`/`backend/routes` (no application code touched).

**G3 status, explicitly not resolved here (by design and by instruction)**: the late-payment/inventory-oversell business policy (what happens when a payment succeeds against inventory a different order already consumed) remains open. This phase's design review confirmed the *schema* needs no change to accommodate whatever policy is eventually chosen — the locked service's rejection guard is already policy-agnostic — so the question is now explicitly re-scoped to a future checkout/webhook **service-design** phase, not attached to "Phase 2F" generally. No inventory column, table, constraint, or migration logic was added for this.

**Documentation note**: `system-architecture.md` §5's stale "one `inventory` row per product" wording (should read "per product variant") was reconfirmed as present and unchanged during this phase's design review. Per this turn's explicit instruction, it was not modified — it remains exactly as already tracked in this file's Known Issues section below.

Not yet done at the time (by design, next phase): Eloquent models — now done, see "Phase 3 (Foundation Application Layer)" below. Still not yet done: the locked inventory-mutation service itself, checkout/webhook service logic (including G3's resolution), RBAC/policies, TenantContext middleware, any seeders, and whatever phase comes after Phase 2F (analytics/AI reports, `reports` table — not yet broken down in detail).

## Phase 3 (Foundation Application Layer)

**Status: Models + Enums slice complete and verified**, 2026-08-27. 24 Eloquent Models and 9 PHP enums created under `backend/app/Models/` and `backend/app/Enums/`; the existing `backend/app/Models/User.php` updated. No migration, docs, routes, or config file touched.

**Enums** (backed string enums, native PHP + Eloquent cast, per Database Design 2.5's stated convention — never MySQL native `ENUM`): `OrganizationStatus`, `StoreStatus`, `CatalogStatus` (shared by `products.status`/`product_variants.status` — independent columns, identical value set), `OrderStatus`, `PaymentStatus`, `RefundStatus`, `InventoryTransactionReason`, `PaymentMethodType`, `OrderAddressType`.

**Deliberately not enum-cast at the time**: `stripe_webhook_events.type` (Stripe's own externally-controlled event-type vocabulary — a closed local enum would break on any future Stripe event type this app doesn't yet recognize, still true) and, at the time of this Phase 3 slice, `organization_user.role` (its value set was undocumented, not merely "external" — resolved and cast in Block 2, see "Phase 4 (Back Office Feature Blocks)" below).

**Models**: one per table — `PlatformAdmin`, `Organization`, `Store`, `User` (updated), `OrganizationUser`, `StoreUser`, `Customer`, `CustomerAddress`, `Category`, `Product`, `ProductOption`, `ProductOptionValue`, `ProductVariant`, `ProductVariantOptionValue`, `ProductImage`, `Order`, `OrderItem`, `OrderAddress`, `PaymentMethod`, `Payment`, `Refund`, `StripeWebhookEvent`, `Inventory`, `InventoryTransaction`. Three required an explicit `$table` override because Laravel's default pluralization guesses wrong: `OrganizationUser` → `organization_user`, `StoreUser` → `store_user`, `Inventory` → `inventory` (Laravel would otherwise guess `organization_users`, `store_users`, `inventories`) — caught by cross-checking every model against the live schema, not assumed.

**Relationships**: every one of the 45 foreign keys verified in the Phase 2A–2F audit is represented as a `belongsTo`/`hasOne`/`hasMany`, plus the 3 genuine many-to-many shapes as `belongsToMany` (`Organization`↔`User` via a custom `OrganizationUser` pivot model carrying `role`; `Store`↔`User` via `store_user`; `ProductVariant`↔`ProductOptionValue` via `product_variant_option_values`).

**Casts/soft-deletes/timestamps**: `decimal:2` on every monetary column; `array` on the 3 JSON columns; `boolean` on `is_default`/`is_primary`; `datetime` on business timestamp columns; `SoftDeletes` trait applied to exactly the 9 tables carrying `deleted_at` (re-verified live: `platform_admins`, `organizations`, `stores`, `users`, `customers`, `products`, `product_variants`, `categories`, `payment_methods`); `const UPDATED_AT = null;` on the 3 `created_at`-only tables (`order_addresses`, `inventory_transactions`, `stripe_webhook_events`).

**Fillable strategy** (an interpretive choice, not spelled out column-by-column anywhere in CLAUDE.md, but directly justified by its already-stated trust-boundary rules): form-style CRUD tables get a fillable list of genuinely user/admin-supplied fields, always excluding tenant FKs, status/audit columns tied to a workflow, and system/Stripe-derived fields. Financial/audit/system-of-record tables (`Order`, `OrderItem`, `OrderAddress`, `Payment`, `PaymentMethod`, `Refund`, `Inventory`, `InventoryTransaction`, `StripeWebhookEvent`, `StoreUser`, `ProductVariantOptionValue`) get an **empty** fillable list — every attribute must be set explicitly by future service-layer code, never mass-assigned. `PlatformAdmin`/`Customer` extend `Authenticatable` (matching the existing `User` convention) but are deliberately **not** wired with `HasApiTokens` or a guard yet — that's the next slice, not this one. No `HasFactory` added to any new model (no factories exist for them, and none were created).

**Verification method**: `php -l` on all 33 touched files — 33/33 passed; `php artisan model:show` run against all 24 models, cross-checking every relationship and cast against the live database schema — confirmed all three table-name overrides work, confirmed every relationship resolves to a valid class, confirmed the `UPDATED_AT = null` overrides correctly suppress `updated_at` on exactly the 3 tables that should lack it; `php artisan test` (2/2 passing, unchanged); `git diff --stat` on `backend/database/migrations/`, `docs/`, `backend/routes/`, `backend/config/` all empty — confirmed zero scope leakage beyond `app/Models/` and `app/Enums/`.

Not yet done (by design, next phases): the multi-guard Sanctum configuration and auth flows, RBAC/Policies (blocked on the `organization_user.role` decision below), TenantContext middleware and global scopes, every domain service (checkout, Stripe payment/webhook, the locked inventory-mutation service, refund, address/payment-method `is_default`, variant `option_signature`/cardinality), the mutate-on-delete soft-delete observer, queue jobs, Controllers/Form Requests/API Resources/routes, seeders, and AI/reports.

## Phase 4 (Back Office Feature Blocks)

Remaining application-layer work is tracked here as a numbered sequence of independently-shippable back-office feature blocks (roadmap agreed for this track before Block 1 began), rather than continuing the Phase-N/database-schema-oriented numbering used above for Phases 0–3.

### Block 1 — Platform Admin Authentication + Back-Office Shell

**Status: complete and verified**, 2026-08-29. Scope was deliberately limited to authentication + application shell only — explicitly excluding RBAC, `OrganizationRole`, `TenantContext`, organization approval, merchant/customer registration or login, and every business feature block (stores, products, orders, customers, inventory, analytics, AI).

**Backend — repository inspected first**: no policies, controllers (beyond the base `Controller`), services, or auth guards/flows existed prior to this block; `routes/api.php` was a one-line stub; `laravel/sanctum` v4 was installed but only the default `web`/session guard was configured.

| Change | Detail |
|---|---|
| `config/auth.php` | Added `platform_admin` and `merchant` guards (`driver: sanctum`), each bound to its own provider (`platform_admins`/`users`); added the `platform_admins` provider (model `PlatformAdmin`). |
| `app/Models/PlatformAdmin.php` | Added `HasApiTokens`, `HasFactory`. |
| `app/Models/User.php` | Added `HasApiTokens`. |
| `app/Http/Controllers/Platform/PlatformAuthController.php` | `login()` (401 on bad credentials, not a validation exception — kept distinct from the Form Request's 422 for missing/malformed fields, since the frontend must branch on the two differently), `logout()` (revokes the token that authenticated the request), `me()` (returns the authenticated identity). |
| `app/Http/Requests/Platform/PlatformAdminLoginRequest.php` | `email`/`password` required. |
| `routes/api.php` | `POST /api/platform/auth/login` (public); `POST /api/platform/auth/logout`, `GET /api/platform/auth/me` (both `auth:platform_admin`); pre-existing `/api/user` stub re-scoped from the provider-agnostic `auth:sanctum` to `auth:merchant` (needed as a real merchant-guarded route to prove cross-guard rejection — this is guard wiring, not merchant registration). |
| `database/factories/PlatformAdminFactory.php` | New — mirrors `UserFactory`'s shape. |

**Isolation mechanism — read from Sanctum's own source (`vendor/laravel/sanctum/src/Guard.php`) before implementing, not assumed**: `Guard::hasValidProvider()` rejects a token whose `tokenable` model isn't an instance of `config("auth.providers.{$this->provider}.model")`. Because each named guard here is constructed with a distinct `provider`, a token issued to a `User` is structurally rejected by `auth:platform_admin` (and vice versa) by Sanctum itself — no custom authorization code performs this check.

**Backend verification**: `php artisan test` — **12/12 passing** (`tests/Feature/Platform/PlatformAdminAuthTest.php`): login success, invalid password, unknown email, missing-fields validation (422), valid-token access to `/me`, merchant-token rejected by `/api/platform/auth/me` (401), platform-admin-token rejected by `/api/user` (401), logout, revoked-token-rejected-after-logout, unauthenticated-rejected. `./vendor/bin/pint --test` — clean, 89 files. Live-stack curl verification through nginx (`http://localhost:8080`, not just the test suite): valid login → 200 + token; wrong password → 401; `/me` with valid token → 200; platform-admin token against `/api/user` → 401.

**Testing-harness gotcha found and documented, not a product bug**: the revoked-token test initially failed (`/me` returned 200 after logout instead of 401) because Laravel's `RequestGuard::user()` caches its resolved user for the lifetime of the booted `Application` (`if (! is_null($this->user)) return $this->user;`), and PHPUnit reuses one booted `Application` across multiple simulated HTTP calls within a single test method — so the second simulated call saw the first call's stale cached user instead of re-authenticating. This cannot happen on real traffic (every real request boots a fresh `Application`/`AuthManager`). Fixed by calling `Auth::forgetGuards()` between the two simulated calls in that one test, with an inline comment explaining why — worth knowing before writing similar multi-request-in-one-test auth tests in later blocks.

**Frontend — repository inspected first**: a bare `npm create vite` React 19 + TypeScript scaffold with no router, no Tailwind, no state-management library, and the stock demo page (`App.tsx`/`App.css`, `react.svg`/`vite.svg`/`hero.png`/`icons.svg`). No new frontend framework was introduced — Tailwind CSS and React Query were already named in CLAUDE.md's declared tech stack, just not yet installed; React Router was added as the minimum routing primitive a multi-page shell requires.

| Area | What was built |
|---|---|
| Removed | `src/App.css`, `src/assets/{hero.png,react.svg,vite.svg}`, `public/icons.svg` (unused demo boilerplate). |
| `src/services/` | `apiClient.ts` (typed `fetch` wrapper, `ApiError` with status + field errors), `platformAuth.ts` (login/logout/me calls). |
| `src/admin/auth/` | `AuthContext.tsx` (token in `localStorage`; React Query-backed `/me` for session restore; `status` derived as `unauthenticated` directly from a query error rather than clearing storage from a `useEffect` — the first version tripped an oxlint cascading-render warning, fixed by deriving state during render instead), `ProtectedRoute.tsx`, `LoginPage.tsx` (200/401/validation/network-error handling, loading state, redirect to `/admin/dashboard` on success). |
| `src/admin/layout/` | `AdminLayout.tsx`, `Header.tsx` (app name, current admin identity, logout), `Sidebar.tsx` (responsive: fixed column on desktop, horizontal scroll strip on mobile), `navigation.ts` (single source of truth for the 10 nav items). |
| `src/admin/dashboard/DashboardPage.tsx` | Placeholder metric cards (Organizations/Stores/Customers/Orders), values explicitly `—`/"No data yet" so nothing reads as real data. |
| `src/admin/pages/ComingSoonPage.tsx` | Reused for every nav item besides Dashboard. |
| `src/App.tsx`, `src/main.tsx` | Route tree (`/admin/login` public, `/admin/*` behind `ProtectedRoute`) and `QueryClientProvider`. |
| `vite.config.ts`, `src/index.css` | `@tailwindcss/vite` plugin; Tailwind v4 CSS-first config (`@theme`, no `tailwind.config.js`); light/dark via `prefers-color-scheme`, matching the removed demo page's existing approach. |
| `.env.example` | `VITE_API_BASE_URL` (defaults to `http://localhost:8080` in code if unset — no `frontend/.env` needed for the default local setup). |

**Frontend verification**: `tsc -b` — no errors. `npm run lint` (oxlint) — clean except one pre-identified harmless warning (`only-export-components`, a fast-refresh hint on `AuthContext.tsx` exporting both the provider and the `useAuth` hook — common pattern, not worth a file split for this block). `npm run build` — succeeds (274.85 kB JS / 16.54 kB CSS, gzipped 86.72 kB / 4.16 kB).

**Not performed**: interactive browser click-through of the login → dashboard → logout flow — browser automation (`claude-in-chrome`) was unavailable/declined this session. Everything the click-through would depend on (endpoint contracts matching the frontend's expectations, build output, dev server serving the correct HTML shell) was verified by the checks above; the manual click-through itself is the one unverified step.

**Not yet done** (by design, later blocks): `OrganizationRole` enum, `TenantContext` middleware, Policies, merchant (`User`)/customer registration and login, organization approval workflow, and every business feature block (stores, products, orders, customers, inventory, analytics, AI) and its corresponding frontend pages (currently all "Coming Soon" placeholders).

### Block 2 — Merchant Identity, Organization Registration & RBAC Foundation

**Status: complete and verified**, 2026-08-30. Scope: resolve the `organization_user.role` open decision, add merchant registration/login/logout/me on the existing `merchant` guard, make merchant registration also bootstrap the initial (`pending`) `Organization` + owning `organization_user` row, and establish the first `TenantContext`/Policy pattern — explicitly not Store-level Policies, Customer authentication, Platform Admin approval endpoints, or any business feature (products/orders/customers/inventory/analytics/AI), all deferred to later blocks.

**Backend — repository inspected first**: no `OrganizationRole` enum, no merchant controllers/requests, no `TenantContext`, no Policies, and no migrations were touched — `organization_user.role` was already a plain (uncast) string column from Phase 2A, per the Phase 3 note this block closes.

| Change | Detail |
|---|---|
| `app/Enums/OrganizationRole.php` | New backed string enum — exactly `owner`/`store_admin`/`staff`, the approved value set. |
| `app/Models/OrganizationUser.php` | `role` now cast to `OrganizationRole::class` (previously a plain string — see "Open Decisions," now closed). |
| `app/Models/Organization.php` | Added `HasFactory` (needed for `Organization::factory()` in the new tests below; no prior model in this codebase had a factory besides `User`/`PlatformAdmin`). |
| `app/Support/TenantContext.php` | New — a small `readonly` value object: `user` (merchant `User`), `organization`, `role` (`OrganizationRole`), `store` (nullable, always null for now — no store-selection concept yet). Bound into the container per-request by the middleware below, not constructed ad hoc by controllers. |
| `app/Http/Middleware/ResolveMerchantTenantContext.php` | New — for a request already authenticated on the `merchant` guard, looks up the authenticated user's own `organization_user` row (with its `Organization`) and binds a `TenantContext` instance into the container. **Reads nothing from the request** (no query param, header, or body field) to determine which organization — tenant identity is derived exclusively from the authenticated user's own membership row, structurally preventing client-supplied tenant spoofing. Aborts 403 if a merchant-authenticated user somehow has no organization membership (shouldn't occur given registration's transactional guarantee, but not assumed). |
| `bootstrap/app.php` | Registered the `tenant.merchant` middleware alias for the class above. |
| `app/Http/Controllers/Merchant/MerchantAuthController.php` | `register()` (creates `User` + `Organization` (status left to the DB's `pending` default, confirmed via `refresh()` after insert since Eloquent doesn't otherwise pull DB-level defaults back into a freshly-created in-memory model) + the owning `organization_user` row, all inside one `DB::transaction()`; issues a token after commit), `login()` (401 on bad credentials, same convention as `PlatformAuthController`), `logout()` (revokes the authenticating token), `me()` (reads the bound `TenantContext`, does not re-query). |
| `app/Http/Requests/Merchant/MerchantRegisterRequest.php` | `name`, `email` (`unique:users,email`), `password` (`confirmed`, `Password::min(8)`), `organization_name` — no new database columns implied; `organization_name` is registration *input*, mapped onto the existing `organizations.name`/`slug` columns (slug generated via `Str::slug()` with a numeric-suffix collision loop against the existing `unique(slug)` constraint). |
| `app/Http/Requests/Merchant/MerchantLoginRequest.php` | `email`/`password` required — mirrors `PlatformAdminLoginRequest`. |
| `app/Policies/OrganizationPolicy.php` | New — first Policy in the merchant RBAC layer, establishing the pattern later Store/Product/Order Policies extend: `view` (any member), `update`/`manageStores`/`manageUsers`/`viewAnalytics` (Owner only — org-wide analytics is explicitly an Owner capability per the documented hierarchy; Store Admin's analytics access is store-scoped and belongs to a future `StorePolicy`, not modeled here). Resolves the acting user's role by querying `organization_user` directly for `(user_id, organization_id)` — a cross-organization user (no membership row) is denied every capability, including plain `view`. Laravel's standard model→policy naming auto-discovery applies; no explicit `Gate::policy()` registration was needed or added. |
| `routes/api.php` | `POST /api/auth/register`, `POST /api/auth/login` (both public); `POST /api/auth/logout`, `GET /api/auth/me` (both `['auth:merchant', 'tenant.merchant']`) — the `/api/auth` namespace CLAUDE.md's API Surface section already names. |
| `database/factories/OrganizationFactory.php` | New — `name`/`slug` only (status left to the DB default); an `afterCreating()` hook calls `refresh()` for the same reason `register()` does, so `Organization::factory()->create()` always returns a model whose `status` is actually hydrated. |

**Registration transaction — what "atomic" means here**: `User::create()`, `Organization::create()` + `refresh()`, and the `OrganizationUser` pivot row (built via direct property assignment, not mass assignment, since `OrganizationUser`'s fillable list is deliberately just `['role']`) all happen inside one `DB::transaction()` closure; the Sanctum token is created *after* the transaction commits. A failure at any step rolls back all three inserts — no orphan `User` or `Organization` is left behind (verified by a dedicated test, see below).

**Backend verification**: `php -l` — 15/15 new/modified files clean. `./vendor/bin/pint --test` — one auto-fix pass needed (`new_with_parentheses` on 4 files, `new OrganizationUser()` → `new OrganizationUser`), then clean, 100 files. `php artisan test` — **35/35 passing** (12 pre-existing Platform Admin tests unchanged + 23 new): `tests/Feature/Merchant/MerchantAuthTest.php` (register success/duplicate-email/missing-fields/transactional-no-orphan, login success/invalid-password/unknown-email, authenticated/unauthenticated `me`, logout, revoked-token-rejected, cross-guard rejection both directions), `tests/Feature/Merchant/TenantIsolationTest.php` (Merchant A vs. B `/me` resolve to their own org only, a spoofed `organization_id` query param **and** header are both ignored, cross-guard rejection both directions, Owner role resolves correctly, `TenantContext` carries the expected user/org/role), `tests/Unit/Policies/OrganizationPolicyTest.php` (Owner passes all five capabilities, Store Admin/Staff pass only `view`, a user with no membership in the target organization is denied even `view`). Live-stack curl verification through nginx (`http://localhost:8080`): register → 201 + token + `organization.status: "pending"` + `role: "owner"`; `/me` with that token → 200, same identity; logout → 200; `/me` after logout → 401. The smoke-test row was deleted from the dev database afterward.

**Testing-harness gotcha hit again, same root cause as Block 1's**: `TenantIsolationTest`'s "Merchant A cannot access Merchant B's context" test issues two simulated requests (different users' tokens) within one test method; `RequestGuard`'s per-`Application` user cache meant the second request initially saw the first request's cached user. Fixed with the same `Auth::forgetGuards()` call between the two requests, with the same inline explanation — now the second documented instance of this gotcha, worth remembering for any future test issuing multiple authenticated requests with different identities in one method.

**Frontend**: added `frontend/src/services/merchantAuth.ts` only — typed request/response functions (`registerMerchant`, `loginMerchant`, `logoutMerchant`, `fetchCurrentMerchant`) mirroring `platformAuth.ts`'s shape, calling the four new `/api/auth/*` endpoints. Deliberately nothing else: the existing `/admin/*` shell (`AuthContext`, `ProtectedRoute`, `LoginPage`, layout, nav) is Platform Admin's own application (its nav items — Organizations/Stores/Users/... — are platform-wide views, not a merchant's own store console), so it needs no adjustment to "coexist" with merchant auth — there is no merchant-facing route tree yet for `AuthContext` to conflict with. No merchant login page, no merchant `AuthContext`, no new routes were added, per the block's explicit "do not build merchant business pages yet" instruction.

**Frontend verification**: `tsc -b` — no errors. `npm run lint` (oxlint) — clean, same one pre-existing `only-export-components` warning on `AuthContext.tsx` from Block 1 (unrelated to this block's changes). `npm run build` — succeeds, same output size as Block 1 (274.85 kB JS / 16.54 kB CSS gzipped) since `merchantAuth.ts` isn't imported anywhere yet.

**Not yet done** (by design, later blocks): Store-level Policies (`StorePolicy` and store-scoped analytics/AI authorization for Store Admin), Eloquent global scopes applying `TenantContext` automatically to tenant-scoped queries (this block establishes the context-resolution foundation; no business models are queried through it yet since no business endpoints exist), Customer authentication/guard, Platform Admin organization-approval endpoints, merchant password reset, and every business feature block (stores, products, orders, customers, inventory, analytics, AI) and its frontend.

### Block 2b — Merchant Authentication UI

**Status: complete and verified**, 2026-08-30. A deliberate follow-up slice closing the frontend gap a post-Block-2 review identified: Block 2 shipped `merchantAuth.ts` (the typed API client) but no browser-facing UI ever called it — no merchant login/registration page existed, so the fully-working backend was only reachable via `curl`/tests. Scope was explicitly limited to merchant authentication UI only — no business pages.

**Backend**: unchanged — no backend file was touched. `merchantAuth.ts` gained exported type names only (`RegisterPayload`/`AuthResponse`/`MeResponse` → `MerchantRegisterPayload`/`MerchantAuthResponse`/`MerchantMeResponse`, all previously private to the module) so `MerchantAuthContext` could reference them; no request/response shape or endpoint changed.

| Area | What was built |
|---|---|
| `src/merchant/auth/MerchantAuthContext.tsx` | New — mirrors `admin/auth/AuthContext.tsx`'s pattern exactly (own `localStorage` key `ai_commerce.merchant_token`, own React Query key namespace `['merchant', ...]`, `status: 'loading' \| 'authenticated' \| 'unauthenticated'` derived the same way), extended with `register()` alongside `login()`/`logout()`, and exposes `user`/`organization`/`role` (not just a single identity object, since merchant identity has three parts). Login/register responses' `organization`/`role` are typed nullable (defensive — unreachable via this UI since registration always creates an org); the cache is only optimistically primed when both are present, otherwise the already-`enabled` `/api/auth/me` query resolves the real state itself, so `/me` stays the single source of truth rather than the mutation success handler re-deriving it. Field-level API error messages (e.g. "The email has already been taken.") are surfaced via a small `describeError()` helper rather than only the generic top-level validation message. |
| `src/merchant/auth/MerchantProtectedRoute.tsx` | New — same shape as `admin/auth/ProtectedRoute.tsx`, redirects to `/merchant/login` (not `/admin/login`), reuses the existing `FullPageSpinner`. |
| `src/merchant/auth/MerchantLoginPage.tsx`, `MerchantRegisterPage.tsx` | New — same visual/structural pattern as `admin/auth/LoginPage.tsx` (Tailwind classes copied, not reinvented), cross-linked to each other. Registration adds `organization_name`/password-confirmation fields and client-side checks (all-fields-required, 8+ char password, passwords match) before the API round-trip, mirroring `MerchantRegisterRequest`'s server-side rules for immediate feedback. |
| `src/merchant/MerchantLandingPage.tsx` | New — deliberately minimal: displays name/email/organization/organization status/role plus a logout button, reusing the existing `Card` component. Explicitly not a dashboard — no nav, no business data, no placeholders for future features. |
| `src/App.tsx` | Modified — purely additive. `AuthProvider` (Platform Admin) still wraps the entire route tree exactly as before; a new sibling layout route (`<MerchantAuthProvider><Outlet/></MerchantAuthProvider>`) wraps only the three new merchant routes (`/merchant/login`, `/merchant/register`, `/merchant`). Every existing Platform Admin route/line is untouched — confirmed via `git diff` (28 insertions, 1 line changed to add the `Outlet` import, 0 deletions elsewhere). |

**Routes added**: `GET /merchant/login`, `GET /merchant/register` (both public), `GET /merchant` (protected — the landing page). No change to `/admin/*` or `/`.

**Isolation verified, not just assumed**: Platform Admin's `AuthProvider` is still an ancestor of the merchant routes in the React tree (since it wraps everything), but it uses its own `localStorage` key and its own React Query key namespace, and nothing under `/merchant/*` calls `useAuth()` (Platform's hook) — only `useMerchantAuth()`. The two identity domains share no state.

**Backend verification**: `php artisan test` — **35/35 still passing** (unchanged from Block 2, confirming the type-only `merchantAuth.ts` edit and the frontend-only changes had no backend effect).

**Frontend verification**: `tsc -b` — no errors. `npm run lint` (oxlint) — 0 errors, 2 warnings (the pre-existing `AuthContext.tsx` warning plus the identical, expected `only-export-components` warning on the new `MerchantAuthContext.tsx`, which exports both its provider and its `useMerchantAuth` hook — same accepted pattern as Platform Admin's). `npm run build` — succeeds (286.41 kB JS / 17.06 kB CSS gzipped 87.93 kB / 4.22 kB, up from Block 1/2's 274.85 kB/16.54 kB as expected for the new pages).

**API-level verification, live stack through nginx**: register → 201 + token + `organization.status: "pending"` + `role: "owner"` (response shape matches `MerchantAuthResponse` exactly); `/api/auth/me` with that token → 200, same identity (matches `MerchantMeResponse`); logout → 200; `/me` after logout → 401; login again → 200 + fresh token. The smoke-test row was deleted from the dev database afterward. The Vite dev server (`http://localhost:5173`) was also confirmed to serve the SPA shell (200) for `/merchant/login`, `/merchant/register`, `/merchant`, and `/admin/login`.

**Not performed**: interactive browser click-through (register → landing page → refresh → session restore → logout → redirect → login again) — browser automation (`claude-in-chrome`) was declined for this session, same limitation Block 1 disclosed. Every piece that click-through would exercise was verified by other means above (route shell serves correctly, API contract matches the frontend types exactly, `tsc`/lint/build all clean), but the actual in-browser interaction sequence itself remains unverified by a human or automated click-through.

**Not yet done** (unchanged, still later blocks): merchant password reset, Store-level Policies/UI, Customer authentication, and every business feature block and its frontend.

### Block 3 — Store Management & Store-Level RBAC Foundation

**Status: complete and verified**, 2026-08-31. A design-then-implement block: the design ("A" through "J" — scope, data/access model, middleware flow, authorization model, API design, frontend design, test matrix, decisions needing approval, implementation order, expected file list) was reviewed and approved before any code was written, then implemented in the approved order (`OrganizationRole` → `TenantAccess` → `StoreFactory` → `StorePolicy` → store context middleware → Resource/Requests → controller/routes → backend tests → verification → frontend → verification → docs), running focused tests after each slice rather than only at the end.

**Backend — repository inspected first**: `stores`/`store_user` tables and `Store`/`StoreUser` models already existed (Phase 2A/3) with no `role` column on `store_user` — role always lives on `organization_user`; confirmed via the actual migration files, not assumed.

| Change | Detail |
|---|---|
| `app/Enums/OrganizationRole.php` | Added `rank()`/`atLeast()` — Owner(3) > Store Admin(2) > Staff(1). Added proactively for Block 4 (Products/Orders will need "at least Store Admin" checks); Block 3's own `StorePolicy` doesn't exercise the Store-Admin-vs-Staff branch since both have identical store-level capability in this block (view an assigned store, nothing more) — deliberately not differentiated, per the approved decision. |
| `app/Support/TenantAccess.php` | New — `membershipFor(User)` (the "discover my org from scratch" shape) and `roleFor(User, Organization)` (the "check role for a known org" shape) consolidate what was three separate duplicated queries; `canAccessStore(User, Store, OrganizationRole)` implements Owner-implicit-access-to-every-org-store vs. Store Admin/Staff-requires-`store_user`-row once, shared by the new middleware and `StorePolicy`. |
| `app/Http/Middleware/ResolveMerchantTenantContext.php` | Refactored to call `TenantAccess::membershipFor()` instead of its own inline query — zero behavior change (re-verified: full Block 2 suite still 100% green after the refactor). |
| `app/Http/Controllers/Merchant/MerchantAuthController.php` | `login()` refactored to call `TenantAccess::membershipFor()` — same zero-behavior-change verification. |
| `app/Policies/OrganizationPolicy.php` | `roleFor()` now delegates to `TenantAccess::roleFor()` instead of its own inline query. |
| `app/Models/Store.php` | Added `HasFactory` (needed for `Store::factory()` in tests, mirroring the `Organization`/`OrganizationFactory` precedent from Block 2). |
| `database/factories/StoreFactory.php` | New — `organization_id` is deliberately excluded from `Store`'s fillable list (tenant FKs are never mass-assignable), so a `forOrganization(Organization $organization)` state sets it via direct property assignment in an `afterMaking()` hook rather than through `definition()`/`create($attrs)`, which would silently drop it. Verified directly via `tinker` before building anything on top of it. |
| `app/Policies/StorePolicy.php` | New — `viewAny`/`view` any org member with access; `create`/`update`/`delete` Owner-only **and** `organization.status === active` (the approved pending-org gate — folded into the Policy, not middleware, so `view`/`viewAny`/list stay unaffected by org status, matching Block 2's "don't gate `/me` on status" precedent). Every method re-derives role/access against the actual `$organization`/`$store` argument via `TenantAccess` — never trusts a bound `TenantContext` refers to the same model being authorized, same discipline the post-Block-2 review confirmed as correct for `OrganizationPolicy`. |
| `app/Http/Middleware/ResolveMerchantStoreContext.php` | New (alias `tenant.merchant.store`) — enriches the already-bound `TenantContext` with a verified `Store` for store-scoped routes. Deliberately does **not** use Laravel implicit route-model binding for `{store}` (that would resolve `Store::findOrFail($id)` with zero tenant awareness, and relying on binding-vs-middleware execution order for a security-load-bearing check wasn't worth it); instead resolves `Store::where('id', ...)->where('organization_id', $context->organization->id)` itself. Wrong org or nonexistent → 404 (a foreign org's store must not be distinguishable from one that doesn't exist); right org but no `store_user` access → 403. |
| `app/Http/Resources/StoreResource.php` | New — introduces Laravel API Resources into this codebase for the first time (Block 1/2 only used ad hoc `present()` arrays for single-object auth responses). Per the approved decision, this is now the standard shape for business-resource responses going forward. |
| `app/Http/Requests/Merchant/{StoreCreateRequest,StoreUpdateRequest}.php` | New — `StoreCreateRequest`: `name` only (slug is always server-generated, per the approved decision). `StoreUpdateRequest`: `name`/`status` (`in:active,inactive`), no `slug` field — not editable in Block 3. |
| `app/Http/Controllers/Merchant/StoreController.php` | New — `index` (role-filtered: Owner sees every org store, Store Admin/Staff see only `store_user`-linked ones, paginated), `store` (create, server-generated unique-per-org slug via the same collision-suffix pattern `MerchantAuthController::uniqueSlug()` already established), `show`/`update`/`destroy` (all read `TenantContext->store`, already resolved and access-verified by the middleware; each still calls `Gate::authorize()` explicitly for the fine-grained action check — the middleware's job is coarse "can this user reach this store," not the final authorization decision). `destroy()` soft-deletes; the dependent-resource blocking check `database-design.md` documents (soft-deleting a store blocked while active Products/Customers/Orders exist under it) is explicitly not implemented — there's nothing to check yet — but the controller carries an inline comment marking exactly where that guard belongs once those blocks land. |
| `routes/api.php` | `GET/POST /api/stores` (`auth:merchant` + `tenant.merchant`); `GET/PATCH/DELETE /api/stores/{store}` (additionally `tenant.merchant.store`). Deliberately flat — no `organization_id` anywhere in the URL; organization is always resolved from `TenantContext`, which removes an entire class of spoofing attempts by construction. |
| `bootstrap/app.php` | Registered the `tenant.merchant.store` middleware alias. |

**Backend verification**: `php -l` — every new/modified file clean. `./vendor/bin/pint --test` — one auto-fix needed (`StoreResource.php`'s `@mixin \App\Models\Store` docblock reference → a proper `use` import), then clean, 113 files. `php artisan test` — **77/77 passing** (the 38 from Block 2/2b unchanged + 39 new): `tests/Unit/Enums/OrganizationRoleTest.php` (3), `tests/Unit/Support/TenantAccessTest.php` (8), `tests/Unit/Policies/StorePolicyTest.php` (8 — including "Store Admin and Staff have identical store-level capability," asserting the two roles' policy results are literally equal, and the pending-org gate for create/update/delete while `view`/`viewAny` stay open), `tests/Feature/Merchant/StoreManagementTest.php` (19 — full CRUD lifecycle, slug collision, validation, every role × every action), `tests/Feature/Merchant/StoreTenantIsolationTest.php` (4 — client-supplied `organization_id` in the create body ignored, cross-org store id 404s on read/update/delete, `TenantContext->store` reflects the verified store, cross-guard rejection). Live-stack verification through nginx went beyond the automated tests: registered two separate merchants/organizations, manually activated both (Platform Admin approval endpoints don't exist yet — direct DB update simulating that), and walked the full lifecycle — create (pending-org 403, then success once active) → slug collision → list → show → update (name+status) → soft-delete → 404-after-delete → cross-org 404 on read → **a live spoofing attempt** (`organization_id` in a create body set to a different, active organization's id) confirmed the store landed in the caller's own organization, not the spoofed one. All smoke-test rows deleted from the dev database afterward.

**Frontend**: `src/services/storesApi.ts` (typed CRUD calls), `src/merchant/stores/useStores.ts` (shared React Query hooks — `useStoresList`/`useStore`/`useCreateStore`/`useUpdateStore`/`useDeleteStore`, one `['stores', ...]` key namespace so mutations invalidate the list consistently), `src/merchant/layout/MerchantLayout.tsx` (minimal header + Stores link + logout, wrapping only the `/merchant/stores/*` subtree — not a full sidebar shell, and `/merchant` itself is untouched, per the approved decision not to auto-redirect it), `src/merchant/stores/{StoreListPage,StoreCreatePage,StoreDetailPage}.tsx`. `MerchantAuthContext` gained an exposed `token` field (previously internal-only) since this is the first block needing authenticated business API calls from a page. `MerchantLandingPage` gained one "Manage stores" link — otherwise unchanged.

**Frontend verification**: `tsc -b` — no errors. `npm run lint` — 0 errors, the same 2 pre-existing `only-export-components` warnings (Block 1's and Block 2b's context files), 0 new. `npm run build` — succeeds (298.63 kB JS / 18.86 kB CSS, gzipped 89.65 kB / 4.55 kB). Vite dev server confirmed serving the SPA shell (200) for `/merchant/stores`, `/merchant/stores/new`, `/merchant/stores/36`.

**Not performed**: interactive browser click-through — `claude-in-chrome` unavailable this session, same disclosed limitation as Block 2b. Route shells, the full API contract (including the live spoofing test above), and build/lint/type-check all verified by other means.

**Not yet done** (by design, later blocks): the dependent-resource blocking check on store soft-delete (no Products/Customers/Orders exist to check against yet — see the `StoreController::destroy()` comment marking where it goes), Store-level capability differentiation between Store Admin and Staff (identical in Block 3, starts mattering once Products/Orders add their own Policies using `OrganizationRole::atLeast()`), Platform Admin organization-approval endpoints (still simulated via direct DB update for testing), Customer authentication, and every other business feature block (Products, Orders, Inventory, Customers, Payments, Analytics, AI) and its frontend.

### Block 4A — Product Management MVP

**Status: complete and verified**, 2026-08-31. A design-then-implement block, same discipline as Block 3: a full Block 4 design (Products/Inventory/Orders scope, RBAC matrix, tenant/context model, Policy model, API/frontend design, test matrix, decisions needing approval) was reviewed and approved first, recommending the block be split into 4A/4B/4C rather than built as one; this slice implements 4A (Products) only. Repository inspected first, not assumed: `products`/`product_variants`/`categories` all carry their own `organization_id`/`store_id` (direct FK columns — Product is directly store-scoped, cannot belong to multiple stores); `Product`/`ProductVariant`/`Order`/`Inventory` models already carried explicit code comments from Phase 3 naming not-yet-built services (`VariantOptionService`, a locked inventory-mutation service, a `CheckoutService`) as the only things allowed to touch certain fields — these comments, not assumption, are why the option/variant matrix, Inventory, and Orders are explicitly out of this slice.

| Change | Detail |
|---|---|
| `app/Policies/ProductPolicy.php` | New — `viewAny`/`view`: any role with store access (identical shape to `StorePolicy`). `create`/`update`/`delete`: **first Policy to use `OrganizationRole::atLeast(StoreAdmin)`** instead of an exact role match, since Owner *and* Store Admin both qualify (unlike `StorePolicy`, which is Owner-only) — plus store access plus `organization.status === active`. Staff gets no additional restriction beyond simply not passing the `atLeast` check — verified by a dedicated test asserting Store Admin's and Staff's `view`/`viewAny` results are literally equal. |
| `app/Http/Middleware/` | **No new file.** Per the approved design, Product reuses `tenant.merchant.store` verbatim — it needed no new context-resolution layer since it's directly store-scoped, same as `Store` itself. |
| `app/Http/Controllers/Merchant/ProductController.php` | New — `{product}` is deliberately not implicitly route-bound (same reasoning `ResolveMerchantStoreContext` used for `{store}`): a private `resolveProduct()` queries `Product::where('id', ...)->where('store_id', $context->store->id)`, so a product id belonging to another store can never be reached regardless of what's in the URL. `store()` (create) builds the `Product` and its single default `ProductVariant` inside one `DB::transaction()` — `option_signature` hard-coded to `''`, no `VariantOptionService`. `update()` edits both the product and its one variant together (the only variant that exists in this MVP) — a single `status` input is applied to both rows identically, a deliberate MVP simplification (the schema has two independent status columns; this block doesn't expose that distinction). `destroy()` soft-deletes the product only — the variant row is left untouched, matching Database Design 2.3's documented rule that product soft-delete never cascades to variants. |
| `app/Http/Requests/Merchant/{ProductCreateRequest,ProductUpdateRequest}.php` | New — `name`/`description`/`category_id`/`status`/`sku`/`price`/`compare_at_price`. No `organization_id`/`store_id`/option-matrix fields accepted at all. `category_id` (when given) is validated via `Rule::exists('categories','id')->where('store_id', ...)` — scoped to the resolved store, never trusting a bare client-supplied id. `sku` uniqueness validated per-store via `Rule::unique(...)->where('store_id', ...)` (`->ignore()`-ing the current variant on update) rather than surfacing a raw DB constraint violation. |
| `app/Http/Resources/ProductResource.php` | New — `id`/`store_id`/`category_id`/`name`/`slug`/`description`/`status`/timestamps, plus a nested `variant` object (this MVP's single default variant). `organization_id`/`metadata` deliberately omitted as not needed by the current frontend contract. |
| `app/Models/Product.php` | Added `HasFactory` (needed for `Product::factory()` in tests, mirroring `Store`'s Block 3 precedent). |
| `database/factories/ProductFactory.php` | New — `organization_id`/`store_id` excluded from `Product`'s fillable list (tenant FKs never mass-assignable), so a `forStore(Store $store)` state sets both via direct property assignment in `afterMaking()`, deriving `organization_id` from the store rather than accepting it independently — verified directly via `tinker` before building anything on top of it, same discipline as `StoreFactory`. |
| `routes/api.php` | `GET/POST /api/stores/{store}/products`, `GET/PATCH/DELETE /api/stores/{store}/products/{product}` — nested inside the existing `tenant.merchant.store` middleware group, no `organization_id` anywhere in the URL. |

**Backend verification**: `php -l` — every new/modified file clean. `./vendor/bin/pint --test` — clean on the first run, 122 files. `php artisan test` — **111/111 passing** (77 from Blocks 2/2b/3 unchanged + 34 new): `tests/Unit/Policies/ProductPolicyTest.php` (9 — Owner/Store-Admin/Staff × every ability, cross-store and cross-org denial, the pending-org gate, and the explicit "Store Admin and Staff have identical read access" equality assertion), `tests/Feature/Merchant/ProductManagementTest.php` (21 — full CRUD lifecycle, slug collision, the default-variant-created-atomically test, a duplicate-SKU-across-the-transaction test asserting neither the product nor variant is left behind on failure, validation, every role × every action, deleted-product exclusion from listing and 404 on direct access, and confirmation the variant row survives a product's soft-delete), `tests/Feature/Merchant/ProductTenantIsolationTest.php` (4 — spoofed `store_id`/`organization_id` in the create body ignored, cross-org product id 404s on read/update/delete, same-org-unassigned-store 403 (not 404), cross-guard rejection). Live-stack verification through nginx went beyond the automated tests: registered two merchants/organizations, activated both (still simulated via direct DB update — no approval endpoint exists), and walked create (blocked while pending → succeeds once active, response includes the atomically-created variant) → list → show → update (name+status+price, confirmed status applied to both rows) → soft-delete → 404-after-delete → cross-org 404 → **a live spoofing attempt** (`store_id`/`organization_id` in a create body pointing at the other merchant's real store) confirmed the product landed in the caller's own store regardless. All smoke-test rows deleted from the dev database afterward.

**Frontend**: `src/services/productsApi.ts`, `src/merchant/products/useProducts.ts` (React Query hooks mirroring `useStores.ts`'s shape exactly, keyed `['stores', storeId, 'products', ...]`), `src/merchant/products/{ProductListPage,ProductCreatePage,ProductDetailPage}.tsx` — all reusing `MerchantLayout`/`useMerchantAuth`/the existing Tailwind conventions unchanged, mutation controls hidden (not just disabled) for Staff client-side while the server remains the actual authority. `StoreDetailPage` gained one "Products →" link; no Inventory/Orders navigation added.

**Frontend verification**: `tsc -b` — no errors. `npm run lint` — 0 errors, the same 2 pre-existing warnings, 0 new. `npm run build` — succeeds (314.69 kB JS / 18.94 kB CSS, gzipped 91.05 kB / 4.56 kB). Vite dev server confirmed serving the SPA shell (200) for all three new routes.

**Not performed**: interactive browser click-through — `claude-in-chrome` had already been declined earlier in this session (and the instruction was not to re-suggest it), so the same disclosed limitation as Blocks 2b/3 applies; every other verification layer (route shells, the full live API contract including the spoofing test, build/lint/type-check) was performed instead.

**Not yet done** (by design, later blocks — see the approved Block 4 design's 4A/4B/4C split): the Product option/variant matrix (`ProductOption`/`ProductOptionValue`/`ProductVariantOptionValue`/`ProductImage`, multiple variants per product), Inventory (Block 4B — requires building the real locked, row-locking mutation service, not a placeholder), Orders (Block 4C — view + a narrow, already-fully-specified status-transition whitelist only, no creation path), Customers, Payments, Analytics, AI.

**Architectural Acceptance Review — Block 4A**: reviewed against tenant isolation, `ProductPolicy` correctness, atomic-creation correctness, API design, `tenant.merchant.store` reuse, frontend isolation, reusable conventions, and test coverage. **Verdict: PASS WITH NOTES**, no blocking issues. Three recommended-cleanup items — completed before Block 4B began (see "Pre-4B Cleanup" below).

### Pre-4B Cleanup

**Status: complete and verified**, 2026-08-31. Three items from the Block 4A review, done before any Block 4B code was written:

1. **A genuine `DB::transaction` rollback test for Product creation.** The existing "atomic on duplicate SKU" test was found to only prove the *validation-rejection* path (rejected before `store()`'s transaction body ever runs) — not that the transaction itself rolls back a real post-validation failure. A new test (`ProductManagementTest::test_creation_transaction_rolls_back_both_product_and_variant_on_a_genuine_insert_failure`) forces a real failure *inside* the transaction via a `ProductVariant::saving()` model-event hook (no controller code touched) and asserts neither row persists. This same technique is reused for Block 4B's own transaction test below.
2. **Product category tenant-isolation coverage.** A new test (`ProductTenantIsolationTest::test_a_category_belonging_to_another_store_cannot_be_referenced_on_product_creation`) proves a `category_id` belonging to a *different store in the same organization* (the stronger case) is rejected — `category_id`'s `Rule::exists(...)->where('store_id', ...)` scoping had no direct test before this.
3. **Extracted duplicated test helpers.** `activeOrganization()`/`memberWithRole()`/`attachToStore()` had been copy-pasted verbatim across seven test classes (`OrganizationPolicyTest`, `StorePolicyTest`, `ProductPolicyTest`, `StoreManagementTest`, `ProductManagementTest`, `StoreTenantIsolationTest`, `ProductTenantIsolationTest`) — the same duplication shape `App\Support\TenantAccess` was created to close in application code after Block 2. Extracted into `Tests\Concerns\CreatesTenantFixtures`, a shared trait all seven now `use` — pure extraction, zero behavior change (confirmed: full suite re-run before and after, identical pass count).

**Verification**: `php -l` clean on every touched file. `./vendor/bin/pint --test` — one auto-fix pass (`no_unused_imports` on two files after the trait extraction removed the last usage of an import), then clean. `php artisan test` — 113/113 passing (111 + 2 new tests) both immediately after the trait extraction (proving zero behavior change) and after adding the two new tests.

### Block 4B — Inventory

**Status: complete and verified**, 2026-08-31. Repository inspected first, not assumed, per instruction: `inventory` has no `organization_id`/`store_id` of its own — only `product_variant_id` (unique) — confirming Inventory's tenant identity is only reachable by joining through the variant, exactly as the Block 4 design predicted. `inventory_transactions` has `delta` (signed int), `reason`, nullable `order_id`/`order_item_id`, `unique(order_item_id, reason)` (harmless for merchant-driven rows, which never set `order_item_id`). Neither `Inventory` nor `InventoryTransaction` had a factory; no code anywhere created an `inventory` row automatically when a variant was created — Block 4A's `ProductController::store()` was confirmed untouched and unmodified for this reason (see below for how that gap is actually handled).

| Change | Detail |
|---|---|
| `app/Services/InventoryAdjustmentService.php` | New — the single locked mutation path required by CLAUDE.md. `adjust()`: `DB::transaction()` → `DB::table('inventory')->insertOrIgnore(...)` (lazily materializes a zero-quantity row the first time a variant is ever adjusted — raw query builder, not Eloquent, since `Inventory`'s `$fillable` is deliberately empty; race-safe, since a losing concurrent insert is a harmless no-op) → `Inventory::where(...)->lockForUpdate()->first()` → quantity check/update → `InventoryTransaction` ledger insert → commit. Throws `InsufficientInventoryException` (new, `app/Exceptions/`) if the resulting quantity would go negative — raised *after* acquiring the row lock, never from a pre-check against a possibly-stale read. The `reason` parameter is **not** restricted here — `sale`/`refund` will need this exact same service, unchanged, once checkout exists; restriction to `restock`/`adjustment` for merchant requests lives one layer up, in `InventoryAdjustRequest`. Verified directly via `tinker` (restock → adjustment → an oversized negative adjustment correctly rejected, final quantity and ledger count both correct) before any test was written. |
| `app/Policies/InventoryPolicy.php` | New — `view`: any role with store access (identical shape to `ProductPolicy`/`StorePolicy`). `adjust`: `OrganizationRole::atLeast(StoreAdmin)` + store access + `organization.status === active` — same pattern as `ProductPolicy`'s mutate gate. **A real bug was found here during implementation, not left to a review**: `InventoryPolicy`'s methods authorize against `ProductVariant` (Inventory has no org/store of its own to authorize against), but Laravel's Gate auto-discovery maps by the *model's* class name (`ProductVariant` → `ProductVariantPolicy`), not the policy class's own name — so `Gate::authorize('view', $variant)` found no policy at all and denied by default, a 403 on every request regardless of role. Diagnosed via a `dd()` in the controller (confirmed context/role/variant were all correct) before finding the actual cause. Fixed with an explicit `Gate::policy(ProductVariant::class, InventoryPolicy::class)` registration in `AppServiceProvider::boot()` (previously empty) — the one Policy in this project not found by naming convention alone, now documented as such in that file. |
| `app/Http/Middleware/` | **No new file**, per the explicit instruction — Inventory reuses `tenant.merchant.store` verbatim. |
| `app/Http/Controllers/Merchant/InventoryController.php` | New — `{variant}` is resolved and store-scoped at the controller level (`resolveVariant()`), same discipline as `ProductController::resolveProduct()` — never implicitly bound, never trusts the bare route segment. `show()` does **not** write on read: if no `inventory` row exists yet for the variant, an unsaved, transient `Inventory` instance (`quantity_on_hand: 0`) is returned instead of persisting one from a GET request; only `adjust()` (via the service) ever writes. `adjust()` catches `InsufficientInventoryException` and converts it to a clean 422. |
| `app/Http/Requests/Merchant/InventoryAdjustRequest.php` | New — `delta` (required, integer, `not_in:0`), `reason` (required, `in:restock,adjustment` — `sale`/`refund` rejected here, not in the service), `note` (nullable, max:255). |
| `app/Http/Resources/InventoryResource.php` | New — `product_variant_id`/`quantity_on_hand`/`low_stock_threshold`/`updated_at`, following `StoreResource`/`ProductResource`'s convention exactly; works identically whether wrapping a real, persisted `Inventory` or the transient zero-quantity instance from `show()`. |
| `app/Models/ProductVariant.php` | Added `HasFactory`. |
| `database/factories/ProductVariantFactory.php` | New — `organization_id`/`store_id`/`product_id` set via `forProduct(Product $product)` in `afterMaking()` (same direct-property-assignment pattern as `ProductFactory::forStore()` — none of these are in `ProductVariant`'s `$fillable`); `option_signature` hard-coded to `''`, matching the only value Block 4A ever produces. Verified via `tinker` before use. |
| `app/Providers/AppServiceProvider.php` | The `Gate::policy()` registration above — previously an empty stub. |
| `routes/api.php` | `GET /api/stores/{store}/variants/{variant}/inventory`, `POST .../inventory/adjust` — flat under store, not nested under `/products/{product}/`, since inventory's authorization only cares about variant→store, not the parent product; nested inside the existing `tenant.merchant.store` group. |

**Backend verification**: `php -l` clean on every new/modified file. `./vendor/bin/pint --test` — clean on the first run, 133 files. `php artisan test` — **143/143 passing** (113 after pre-4B cleanup + 30 new): `tests/Unit/Policies/InventoryPolicyTest.php` (9 — Owner/Store-Admin/Staff × view/adjust, cross-store and cross-org denial, the pending-org gate, Staff-equals-Store-Admin read-access equality), `tests/Feature/Merchant/InventoryManagementTest.php` (15 — never-adjusted read returns a zero snapshot **without writing a row** (explicitly asserted via `assertDatabaseMissing`), restock/adjustment, RBAC matrix, the pending-org gate, negative-resulting-inventory rejection with the prior state proven unchanged, validation, `sale`/`refund` rejected from the merchant endpoint, multi-adjustment accumulation, **a `DB::listen()`-based test proving the adjustment query pipeline actually issues a `SELECT ... FOR UPDATE`** against `inventory`, and **a forced-mid-transaction-failure test** (same `Model::saving()` technique as the Pre-4B cleanup's Product test) proving both the quantity update *and* the ledger insert roll back together), `tests/Feature/Merchant/InventoryTenantIsolationTest.php` (6 — cross-org variant id 404s, same-org-unassigned-store 403, a variant from a *different store in the same org* rejected even though the caller could reach that other store directly, a spoofed `variant_id`/`product_variant_id` in the adjust body ignored, pending-org read-allowed/adjust-denied, cross-guard rejection). Live-stack verification through nginx: registered a merchant, activated the org, created a store+product, confirmed the never-adjusted variant reads as 0 quantity with no row written, restocked +50, adjusted −3 (→47), confirmed an oversized negative adjustment (−1000) correctly 422s leaving quantity at 47, confirmed `reason: sale` is rejected with 422, re-read final state to confirm no partial effects. All smoke-test rows deleted from the dev database afterward.

**Frontend**: `src/services/inventoryApi.ts`, `src/merchant/products/useInventory.ts` (`useInventory`/`useAdjustInventory`, keyed `['stores', storeId, 'variants', variantId, 'inventory']`), `src/merchant/products/InventorySection.tsx` — a component embedded directly in `ProductDetailPage`, **not** a new top-level Inventory browsing screen, per the explicit instruction. Shows current quantity to every role; the adjust form (delta/reason/note) renders only when `canAdjust` (Owner/Store Admin) — Staff sees the same quantity display with no form, and the server-side `InventoryPolicy` remains the actual authority regardless of what the UI shows.

**Frontend verification**: `tsc -b` — no errors. `npm run lint` — 0 errors, same 2 pre-existing warnings, 0 new. `npm run build` — succeeds (318.71 kB JS / 19.17 kB CSS, gzipped 91.64 kB / 4.60 kB). Vite dev server confirmed serving the SPA shell (200) for the product detail route the Inventory UI lives on.

**Not performed**: interactive browser click-through — `claude-in-chrome` unavailable this session (declined earlier, instructed not to re-suggest), same disclosed limitation as every prior block. Route shell, the full live API contract (including FOR UPDATE and rollback proofs), and build/lint/type-check were all verified by other means.

**Not yet done** (by design, later blocks): Orders (Block 4C), Customers, Payments, Analytics, AI, the Product option/variant matrix, and system-driven (`sale`/`refund`) inventory mutations — `InventoryAdjustmentService` is ready for them (reason-agnostic by design), but nothing calls it that way yet since checkout doesn't exist.

### Block 4C — Order Management

**Status: complete and verified**, 2026-08-31. Design freshly re-verified against the live repository state (migrations, models, enums, policies, middleware, `database-design.md`'s "Order & Payment State Models" §3–8, `project-status.md`) before implementation, not assumed from the earlier Block 4 design pass — confirmed `orders`/`order_items`/`order_addresses`/`payments`/`refunds` all unchanged since Phase 2D/2E, `OrderStatus` enum exactly matches the documented seven-state lifecycle, and neither `Order`/`OrderItem`/`OrderAddress`/`Customer` had `HasFactory` or a factory yet.

| Change | Detail |
|---|---|
| `app/Support/MerchantOrderStatusTransitions.php` | New — a pure, static, RBAC-free whitelist of exactly `pending→cancelled`, `paid→processing`, `processing→shipped`, `shipped→completed`. Named "Merchant..." specifically so it can never be extended to (or confused with) the webhook/system-only edges (`pending→paid`, `{paid,processing,shipped,completed}→refunded`) — those are a structurally different concern with a different actor, deliberately not represented here at all. |
| `app/Exceptions/InvalidOrderTransitionException.php` | New — thrown when the locked, freshly-read current status doesn't have the requested target as a whitelisted edge. |
| `app/Services/OrderStatusUpdateService.php` | New — mirrors `InventoryAdjustmentService`'s shape exactly: `DB::transaction()` → `Order::where(...)->lockForUpdate()->first()` → re-validate against `MerchantOrderStatusTransitions` (using the freshly-locked status, never the caller's possibly-stale view) → apply `status` (+ `cancelled_at` on cancellation) → save → commit. Directly satisfies `database-design.md`'s own documented concurrency requirement for the "order cancellation vs. payment webhook" race. |
| `app/Policies/OrderPolicy.php` | New — `viewAny`/`view`: any role with store access (identical shape to `ProductPolicy`/`InventoryPolicy`). `updateStatus`: `OrganizationRole::atLeast(StoreAdmin)` + store access + `organization.status === active`. No `create`/`delete` — no use case for either. Found and authorized correctly by Laravel's naming-convention auto-discovery (`Order` → `OrderPolicy`) with **no** `AppServiceProvider` registration needed, unlike Block 4B's `InventoryPolicy`/`ProductVariant` exception. |
| `app/Http/Middleware/` | **No new file** — Order reuses `tenant.merchant.store` verbatim, per the approved design. |
| `app/Http/Controllers/Merchant/OrderController.php` | New — `index`/`show`/`updateStatus` only. `{order}` resolved and store-scoped at the controller level (`resolveOrder()`), same discipline as `resolveProduct()`/`resolveVariant()`. `index()` validates an optional `status` query filter inline (`Rule::in` against `OrderStatus` values) rather than a dedicated FormRequest — the only query-param validation in this project so far, kept inline since it's a single field. `updateStatus()` catches `InvalidOrderTransitionException` and converts it to a clean 422, same pattern as `InventoryController`'s `InsufficientInventoryException` handling. |
| `app/Http/Requests/Merchant/OrderStatusUpdateRequest.php` | New — accepts **only** `status`, restricted to `Rule::in(['cancelled','processing','shipped','completed'])` — `pending`/`paid`/`refunded` are rejected here as a validation error, before `OrderStatusUpdateService` is ever reached. No `status_reason`/note field in this block (approved decision) — `status_reason` stays untouched for future functionality. |
| `app/Http/Resources/OrderResource.php` | New — single resource class (not separate List/Detail classes, approved decision) using `whenLoaded()` for `items`/`shipping_address`; `index()` doesn't eager-load either (list stays light — both keys are simply absent from list JSON), `show()` eager-loads both. No payment/refund data anywhere (approved decision). |
| `app/Models/{Order,OrderItem,OrderAddress,Customer}.php` | Added `HasFactory` to all four — strictly test-fixture infrastructure (approved decision); no customer-management feature. |
| `database/factories/{Customer,Order,OrderItem,OrderAddress}Factory.php` | New. `Order`/`OrderItem`/`OrderAddress` all have fully empty `$fillable`, unlike every prior factory in this project (`Product`/`Store`/`ProductVariant` all had *some* fillable fields) — `definition()` returns `[]` for all three, and every field is set via direct property assignment in an `afterMaking()` state method (`forStore()`/`forOrder()`) instead. `OrderFactory::forStore(Store $store)` auto-creates its own `Customer` via `Customer::factory()->forStore($store)`. Tests needing a non-default starting status set it directly after creation (`$order->status = OrderStatus::Paid; $order->save();`), the same pattern already used for `Organization::status` in `CreatesTenantFixtures`. |
| `routes/api.php` | `GET /api/stores/{store}/orders`, `GET .../orders/{order}`, `PATCH .../orders/{order}/status` — nested inside the existing `tenant.merchant.store` group, no `organization_id` anywhere in the URL, no separate cross-store aggregate route (an Owner reaches every store's orders the same way they already reach every store's products — by calling the store-scoped endpoint for each store they have access to). |

**Backend verification**: `php -l` clean on every new/modified file. `./vendor/bin/pint --test` — one style issue on first run (`OrderStatusUpdateService.php`, `unary_operator_spaces`/`not_operator...`), fixed via `pint` (not `--test`), re-verified clean across all 148 files. `php artisan test` — **201/201 passing** (143 prior + 58 new): `tests/Unit/Support/MerchantOrderStatusTransitionsTest.php` (17 — the 4 documented edges individually, 12 explicitly-named rejected transitions from the approved decisions, plus a full 7×7 sweep across every `OrderStatus` pair proving the whitelist is *exactly* the four documented edges and nothing more), `tests/Unit/Policies/OrderPolicyTest.php` (8 — Owner/Store-Admin/Staff × view/updateStatus, cross-org denial, the pending-org gate distinguishing view-allowed from updateStatus-denied), `tests/Feature/Merchant/OrderManagementTest.php` (27 — list/detail/RBAC, the `status` filter, `created_at` DESC ordering, 15/page pagination, all four valid transitions, `paid→cancelled` explicitly rejected, `pending→paid`/`any→refunded` rejected at the validation layer, skip-ahead/backward/terminal-state transitions rejected, immutable fields (`total`/`customer_id`/`store_id`/`organization_id`) and `status_reason` both proven unwritable through this endpoint, a `DB::listen()` proof of `SELECT ... FOR UPDATE` against `orders`, a forced-mid-transaction-failure rollback proof via `Order::saving()`, nonexistent-order 404, unauthenticated 401), `tests/Feature/Merchant/OrderTenantIsolationTest.php` (6 — cross-org order id 404, same-org-unassigned-store 403, an order from a different store in the same org rejected even though the caller could reach that other store directly, spoofed `store_id`/`organization_id` in the status body ignored, pending-org read-allowed/update-denied, cross-guard rejection). Live-stack verification through nginx: registered a merchant, activated the org, created a store, inserted a customer + a `pending` order + a `paid` order (with an item and a shipping address) directly via SQL (mirroring reality — no order-creation endpoint exists), confirmed list/filter/detail responses exactly match `OrderResource`'s shape, walked the full valid lifecycle `pending→cancelled` and `paid→processing→shipped→completed`, confirmed every explicitly-named invalid transition 422s (`paid→cancelled`, `pending→paid`, skip-ahead, terminal-state, `→refunded`), and confirmed a second organization gets a real 404 (not 403) reaching into the first org's store id. All smoke-test rows deleted from the dev database afterward.

**Frontend**: `src/services/ordersApi.ts`, `src/merchant/orders/useOrders.ts` (`useOrdersList`/`useOrder`/`useUpdateOrderStatus`, keyed `['stores', storeId, 'orders', ...]`), `src/merchant/orders/OrderListPage.tsx` (status filter dropdown, previous/next pagination from the paginator's own `meta`), `src/merchant/orders/OrderDetailPage.tsx` (items + shipping address, a single next-status action button computed from a small client-side mirror of `MerchantOrderStatusTransitions`, shown only when `role` is Owner/Store Admin). `StoreDetailPage` gained a second "Orders →" link alongside the existing "Products →" link. As with every prior block, the frontend role gate is presentation-only — the feature tests hit the raw HTTP endpoint directly and confirm 403 regardless of what the UI would show.

**Frontend verification**: `tsc -b` — no errors. `npm run lint` — 0 errors, same 2 pre-existing warnings, 0 new. `npm run build` — succeeds (328.26 kB JS / 19.25 kB CSS, gzipped 92.83 kB / 4.63 kB).

**Not performed**: interactive browser click-through — `claude-in-chrome` unavailable this session (declined earlier, instructed not to re-suggest), same disclosed limitation as every prior block. Route shell, the full live API contract (including the FOR UPDATE and rollback proofs and the full transition matrix), and build/lint/type-check were all verified by other means.

**Not yet done** (by design, later blocks): Checkout (order creation, `pending→paid`, `sale`/`refund` inventory mutations), Customers (beyond the test-fixture factory), Payments, Analytics, AI, the Product option/variant matrix.

## Not Started

- Database migrations for the full Database Design 2.5 schema, except `reports` — all 24 Phase 2A–2F tables are done (see Phase 2A/2B/2C/2D/2E/2F above). `reports` (AI-generated artifacts) is the only table not yet broken into its own phase. Note: `carts`/`cart_items` are **not** on this list at all — per Database Design 2.2, they are never migrated to MySQL; cart state is ephemeral (guest `localStorage`, authenticated Redis).
- ~~Platform Admin *organization review/approval* API surface~~ — **done, Phase 9A** (`/api/platform/organizations/*` — approve/reject/suspend/reactivate, platform-wide visibility, `a3a0ccf`). This bullet is left in place, struck through rather than deleted, per this document's "don't rewrite history" convention (see "Known Issues" for the broader staleness of this section, predating Phase 5, left otherwise untouched).
- Store-level differentiation between Store Admin and Staff capability (identical in Block 3 — both get `view`-only access to an assigned store; `OrganizationRole::atLeast()` exists for whichever later block first needs to tell them apart) and Eloquent global scopes that automatically apply the bound `TenantContext` to tenant-scoped queries generally (`StoreController` applies it by hand for now — Store is the only tenant-scoped resource queried through it so far)
- Store deletion's dependent-resource blocking check (`database-design.md`'s documented rule that soft-deleting a store is blocked while active Products/Customers/Orders exist under it) — the `DELETE /api/stores/{store}` endpoint exists and soft-deletes (Block 3), but there's nothing to check against yet since none of those resources exist; the guard's location is marked with a comment in `StoreController::destroy()`
- Customer authentication (register/login/logout/password reset) and its guard — Platform Admin's (Block 1) and merchant's (Block 2) auth/guard/registration are both done; no `customer` guard exists yet
- The Product option/variant matrix (`ProductOption`/`ProductOptionValue`/`ProductVariantOptionValue`/`ProductImage`, multiple variants per product, `option_signature` generation, variant option-value cardinality enforcement, product-soft-delete query scoping) — Block 4A deliberately shipped Product + exactly one default variant only (`option_signature: ''`), per the approved design's smaller-slice recommendation; no `VariantOptionService` exists
- Cart / checkout business logic — will use browser `localStorage` (guest) and Redis (authenticated, server-derived tenant/customer key) per Database Design 2.2, not MySQL `carts`/`cart_items` tables; order **creation** (checkout's job) and the `pending→paid` webhook transition both depend on this
- ~~System-driven (`sale`/`refund`) inventory mutations~~ — **done**: `checkout`/`release` reasons since Phase 5, `refund` reason since **Phase 9D** (`StripePaymentWebhookService::restoreInventoryForRefund()`, `0b0e59e`) — `InventoryAdjustmentService` (Block 4B) was already reason-agnostic and ready for this. Struck through, not deleted, per this document's "don't rewrite history" convention.
- ~~Stripe integration (PaymentIntents, webhooks, refunds)~~ — **done**: PaymentIntents/webhooks since Phase 5, refunds since **Phase 9D** (`0b0e59e`). Struck through, not deleted (see "Known Issues" for the broader staleness of this section, predating Phase 5, otherwise left untouched).
- Business queue jobs
- Redis analytics caching and rate limiting rules
- Analytics service/endpoints
- AI agent, AI tools, and Laravel AI SDK integration
- Report generation
- Business REST API endpoints (checkout/customers/analytics/AI/reports) — `/api/auth` (merchant), `/api/platform/auth` (Platform Admin), `/api/stores` (Block 3), `/api/stores/{store}/products` (Block 4A), `/api/stores/{store}/variants/{variant}/inventory` (Block 4B), and `/api/stores/{store}/orders` (Block 4C — list/detail/status only, no creation) are done
- Admin back-office business feature UI and storefront UI (React) — the Platform Admin back-office application shell (layout, protected routing, login, dashboard placeholder) exists per Phase 4 Block 1; Organizations/Stores/Users/Products/Orders/Customers/Inventory/Analytics/AI are currently "Coming Soon" placeholders only. Merchant authentication UI exists per Block 2b, merchant Store Management UI (`/merchant/stores*`) exists per Block 3, merchant Product Management UI (`/merchant/stores/:storeId/products*`) exists per Block 4A, and Inventory visibility/adjustment is embedded in the Product detail page per Block 4B (deliberately not a separate top-level screen), but no Orders/Customers/Analytics/AI UI and no storefront UI exist at all.
- Business test coverage beyond Platform Admin + merchant authentication + Store/Product/Inventory Management (PHPUnit for Order/Customer/etc. flows, Playwright E2E) — 143 backend tests exist total (12 Platform Admin, 35 merchant auth/tenant-isolation/`OrganizationPolicy`, 30 `OrganizationRole`/`TenantAccess`/`StorePolicy`/Store Management/store-tenant-isolation, 36 `ProductPolicy`/Product Management/product-tenant-isolation, 30 `InventoryPolicy`/Inventory Management/inventory-tenant-isolation); no frontend tests exist
- CI/CD (GitHub Actions)
- Deployment (Laravel Cloud)

## Infrastructure (Phase 1)

Complete and verified by actually running every command below — not assumed.

**Technology (as actually installed, verified inside the running containers):**

| Component | Version |
|---|---|
| PHP | 8.5.9 |
| Laravel | 13.29.0 |
| Composer | 2.10.2 |
| React | 19.2.8 |
| React DOM | 19.2.8 |
| TypeScript | 6.0.3 |
| Vite | 8.2.2 |
| Node.js | 24.19.0 (`node:24-alpine`) |
| MySQL | 8.4.11 |
| Redis | 7.4.11 |
| Nginx | 1.30.4 |
| Docker | 29.7.2 |
| Docker Compose | v5.4.0 |

**Infrastructure — DONE (verified):**
- Docker Compose stack (`app`, `nginx`, `mysql`, `redis`, `queue`, `node`) — all 6 services build and run; `mysql`/`redis` pass healthchecks before dependents start.
- PHP-FPM (`app`) — PHP 8.5.9 confirmed, all required extensions present (PDO, pdo_mysql, mbstring, bcmath, intl, xml, ctype, fileinfo, tokenizer, openssl, pcntl, redis/PhpRedis, zip).
- Nginx — serves Laravel on `http://localhost:8080` (HTTP 200 confirmed); `.env`, `.git`, `vendor`, `composer.json` all confirmed returning 404, not served.
- MySQL — Laravel connects, runs migrations, and executes real queries (`php artisan db:show`, `php artisan migrate`, a live `SELECT 1+1` all confirmed) — not just "container is running."
- Redis — Laravel connects via the real `Redis` facade (`ping()` and a `set`/`get` round trip both confirmed via PhpRedis), not faked.
- Queue worker — a temporary `TestInfrastructureJob` was dispatched and confirmed processed (Laravel → Redis → queue worker → job executed, verified via both worker logs and an independent Redis marker written by the job); removed after verification.
- Laravel — `php artisan --version` confirms Laravel Framework 13.29.0; default framework test suite passes (2 tests, 2 assertions).
- React/Vite — serves on `http://localhost:5173` (HTTP 200 confirmed, HMR client + React refresh present); exact installed versions confirmed as above.
- Sanctum foundation — `laravel/sanctum` v4.3.3 installed, `php artisan install:api` run (creates `routes/api.php`, publishes the `personal_access_tokens` migration). No login/register/logout/RBAC/policies implemented — foundation only, as scoped.

**Not part of Phase 1 (unchanged, still not started):** database migrations for the approved business schema, TenantContext, RBAC, authentication flows, and everything else listed under "Not Started" above.

**Project structure**: Laravel lives entirely under `backend/` (not the repo root); React lives entirely under `frontend/`. Docker/nginx/mysql config lives under `docker/`.

**Root `.env` vs. `backend/.env`** — the chosen simplest-maintainable pattern: the project-root `.env` (gitignored; `.env.example` committed) is the single source of truth for MySQL credentials (`MYSQL_DATABASE`/`MYSQL_USER`/`MYSQL_PASSWORD`/`MYSQL_ROOT_PASSWORD`). `docker-compose.yml` both configures the `mysql` service from these values *and* injects the same values as real container environment variables into `app`/`queue` (`DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD`, plus `DB_HOST=mysql`/`REDIS_HOST=redis`/`QUEUE_CONNECTION=redis`). Because Laravel's `.env` loader (`vlucas/phpdotenv`) never overwrites a variable that's already set in the real process environment, these injected values always win — `backend/.env`'s own `DB_*` lines are inert fallbacks (kept in sync for clarity, and for the rare case of running artisan outside `docker-compose exec`), never a second source of truth that could drift from the root `.env`.

**Port bindings**: Nginx `8080` (all interfaces, browser-facing) · Vite `5173` (all interfaces, browser-facing) · MySQL `127.0.0.1:3306` only (local dev-tool access, e.g. TablePlus/DBeaver — never exposed beyond localhost) · Redis has no host port mapping at all (fully internal to the Docker network) · PHP-FPM (`app:9000`) is never exposed to the host, only reachable from `nginx` over the internal network.

**MySQL data persistence**: `mysql_data` is a named Docker volume — survives `docker compose down` and container recreation (confirmed: data survived an app/queue rebuild+recreate during this session). `docker compose down -v` would delete it and should not be used as a routine shutdown command.

**Development commands:**

```
Start:            docker compose up -d
Stop:              docker compose down            (never `down -v` unless intentionally wiping the DB)
Status:            docker compose ps
Logs (all):        docker compose logs
Logs (one):        docker compose logs -f queue
Laravel shell:     docker compose exec app bash
Artisan:           docker compose exec app php artisan ...
Composer:          docker compose exec app composer ...
Frontend/npm:      docker compose exec node npm ...
Backend tests:     docker compose exec app php artisan test
```

Laravel: http://localhost:8080 — React/Vite: http://localhost:5173

**Warnings / notes for the next session:**
- A real, now-fixed issue: PHP-FPM's `www-data` worker user cannot write to `storage/`/`bootstrap/cache` on this Windows → Docker Desktop (WSL2) bind mount (files land owned in a way non-root can't write to, regardless of `chmod` from the Windows side — `chmod` via Git Bash on the host is a no-op on NTFS). Fixed via `docker/php/entrypoint.sh`, which `chmod -R 777`s those two directories as root on every container start before dropping to the configured command. This is a local-dev-only concession specific to the Windows bind-mount path — irrelevant to a real (non-bind-mounted) deployment. Attempting to instead run the php-fpm pool itself as root was tried first and rejected: PHP-FPM hard-refuses to start with `user`/`group` set to root ("please specify user and group other than root").
- A transient, self-resolving error was observed the first time the `queue` container started before `php artisan migrate` had run (Laravel's default `CACHE_STORE=database` means the queue worker's restart-signal check needs a `cache` table that didn't exist yet). Resolved automatically once migrations ran; not an infrastructure defect, just documented here so it isn't mistaken for one if seen again on a completely fresh `docker compose up` before ever running `php artisan migrate`.
- Laravel's default skeleton ships auto-generated `CLAUDE.md`/`AGENTS.md` files (instructing an agent to install `laravel/boost`) inside a fresh `backend/` — these were deleted after scaffolding since they weren't requested and this repo's root `CLAUDE.md` already governs the whole project.
- `backend/database/database.sqlite` (Laravel 11+'s default local-dev DB) was removed since the project targets MySQL, not SQLite.
- Docker itself is installed at a non-default path on this machine (`C:\Users\guoch\AppData\Local\Programs\DockerDesktop\`, not `Program Files`) and is not on this shell session's PATH — commands in this session used the full path. A fresh terminal (or a PATH update) should resolve `docker` normally going forward.
- `docker compose exec` defaults to `root` unless `-u` is passed (there's no `USER` directive in the Dockerfile) — this is fine for local dev/debugging exec sessions; the actual php-fpm request-handling workers run as `www-data` per the pool config.

## Important Architectural Decisions

These are established and must not be silently changed by a future session. See `docs/architecture/system-architecture.md` for full detail.

- Laravel is the only component with database access.
- React communicates with Laravel exclusively through the API — no direct DB/Stripe/LLM access from the frontend.
- MySQL is the durable source of truth.
- Redis is used for cache, queue backend, rate limiting, and authenticated-customer cart storage (ephemeral, TTL'd, server-derived tenant/customer key — Database Design 2.2), but is not a source of truth for anything, including the cart role.
- **Platform Admin is a third, structurally separate identity domain** — `platform_admins`, outside the Organization → Store → merchant-user hierarchy, with its own Sanctum guard. It is never merged into `users`/`organization_user`/`store_user`, never granted a role in the merchant RBAC ladder, and never evaluated against a tenant `TenantContext` (it isn't scoped to any organization/store).
- **Cross-identity token isolation uses one Sanctum guard per identity, each bound to its own provider** (`platform_admin` → `platform_admins`, `merchant` → `users`; a `customer` guard will follow the same pattern) — established in Phase 4 Block 1. Sanctum's own `Guard::hasValidProvider()` rejects a token whose `tokenable` model doesn't match the resolving guard's provider model, so this isolation is enforced by the framework itself, not custom application code. Do not consolidate these back into the single default `auth:sanctum` guard, which has no provider restriction and would accept any identity's token.
- Tenant context (organization, store, user, role) is resolved server-side, once per request, and bound for reuse by controllers, policies, scopes, and AI tools.
- AI tools must never receive organization/store identity from the LLM — tenant context is injected server-side into every tool call.
- The REST API and AI tools share the same authorization rules and the same underlying application services — there is no separate, looser data-access path for AI.
- Stripe webhook processing is signature-verified and idempotent.
- Webhook event insertion (`stripe_webhook_events`) and the order's `pending → paid` state transition must occur within the same database transaction.
- `orders.status` (fulfillment lifecycle) and `payments.status` (payment-attempt lifecycle) are separate state machines — never merge them into a single column; payment status at the order level is always derived, never stored as `orders.payment_status`.
- Inventory mutation uses row-level locking (`SELECT ... FOR UPDATE`) plus an append-only transaction ledger (`inventory_transactions`); `inventory.quantity_on_hand` is a maintained materialization of that ledger.
- Inventory is never directly modified by request handlers — all mutation goes through one locked service method.
- Order totals are always calculated server-side, never accepted from the client.
- No guest checkout for MVP — customer accounts are required. **Clarification (Database Design 2.2)**: guest browsing and guest cart-building (via `localStorage`) are permitted; "no guest checkout" refers only to the checkout/payment step itself, which always requires an authenticated `customers` account.
- **Carts are not persisted in MySQL for MVP** (Database Design 2.2) — guest: `localStorage`; authenticated: Redis. Checkout treats cart contents as untrusted input and always revalidates `product_variant_id`/quantity and recalculates price/totals against MySQL. Do not reintroduce a MySQL `carts`/`cart_items` table without explicitly reconsidering and re-approving this architecture.
- MVP does not use inventory reservations/soft-holds — inventory decrements only at the `paid` transition.
- Analytics use direct transactional queries against MySQL with Redis cache-aside caching — no pre-aggregated/materialized rollup tables for MVP.
- AI Q&A and Insights are not persisted for MVP — computed on demand, optionally Redis-cached.
- AI Reports are persisted (the one durable AI-related table).

## Database Decisions

Full detail lives in `docs/database/database-design.md` — this is a summary, not a substitute. Reflects **Database Design 2.5 (APPROVED)**.

### Final Resolution — Order/Payment Status Separation (CLOSED)

- `orders.status` and `payments.status` are **separate state machines** — confirmed structurally, not just by convention: one order has exactly one fulfillment status at a time, but can have many payment attempts (1 order : N `payments` rows, one per PaymentIntent including retries), each running its own independent instance of the payment lifecycle.
- `orders.status` represents the **order/fulfillment lifecycle**: `pending → paid → processing → shipped → completed`, with `cancelled`/`refunded` branches. `paid` and `refunded` are the order's own fulfillment milestones, not a mirror of payment-attempt detail.
- `payments.status` represents the **individual payment-attempt lifecycle**: `requires_payment → processing → {succeeded | failed}`, or `→ canceled`. Terminal states are never reopened; a retry is always a new row.
- **Payment status at the order level is derived, never stored.** There is no `orders.payment_status` column. When an order needs to display a payment status, it is computed from the order's associated payment attempt(s) — satisfying PRD.md §7.1's "Payment Status" field as a view-level concept, not a second independently-writable column that could drift from `payments`.
- **A payment reaching `succeeded` is the sole controlled trigger for `orders.status: pending → paid`.**
- **A refund reaching `succeeded` is the sole controlled trigger for `orders.status: {paid,...} → refunded`.**
- No other payment or refund state (`processing`, `failed`, `canceled`, refund `pending`/`failed`) ever mutates `orders.status`.

Full transition tables, webhook/refund/failed-payment/retry behavior, and diagrams: `docs/database/database-design.md` §"Order & Payment State Models — Authoritative Interaction Model."

### Platform Admin (Database Design 2.1, approved 2026-08-26)

Identified and corrected before any Phase 2 migration was written — PRD.md originally described only Customer / Store Admin / Organization Owner (§3.1–3.3), with no platform-operator role. `platform_admins` is a new, standalone table:

- No `organization_id`/`store_id` — ever. Never referenced by `organization_user`/`store_user` as a member.
- No `role` column for MVP — a single flat Platform Admin capability set, not a tiered platform-role system (nothing requires that yet).
- `organizations` gains a lifecycle: `status` (`pending`→`active`, or `rejected`/`suspended`), default `pending` — **a new organization requires Platform Admin approval before it can operate.**
- Explicit per-action audit column pairs (not a single `reviewed_by`/`reviewed_at`, which would lose history across repeated status changes): `approved_at`/`approved_by_platform_admin_id`, `rejected_at`/`rejected_by_platform_admin_id`, `suspended_at`/`suspended_by_platform_admin_id`, plus `status_reason`. All three `*_by_platform_admin_id` columns are nullable FKs → `platform_admins.id`, `ON DELETE SET NULL`. Rejection got its own audit pair (not just `status_reason`) specifically for symmetry/accountability with approval — see `docs/database/database-design.md` for the full reasoning.
- No separate `audit_logs` table added — these column pairs are the only lifecycle-audit facts currently required.
- Three identity tables now exist platform-wide: `platform_admins`, `users` (merchant), `customers` (shopper) — each with its own Sanctum guard, all sharing Sanctum's existing polymorphic `personal_access_tokens` table (no schema change needed there).

Full schema, rationale, and ERD: `docs/database/database-design.md`.

### Cart Architecture (Database Design 2.2, approved 2026-08-26)

Identified and approved before Phase 2D (Cart & Orders, not yet started) was reached — no migration existed for `carts`/`cart_items` at the time of this correction, so nothing needed to be reverted.

- **No `carts`/`cart_items` MySQL tables.** Cart state for MVP is ephemeral: guest carts live in browser `localStorage`; authenticated-customer carts live in Redis, keyed by a **server-derived** tenant/customer namespace (never client-supplied), with a TTL.
- **Redis remains non-durable, not a source of truth** — the cart role is held to the same standard as Redis's other three roles. Cart loss (eviction, TTL expiry, cleared browser) is an accepted, low-stakes failure mode; the durable record begins at the **pending order**, not at any cart representation.
- **Cart contents are untrusted input.** Checkout reads only `product_variant_id`/quantity from the cart; price, availability, inventory, and totals are always revalidated/recalculated from MySQL — reinforcing, not changing, the existing "order totals always server-side" rule.
- **No FK-based cleanup anymore** — a deleted/archived `product_variant` is not automatically pruned from a cart the way the old `CASCADE` FK on `cart_items` would have done; checkout (and any cart-read path) must defensively handle a stale `product_variant_id` reference.
- **Intended concurrency primitive (not yet built)**: a Redis Hash per cart (field = `product_variant_id`, value = quantity), mutated via atomic `HINCRBY` — the same no-lost-update guarantee the old `unique(cart_id, product_variant_id)` + upsert pattern provided.
- **Merge-on-login strategy** and **Redis eviction/isolation policy** (cache role vs. cart role sharing one Redis instance) are both **intentionally left as future implementation/operational decisions** — not resolved now.
- **No guest checkout, unchanged**: guest browsing/cart-building is permitted; checkout still requires an authenticated `customers` account.
- **Do not reintroduce a MySQL `carts`/`cart_items` table** without explicitly reconsidering and re-approving this architecture.

Full schema-level rationale: `docs/database/database-design.md` §"Cart — intentionally NOT a MySQL table." Full architectural detail: `docs/architecture/system-architecture.md` §7 "Cart Architecture."

- **Tenant hierarchy**: `Organization → Store → store-scoped resources`. Platform Admin (`platform_admins`) sits above this hierarchy, not inside it.
- **Tenant columns**: every top-level, independently-queried tenant-scoped table carries `organization_id` (and `store_id`, where applicable) directly — `products`, `categories`, `orders`, `payments`, `refunds`, and now `product_variants` (promoted in 2.0, since variants are queried independently — SKU lookups, low-stock reports — not only reached through a parent). Pure child/detail tables (`order_items`, `product_images`, `inventory_transactions`, `product_options`, `product_option_values`, `product_variant_option_values`, `customer_addresses`, `payment_methods`, `order_addresses`) rely on their parent's tenant columns instead.
- **Users vs. customers**: two separate identities, unchanged.
- **RBAC structure / store-user assignment**: unchanged — `organization_user` (one org per user for MVP), `store_user`.
- **The sellable unit is the Product Variant, not the Product** (Database Design 2.0's central change). `products` is catalog/marketing data only — no price, no SKU. `product_variants` carries the canonical `sku` and `price`; a product without meaningful variation still gets exactly one "default" variant, so there is only ever one inventory/pricing system, never two competing ones. `order_items`, `inventory`, and `inventory_transactions` all reference `product_variant_id` (cart state also references it, ephemerally, outside MySQL — see Cart Architecture above).
- **Product options/variants are relational, not JSON**: `product_options` → `product_option_values` → `product_variants` → `product_variant_option_values` (pivot). Options are scoped per-product (no shared global option library — avoids building a full PIM). Duplicate variant combinations are prevented by a database-enforced `unique(product_id, option_signature)`, where `option_signature` is an application-maintained, sorted signature of the variant's option-value set.
- **Order creation before payment**: unchanged.
- **Order item snapshots**: `order_items` now also snapshots `selected_options` (JSON — a frozen historical snapshot, not a live relational structure) alongside `product_name`, `sku` (the variant's SKU), and `unit_price`. Both `product_id` and `product_variant_id` are nullable with `ON DELETE SET NULL`.
- **Customer addresses**: `customer_addresses` (reinstated — live, mutable, reusable) is distinct from `order_addresses` (new — immutable per-order shipping snapshot, replacing the old inline `shipping_*` columns on `orders`). No billing address is stored for MVP; Stripe's own payment collection handles billing-address/AVS needs.
- **Stripe customer identity & saved payment methods**: `customers.stripe_customer_id` (nullable, unique). `payment_methods` stores only Stripe identifiers and non-sensitive display metadata (brand/last4/expiry) — never card numbers or CVV — and is kept structurally separate from `payments` (saved/reusable data vs. one payment attempt).
- **Payment and refund structure**: unchanged in shape; `payments` gains a nullable `payment_method_id` reference. MVP scope remains full-order refunds only, but the schema is already shaped so partial refunds could be added later without restructuring (see inventory idempotency fix below).
- **Inventory design**: `inventory` (one row per **variant**, current `quantity_on_hand`, plus a new nullable `low_stock_threshold` for derived low-stock queries — no separate low-stock table) plus `inventory_transactions` (append-only ledger: `sale`/`restock`/`adjustment`/`refund`).
- **Inventory idempotency key corrected**: `inventory_transactions` is now keyed by `(order_item_id, reason)`, not `(order_id, reason)`. The old key was a real bug once an order can contain multiple line items — it could only guarantee one `'sale'` row per whole *order*, not per line item, meaning a multi-item order would either fail to insert its second item's transaction or silently under-track inventory. This was found and fixed during the Database Design 2.0 pass, not merely renamed.
- **Stripe webhook idempotency**: unchanged, confirmed still sufficient after 2.0's additions.
- **Order numbers, enum representation, MySQL version requirement**: unchanged (ULID order numbers; varchar + PHP enum casts, not MySQL `ENUM`; MySQL ≥8.0.16).
- **Soft-delete strategy — now resolved (was open)**: mutate-on-delete. A soft-deleted row's unique value (`email`, `slug`, `sku`) is suffixed at delete time (e.g. via a model `deleting` observer), freeing it for reuse rather than permanently reserving it. Applied to `organizations`, `stores`, `users`, `customers`, `products`, `product_variants`, `categories`.
- **Cascading soft-deletes — now resolved (was open)**: soft-deleting a `store` does **not** cascade to its `products`/`categories`/`customers`. Deactivation is blocked at the application layer while active resources exist, rather than silently cascading.

### Catalog Design Review Corrections (Database Design 2.3, approved 2026-08-27)

Identified and resolved during the Phase 2C design review — before any Phase 2C migration was written, the same pattern as the Platform Admin (2.1) and Cart Architecture (2.2) corrections before their respective phases. No migration existed for any of the 7 catalog tables at the time, so nothing needed to be reverted.

- **`categories.deleted_at` added — documentation correction, not a design change.** The design review surfaced an internal contradiction in `database-design.md`: the `categories` table's own "Rejected" note said "categories already soft-delete," and the Soft-Delete Strategy section already listed `categories.slug` in its mutate-on-delete column list — but the `categories` column table itself never actually listed a `deleted_at` column. Resolved in favor of the two statements that assumed soft-delete: `categories` formally has `deleted_at` (nullable timestamp) and follows the same mutate-on-delete `slug`-suffixing pattern as `products`/`stores`. No `status` column was added (re-confirmed rejected, unchanged).
- **Variant option-value cardinality — documented as an application-layer invariant, not a schema change.** `product_variant_option_values`' constraints prevent a duplicate link but not an invalid *set* — nothing stops a variant from linking to two values of the same option, or omitting a required option. Decision: for a given variant, at most one selected `product_option_value` per `product_option`; if the product defines options, a variant must have exactly one value for each; the option-less "default" variant is the sole exception. This must be enforced by the service layer atomically together with `option_signature` recomputation whenever a variant's option-value links change. `option_signature`'s DB-level unique constraint remains the mechanism that prevents duplicate *combinations* — this invariant is a separate, complementary rule about what makes a combination *valid* in the first place.
- **Product soft-delete vs. active variants/options — resolved, deliberately asymmetric to the store-level rule.** A `product` may be soft-deleted even while it still has active `product_variants`/`product_options` — this is **not** blocked at the application layer, unlike store deactivation (§ Cascading soft-deletes, above). Rationale: the store-level block protects other tenants' independently-visible top-level resources (products, categories, customers); a product's variants/options are pure children with no independent visibility to protect, so there's nothing distinct for a block to guard. On product soft-delete, child rows are neither cascaded nor mutated — they persist untouched (preserving historical order/inventory references) but become unreachable through normal storefront/catalog queries, which scope on `products.deleted_at IS NULL`. No new DB constraint, column, or migration.

Full detail and rationale for all three: `docs/database/database-design.md` (Database Design 2.3 changelog entry, the `categories` table definition, the `product_variant_option_values` section, and "Soft-Delete Strategy").

### Phase 2D Orders Design Review Resolutions (Database Design 2.4, approved 2026-08-27)

Identified and resolved during the Phase 2D design review — before any Phase 2D migration was written, the same pattern as the Platform Admin (2.1), Cart Architecture (2.2), and Catalog (2.3) corrections before their respective phases. `orders`/`order_items`/`order_addresses` were already fully specified since Database Design 2.0 and unchanged by 2.1–2.3; this pass validated that existing design against a full 19-point review and resolved four open questions (G1–G4):

- **G1 — No `orders.shipping_total` column (confirmed, no schema change).** PRD §7.1's authoritative order-field list omits shipping; PRD §29 excludes Shipping Provider Integration from MVP scope entirely. "Shipping information" in PRD §5.5's checkout summary means the shipping *address* (already captured by `order_addresses`), not a cost line. The existing (unmodified since 2.0) schema was already correct; this makes the omission an explicit, confirmed decision rather than an unreviewed gap.
- **G2 — `paid → cancelled` recorded as an explicitly disallowed transition (authoritative business invariant, no schema change).** `cancelled` is reachable only from `pending`. A `paid`-or-later order can only be stopped via the refund flow. Not a new rule — already implied by the existing transition table — but now named explicitly under "Order & Payment State Models" §3 because it constrains future Store Admin order-action UI (a "Cancel" action must be unavailable, or must route to "Refund," once an order is `paid` or later).
- **G3 — Late-payment / inventory-oversell scenario recorded as a known, explicitly deferred Phase 2F decision (no schema change, no behavior implemented).** Phase 2D creates `pending` orders with no inventory reservation (MVP has no soft-hold system), so multiple pending orders can reference the same remaining stock; if a payment later succeeds against depleted inventory, the resulting webhook-level behavior (auto-refund, manual reconciliation, or otherwise) is explicitly **not** decided now. Documented under "Concurrency Review" item 10 and "Open Decisions" in `database-design.md` so it isn't lost before Phase 2F is designed.
- **G4 — `orders`' customer-history index widened**: `(store_id, customer_id)` → `(store_id, customer_id, created_at)` — supports the customer `/orders` history query (newest-first) without a filesort, same tenant/customer lookup prefix preserved. The one genuine schema change in this pass.

Also applied: `docs/architecture/system-architecture.md` §4's checkout transaction-boundary sentence was extended to explicitly name `order_addresses` alongside `orders`/`order_items` — a documentation-completeness fix, not an architectural change (the three tables were already implicitly expected to be created atomically; the sentence just didn't enumerate all three).

Full detail and rationale for all four: `docs/database/database-design.md` (Database Design 2.4 changelog entry, the `orders` table definition, "Order & Payment State Models" §3, "Concurrency Review" item 10, and "Open Decisions").

## Open Decisions

**No items open. All six items recorded here are now closed.**

- **CLOSED**: PRD.md §7.1 "Payment Status" vs. "Order Status" — see "Final Resolution" under Database Decisions above.
- **CLOSED (design: Database Design 2.6; implementation: Phase 9E-1, G3-A)**: G3, the late-payment/inventory-oversell webhook-level consequence. This entry previously read "OPEN, re-scoped to a future service-design phase" — that wording had gone stale and is corrected here, not superseded by a new decision. Two distinct things were true simultaneously by the time Phase 9E-1 began: (1) `database-design.md`'s own authoritative "Open Decisions" section had already recorded the *design* question as CLOSED as part of Database Design 2.6 (inventory is claimed atomically at checkout, not at payment success, which structurally eliminates the original "payment succeeds after stock is already depleted" race this item was opened to track — confirmed by code inspection: `CheckoutOrderCreationService`/`InventoryAdjustmentService` already claim and lock inventory inside the same transaction as pending-order creation, before Stripe can ever resolve); (2) a narrower, genuinely live implementation gap remained — `database-design.md` §14's explicit promise that a late `payment_intent.succeeded` webhook landing on an already-non-`Pending` Order would be "logged for manual reconciliation" with an "alarm-worthy" `status_reason` had never actually been implemented for the **merchant-cancellation** case (verified via code inspection and a passing test, `test_succeeded_webhook_does_not_regress_a_non_pending_order`, that predates Phase 9E-1 and asserted only silence). That implementation gap is what Phase 9E-1 (G3-A) closed — see its entry above and commit `df6ce9a`. (§14's *other* named case, the expiry sweep, was separately confirmed still-uncovered by Phase 9E-1 and closed by Phase 9E-3 — see below.)
- **CLOSED (design: Phase 9E-1 proposal stage; implementation: Phase 9E-2, G3-B)**: manual merchant compensation for the G3-A merchant-cancellation alarm. Previously read "OPEN, new... not reviewed, approved, or implemented." Now implemented exactly as originally analyzed: a narrowly extended `RefundService` eligibility condition (`isRefundableOrderState()`) plus the required `restoreInventoryForRefund()` double-credit guard — full-refund-only, Stripe-PaymentIntent-based, reconciled through the existing `refund.created`/`refund.updated` webhook path, never a general rule permitting refunds on arbitrary `Cancelled` orders. See the "Phase 9E-2" entry above and commit `84a73c3`.
- **CLOSED (design: Phase 9E-3 Discovery/Design; implementation: Phase 9E-3)**: the *other* half of the G3 gap — a late `payment_intent.succeeded` webhook for a Payment already marked `Canceled` by the expiry sweep (`database-design.md` §14's named expiry-sweep race) was, before this phase, silently absorbed by the pre-existing Payment terminal-status guard, never reaching any G3-A alarm logic. Closed by a narrow, separate detection branch: `Order.status_reason` becomes `payment_succeeded_after_expiry_cancellation` and a `Log::critical()` is emitted, while **`Payment.status` deliberately remains `Canceled`** — the terminal-status invariant was evaluated and explicitly *not* relaxed (a design option considered and rejected — see the "Phase 9E-3" entry above for the full reasoning). This case is deliberately not eligible for the G3-B refund flow. See the "Phase 9E-3" entry above and commit `84a73c3`.
- **CLOSED (design: Phase 9E-4 Discovery/Design; implementation: Phase 9E-4)**: a compensation/recovery path for the expiry-sweep late-success case. Previously read "OPEN, new... not reviewed, approved, or implemented." Resolved as **refund-only, admin-triggered** (re-fulfillment/re-claim was explicitly evaluated and rejected — the released inventory cannot be reliably reclaimed under the existing `dedup_key` scheme, and there is no reliable way to know whether it is even still available): a new, structurally separate `RefundService::refundLateSucceededExpiredPayment()` entry point independently re-verifies `Order`/`Payment` state and issues a Stripe refund against the still-`Canceled` Payment's PaymentIntent, reconciled through the existing `refund.created`/`refund.updated` webhook path exactly like G3-B, resolving `status_reason` to a new, distinct `payment_refunded_after_expiry_cancellation` literal. **`Payment.status` remains `Canceled`** throughout — never corrected to `Succeeded` — preserving the terminal-status invariant this entire G3 family has consistently protected. See the "Phase 9E-4" entry above for full detail.
- **CLOSED (Block 2)**: `organization_user.role`'s exact string value set — approved as `owner`/`store_admin`/`staff`, implemented as the `OrganizationRole` backed enum and cast on `OrganizationUser`. See "Block 2 — Merchant Identity, Organization Registration & RBAC Foundation" under "Phase 4 (Back Office Feature Blocks)" below.

## Known Issues

None currently blocking. One non-blocking local-environment gap and two minor documentation-staleness items are worth a cleanup pass at some point, but none blocks implementation:
- **Local dev environment's `STRIPE_SECRET` is blank** — `payments:expire-stale` (Phase 6) currently errors at runtime here (`Stripe\Exception\InvalidArgumentException`), exactly as a live checkout/retry attempt already would outside the test suite; this is pre-existing (nothing before Phase 6 ever resolved `StripePaymentIntentGateway` outside tests) and not introduced by any specific change. A real Stripe test-mode key in `backend/.env` is needed to exercise checkout/retry/expiry end-to-end in this environment.
- `docs/architecture/system-architecture.md` §5 still describes "one `inventory` row per product" — now stale, should read "per product variant" per Database Design 2.0.
- `docs/architecture/architecture-review.md` (a historical review record, intentionally not rewritten) contains a couple of now-superseded schema references (e.g. a `products.sku` constraint, the old `(order_id, reason)` inventory idempotency key) from before Database Design 2.0 — a short superseding note has been added there rather than altering its historical findings.
- **RESOLVED by the Phase 8 Block 8B Revision (2026-09-06; not yet committed).** Originally flagged by the 2026-09-06 documentation-reconciliation pass: Block 8B's storefront cart deviated from the approved Cart Architecture (Database Design 2.2 / CLAUDE.md / `system-architecture.md` §7) — a single `localStorage` cart shared by guest and authenticated customers, no `/api/cart` endpoint, no server-side merge-on-login. Closed by implementing the originally-approved design as-specified (Redis-backed authenticated cart via `App\Services\CartService`, `/api/cart*` endpoints, login-time merge, TTL) rather than by revising the documentation to match the deviation — see "Phase 8 Block 8B Revision — Authenticated Redis Cart" above and `system-architecture.md` §7 for the implemented behavior. This entry is left in place, marked resolved, rather than deleted, per this project's own "don't rewrite history" convention.
- **Open, noted by the same pass: Phase 8's Playwright suite (24/24 passing) mocks the backend API for every spec, and mocks Stripe.js for the checkout specs** — none of it is a verified live-integration test against the real Docker backend or a real Stripe test-mode account, and no manual browser walkthrough of the storefront has been recorded.
- **RESOLVED (2026-09-12, v0.1.1 documentation pass).** `README.md` previously described the project as being in an early "Infrastructure / bootstrap stage" and did not mention Phases 2–9F, Stripe, RBAC, cart architecture, or Analytics at all. It has been rewritten to describe the actual current implementation (multi-tenancy/RBAC, storefront, checkout/Stripe, orders, refunds, G3 edge-case handling, Analytics v1) and explicitly states that the AI Assistant is unimplemented. The "## Not Started" section further below **in this file** still predates Phase 5 in places and is left as historical record per this document's "don't rewrite history" convention — cross-reference the canonical Phase Roadmap table at the top of this document for current status, not that section.
- **RESOLVED (2026-09-12).** A `php artisan test` run inside the `app` Docker container silently connected to the real development MySQL database instead of the isolated SQLite `:memory:` database, because Docker-injected `DB_*` environment variables took precedence over PHPUnit's non-forced `<env>` overrides — see the "PHPUnit test-database-isolation incident and fix" entry above (under "Current Task") for full root-cause detail and verification. Fixed via `force="true"` on the two affected `phpunit.xml` entries; verified against the full 607-test suite with before/after MySQL row-count and migration-batch checks.

## Files and Documentation

| File | Purpose |
|---|---|
| `PRD.md` | Product requirements and MVP scope. Source of truth for *what* the product must do. |
| `CLAUDE.md` | Operating guidance for Claude Code in this repository — architecture summary, corrected technical decisions (Sanctum, payment flow, commands, frontend state, etc.), development rules, code-quality conventions, tech stack. |
| `docs/architecture/architecture-review.md` | The authoritative review record — critical issues, recommended improvements, MVP decisions intentionally accepted, security risk ranking, scalability notes, interview/portfolio value, final score. |
| `docs/architecture/system-architecture.md` | The resulting approved system design — component responsibilities, multi-tenancy, auth/RBAC, order/payment lifecycle, inventory/concurrency, Redis/queues, analytics, AI agent, API surface, frontend architecture. |
| `docs/database/database-design.md` | The approved database schema (Database Design 2.5) — every table, ERD, state models, constraints, and concurrency considerations. See "Open Decisions" for what remains open. |
| `docs/development/project-status.md` | This file — persistent development-state tracker, read/updated across sessions instead of relying on conversation memory. |
| `docs/development/development-environment.md` | Permanent record of the verified local Docker development environment — exact versions, architecture, service config, credential flow, commands. |
| `docs/api/`, `docs/decisions/`, `docs/development/` (besides this file) | Reserved, currently empty — for future API reference docs, architecture decision records, and other development documentation as the project progresses. |
| `backend/` | The Laravel 13 application (PHP 8.5). All 24 Phase 2A–2F migrations, Eloquent Models, and enums exist (Phase 3). Phase 4 Block 1 added Platform Admin authentication (`platform_admin`/`merchant` Sanctum guards, login/logout/me, feature tests) and re-scoped the `/api/user` stub to the `merchant` guard; no policies, `TenantContext`, business services, or merchant/customer auth flows yet. |
| `frontend/` | The React 19.2 + TypeScript + Vite application. Phase 4 Block 1 added the back-office application shell — Tailwind CSS, React Router, React Query, Platform Admin login/auth state, protected routing, admin layout (header/sidebar), a dashboard placeholder, and Coming Soon pages for every other nav item. No business feature UI or storefront UI yet. |
| `docker/php/Dockerfile`, `docker/php/entrypoint.sh` | PHP 8.5-FPM image (shared by `app` and `queue`) and its startup permission fix (see Infrastructure section above). |
| `docker/nginx/default.conf` | Nginx config — Laravel front-controller routing, denies `.env`/`.git`/`vendor`/etc. |
| `docker/mysql/my.cnf` | MySQL server configuration (no credentials). |
| `docker-compose.yml` | Defines all 6 services, the private `app-network`, and the `mysql_data`/`frontend_node_modules` volumes. |
| `.env` / `.env.example` | Root Docker Compose environment (MySQL credentials) — see "Root `.env` vs. `backend/.env`" above. |

## Development Rules

1. Read `CLAUDE.md` before making significant changes.
2. Read `project-status.md` before making significant changes.
3. Read the relevant architecture/database documentation before modifying the corresponding implementation.
4. Treat approved architecture and database design as authoritative.
5. Do not silently redesign approved architecture.
6. Do not introduce new dependencies without justification.
7. Do not implement speculative features.
8. Do not mark work as completed until it has actually been implemented and verified.
9. After completing a significant development phase, update `project-status.md`.
10. Record important decisions and deviations from the approved design.
11. Keep documentation and implementation consistent.
12. If implementation conflicts with the approved architecture, stop and report the conflict instead of silently changing the architecture.
13. Never rely solely on previous conversation context to understand project state.
14. Git history and project documentation should be treated as persistent project history.
15. Before starting a new major phase, verify the current state of the repository using `git status` and the relevant documentation.
